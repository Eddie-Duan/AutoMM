# AutoMM Agent 规则

> 完整项目指南见 `PROJECT.md`；自动研究循环见 `RESEARCH_LOOP.md`。两者是本文档的补充规范，冲突时以更具体的阶段/响应约束为准。

- 使用简体中文；公式、代码标识符和参考文献标题可保留原文。
- 先读取题面、配置和已有产物，再修改当前阶段的文件。
- 每次动作开始时，必须读取当前小问最新 accepted 假设版本的 `assumptions.md`，尤其是末尾的「待团队选定的决策点」「团队裁定」「团队勘误」小节。这些小节**效力高于**假设正文、公式正文与全局符号表的旧登记；冲突时按团队口径执行，并**在响应中显式记录冲突点与差异**，不得静默沿用旧登记。
- 不直接编辑 `runtime/workflow_state.json` 或受保护的 manifest 字段，状态变更必须通过响应 `commands`。
- 不覆盖假设、公式、结果、日志和结论历史；当前假设版本内的工作代码可以更新。
- implementation Agent 只做静态检查和小型探针，不运行完整数据集或等待 worker。
- 计算必须使用隔离 task、独立输出目录和 supervised worker。
- 任何 Harness invariant 都必须严格失败；模型计算的非关键故障应按 failure class 恢复或降级审查。
- 最终响应必须是符合 `config/agent_response.schema.json` 的 JSON。

## 响应 payload 纪律（团队补充，2026-09-11；**已实测 3 次**）

- `config/agent_response.schema.json` 里**每个 command 的 `arguments` 都是 `additionalProperties: false`**：
  多写**一个未登记的键**（哪怕只是备注）就会让**整批响应**判非法，**已完成的实质工作全部作废**。
- 后果链（实测）：schema 校验失败 → action 判 `harness_invariant` → `workflow` 返回机械 `blocked` →
  `runner` 把 `recovery_status` 覆写为 `human_blocked` → 之后**每次唤醒都秒返 `blocked`（1 秒空转），永久自锁**。
- 实测案例：`reason_note`（prob02）、`target_stage_note`（prob03 `act-1d13382f65c349a6`）、
  以及证据清单里的悬空路径（`probe_result3.xlsx` 写成 `result3.xlsx`）。
- **纪律**：`arguments` **只填 schema 逐字列出的键**；备注、说明、理由补充一律写进
  `findings` / `warnings` / `blocking_reasons`，**不要塞进 `arguments`**。
- 提交前自检：把每个 command 的 `arguments` 的键与 schema 对照一遍（键名逐字符一致、**不多不少**）。
- 被自锁卡住时：实质工作通常已完成，可用 `python scripts/harness.py control RESUME --source cli` 解锁
  （团队已修 `scripts/automm/state.py` 的 RESUME 分支，使其同时复位 `recovery_status` 与 `failure_class`）。

## 运行环境（团队提供的事实）

- CPU：32 逻辑核；内存：15.7 GB。
- GPU：NVIDIA GeForce RTX 5070 Laptop（8 GB 显存，Blackwell 架构），驱动 582.05，单卡。
- Python：3.14.4；torch 2.11.0+cu128 已安装并验证 CUDA 可用（`cuda.is_available()=True`，2026-09-10 实测）。

## GPU 使用规则

- 训练/推理/大规模矩阵类模型优先使用 GPU；代码内用 `torch.cuda.is_available()` 动态选择 device，不得硬编码 `cuda:0`。
- 显存预算 8 GB：batch_size 必须按显存预算设置；显存不足时用梯度累积或降 batch，禁止直接 OOM。
- 单卡 GPU：GPU 计算任务必须串行（同一时间最多 1 个 GPU 任务），task spec 中须注明该约束。
- 求解器（HiGHS 等）默认 CPU；只有数据规模大到 CPU 不可行时才考虑 GPU 路径。
- RTX 50 系（Blackwell）需要 CUDA ≥ 12.8：torch 必须装 cu128 及以上构建，否则 `cuda.is_available()` 为 False。

## GPU 依赖自装规则（torch CUDA 版）

- 当某阶段（实现、计算、鲁棒性等）确实需要 torch 且环境未装（或未装 CUDA 构建）时，允许 Agent 自行安装，不必等待人工；这是对「小型探针」约束的环境准备例外。
- 安装前先执行 `nvidia-smi` 确认驱动支持的 CUDA 版本；按 https://pytorch.org/get-started/locally/ 选择官方命令（RTX 50 系用 `--index-url https://download.pytorch.org/whl/cu128` 或更新）。
- Python 3.14 需要 torch ≥ 2.9。安装后必须运行 `python -c "import torch; print(torch.__version__, torch.cuda.is_available())"` 验证 CUDA 可用，并把验证输出写入当前动作目录作为安装证据。
- 只在确实需要 GPU 的步骤安装；不得在每次动作中重复安装；安装失败（网络/无匹配轮子）时记录证据并按 failure_class 路由，不得伪报安装成功，也不得因此人工 blocked。
- 不得卸载或升级与当前任务无关的依赖。

## prob04 交付结构：**一道小问、两套结果**（团队纪律，2026-09-11）

> 题面「问题 4」在**数学上等价于两道题**：在**实时波动电价**（附件 4）这一新情景下，**分别重算问题 2 与问题 3**。
> 交付件也是**两个**：`result4-2.xlsx`（≙ 问题 2 的全部结果）与 `result4-3.xlsx`（≙ 问题 3 的全部结果）。
> 题面原文：*「根据附件 2 中的小区负载和光伏发电、附件 3 中的光伏发电预报和附件 4 中的电价数据，在波动电价下重新计算问题 2 和问题 3」*。
> 因此 **prob04 的每一个阶段都必须同时覆盖 `4-2` 与 `4-3` 两条产物链**，**不得**只做一个、也不得把两条链混为一条。

- **阶段级强制要求**（缺少任一条即视为该阶段未完成）：
  1. `problem_understanding`：必须显式写出 `4-2` 与 `4-3` 的**两套问题定义**、**两套输入**、**两个交付件**，
     并登记「两者共用附件 4 电价、但决策结构与结算口径不同」（`4-2`：逐日计划 + 5× 价紧急购电；
     `4-3`：0:00 计划 + 6:00/12:00/18:00 调整 + 0.5×/1.5× 分段结算 + 紧急购电）。
  2. `assumption_definition`：两份口径**都要**登记（可共用一份 `assumptions.md`，但每条假设必须标明适用 `4-2`/`4-3`/两者）。
  3. `mathematical_formulation`：**两个模型**（或一个模型加两个明确参数化口径）必须**分别写出**，并各自给出信息集与价格可观测性口径。
  4. `implementation` / `computation`：**必须同时产出** `result4-2.xlsx` 与 `result4-3.xlsx`，各自有独立的 `output_directory` 与可核验的 `run_manifest`。
  5. `sanity_check` / `visualization` / `robustness` / `ablation`：判据与图表**必须分别指明**是哪一条链的结论。
- **模板事实（已登记，必读）**：`data/附件5/result4-2.xlsx` 与 `result2.xlsx` **逐字节相同**、`result4-3.xlsx` 与 `result3.xlsx` **逐字节相同**
  ——这是附件 5 的发放方式，**不是文件损坏**。**prob04 交付时必须新写这两个文件**，
  **不得**把 prob02 的 `result2.xlsx`、prob03 的 `result3.xlsx` 直接当作 prob04 的结果（价格情景不同，数值必然不同）。
- **待裁决策点已裁定**：`A8`（0:00 制定计划时，波动电价是否全天已知？候选 (a) 已知全天 / (b) 仅历史、需价格预测 / (c) 实时滚动观测）
  → **团队裁定 = `A8-(b)`（2026-09-11）**：**0:00 时当天实时电价（附件 4）未知**，决策只能用**历史价格（≤ 前一日 144 点）＋ 其他已有附件**；
  **当天电价到结算时才可知**。以下派生口径**强制**，须在 `assumptions.md` 逐条登记后方可进入 `mathematical_formulation`：
  1. **价格预测器必须显式给出**（候选：历史同时段均值 / 前一日同时段 / 季节+时段双因子 / 回归或 AR），
     并**用 2025 全年做滚动回测**（只用 t 日之前的信息）报告误差（MAE / MAPE / 分位数），**不得**只声称「用了预测」而无证据。
  2. **决策—结算分离**：决策层（计划层与 `4-3` 的调整层）用**预测价**；**结算一律用附件 4 的实际价**。
     费用分解必须**同时**给出按预测价与按实际价两套口径的差 ⇒ 即「**价格预报误差的代价**」（与 prob03 的 R11 同构），论文必须披露。
  3. **`4-3` 的调整时刻**：6:00 / 12:00 / 18:00 时价格**仍非全天已知** ⇒ 调整层用**滚动更新的价格预测**
     （信息集 = 截至该时刻已实现的实际价 + 历史 + 该时刻的光伏预报）。**每个层的信息集必须逐层写明。**
  4. **不确定性处理**：**主口径 = 价格点预测 + 确定性 LP**（B0：主结果只出一个主口径）；**鲁棒优化 / 场景随机**放 `ablation` 作对照，
     不得喧宾夺主。若要改主口径，必须先由团队裁定。
  5. **禁止**把附件 4 的实际价当作 0:00 已知（那是 `A8-(a)`，已否）；论文、`sanity` 验收脚本、图表注记中**不得**出现「全天电价已知」类表述。
  6. **两条链一致性**：`4-2` 与 `4-3` **共用同一价格预测器与同一结算口径**；差异仅在于 `4-3` 多了 6:00/12:00/18:00 调整层与 `0.5×`/`1.5×` 分段结算。

### prob04 预注册 `ablation` 对照项（团队定，2026-09-11；**必须在 `ablations/plan.md` 跑数前照录并冻结**）

> **定位**：主口径**不变**（B0：价格点预测 + 确定性 LP，`result4-2.xlsx`/`result4-3.xlsx` 按其出数）；本节各项**只进 `ablation`**。
> **必要性论证（必须写入论文）**：R11 实测 prob03 比 prob02 贵 **+2,307,565.50 元（+18.9%）** 且模型**无对冲机制**（T6 撤销 `PLAN-EXP`）；
> `knowledge/optimization.md` 要求「随机规划/鲁棒优化必须由问题结构与预算证明必要性」——上述差额与无对冲事实即论证。

- **`S-VAR`（价格场景两阶段随机对照）— 已批准**
  - **模型**：两阶段随机 LP。**第一阶段** = 计划购电量 `b`（**全场景共用**，here-and-now）；**第二阶段** = 场景内的调整/结算（`c, q_dis, q_em, E, q`）。
    目标：`min_b Σ_s p_s [ Σ_t c_t^(s)·b_t·Δt + Q_s(b) ]`。
  - **必须的改写（漏则数字错）**：**不得**把首阶段费用写成 `c^base·P_plan`。`A8-(b)` 下 0:00 电价未知、**结算一律用附件 4 实际价**，
    故**计划购电的账单本身是场景相关的**，必须落在 `Q_s` 内（数量共用、费用进场景）。
  - **场景数 = 24**，且**必须用附件 4 的历史实际价格构造经验场景**（对第 `d` 天取此前同型日的 144 点实际价向量、等概率；或同时段残差 bootstrap）。
    **不得凭空设分布**（E4/证据纪律）。
  - **规模裁剪**：**代表性日**（12 个，或与 prob03 一致的 4 个指定日 + 月度代表日）× 24 场景，写成**确定性等价**大 LP（`linprog` 直接解）。
    第二阶段线性 ⇒ 追索函数 `Q_s(b)` 凸分片线性 ⇒ **确定性等价与 Benders/L-shaped 同解**，故**不实现 Benders**（省代码、易独立复算）。
  - **报告四项**：(a) 期望费用相对主口径「点预测 + 确定性」的**下降幅度**；(b) 首阶段计划 `b` 的**形状差异**（对冲体现在哪些时段）；
    (c) **计算成本**（证明裁剪必要）；(d) 与 `A8-(b)` 的「价格预报误差的代价」并置披露。
  - **计算量标定（实测）**：单日链路 LP ≈ **0.104 s**（`M1` 全年 37.9 s ÷ 365）；12 代表日 × 24 场景 × 单日 ≈ **30 s**，完全可行。
- **`S-RH`（滚动时域 / 前瞻深度对照）— 已批准（2026-09-11）**
  - **模型**：滚动（receding-horizon）确定性 LP。**决策时刻与承诺规则完全不变**（`4-2`：每天 0:00 一次；`4-3`：0:00 + 6:00/12:00/18:00），
    **只改"优化时看多远"**：每次决策在 `[t, t+H]` 窗口上求解，**只提交属于当天（当层）的决策**，时钟前进后以更新状态重解。
    ⇒ **非预期性（AS07/AS11）自动满足**，窗口内的未来变量**只用于优化、不进入承诺**。
  - **前瞻深度 `H ∈ {1, 3, 7, 14}` 天**；**`H = 1` 必须逐位复现 `M1`（基线闸门）**。
  - **超 24 h 的预期（强制登记、必须有数据出处）**：题面**只给未来 24 h 光伏预报**，故 `H > 1` 的部分只能用历史：
    价格用已登记的 **`PF-PERSIST`**（只用 `≤ t−1` 的历史）；光伏用**附件 2 的同季节/同月历史均值**，
    并以「**最近 7 天实测**」作为第二口径做**敏感性**。**禁止**声称获得了超过 24 h 的预报。
    ⇒ 该假设必须登记进 `prob04` 的 `assumptions.md`（若该版本已 accepted，则走**团队勘误 E 节追加**，不改既有口径）。
  - **报告三项**：(a) 全期/交付期费用差；(b) **日边界储电量行为** —— 是否**不再贴 1200 下限**、
    跨日携带量（`Σ_d |E_{d,144} − 1200|` 与 `E_{d,144}` 的均值/唯一取值数）—— **直接回应 E8/R18**；
    (c) 每层与总求解时间。
  - **预注册判据**：① `H = 1` 基线闸门**逐位复现**；② `H` 增大 ⇒ 费用**不增**（违反则按 **T7-4 如实登记反例，禁止强凑单调性**）；
    ③ 末窗（窗口越过 12-31）按**终端自由**处理并登记。
  - **组合项（可选）**：`S-VAR-RH` = **24 场景 × `H = 3`**（12 代表日），检验「对冲 + 前瞻」是否**叠加**收益。
  - **计算量标定（实测）**：单日链路 LP ≈ **0.104 s** ⇒ `H=3` 全年 ≈ **114 s**、`H=7` ≈ **265 s**、`H=14` ≈ **530 s**；
    24 场景 × 12 代表日 × `H=3` ≈ **90 s**。
  - **定位**：**只进 `ablation`**，主口径不变；**禁止回溯修改 prob03**（`run003` + L1–L4 + L6 + `robustness` + `ablation` 全部会失效）。
  - **论文价值**：与 `M5a`/`M5b`（加密决策时刻）、`S-VAR`（场景对冲）一起，构成题面「是否需要引入其他时刻的预报」的**三层递进答案**。
- **`C-ANCHOR-P03`（实现正确性锚点）— 已批准（2026-09-11，团队勘误 `E-F4`）**
  - 把 `4-3` 的**价格数据整体替换为附件 1 的单日曲线**（365 天重复、0:00 全天已知 ⇒ `PF-PERSIST` 精确），
    其余输入（附件 2 实际、附件 3 预报）与 tie-break（`AS21` 字典序）不变 ⇒ **必须逐位复现 prob03 accepted `run003` 的 `M1`**：
    全期 `16,179,176.145228` 元 / 交付期 `14,540,616.335652` 元 / `C_plan 13,885,669.475555` / `C_adj 563,098.132533` /
    `C_em 1,730,408.537141` / 交付期 `Σq^em 391,274.485914` kWh。**判据 ≤ 1e-9（逐位）；失败即判实现错误。**
  - **口径放宽**：`prob04/formulations/formulation_v001` 的 `F-8`/§8.2「不得用 prob02/prob03 数值作**交付值或对照锚点**」
    **收窄为**「禁止**填充/替代/混入交付值**；**允许并鼓励退化情景复现类锚点**」。
- **主价格预测器报告纪律（团队勘误 `E-F5`）**：主口径 = **`PF-PERSIST`**（以 `MAE`/`MAPE`/`P50` 为主判据）；
  `PF-DUAL`、`PF-HIST` 作对照并**成对给出 `RMSE`/`P90`/`P99`**；**禁止只报单一指标**
  （实测两预测器优劣方向相反：`PF-PERSIST` MAPE 13.78% 更优、`PF-DUAL` RMSE 0.1216/P90 0.2065 更优）。
- **两条数据事实（`E-F2`/`E-F3`，论文强制披露）**：① 附件 4 含极端低价（`min 0.0076`、`<0.10` 仅 28 格）⇒
  以 `p_min` 构造的解析下界（136,272 元）**极松**，量级自检**以 `A2-PRE` 为准**；深谷时段 5× 紧急价仅 0.038 元/kWh。
  ② 附件 4 的**日内形状 ≡ 附件 1 曲线**（相关 1.0000、极值相同），差异在日间水平与逐点扰动 ⇒ 论文写一句，并作为 `PF-DUAL` 必需性的依据。