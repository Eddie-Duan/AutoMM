# 工作流阶段与 Agent 映射

> 本文整理 AutoMM 的 13 个流程阶段、各阶段对应的专职 Agent 提示词文件，以及映射中的例外情况。
> 权威来源：`config/workflow.yaml`（阶段顺序）、`config/agent_registry.yaml`（Agent 注册与阶段绑定）、`agents/*.md`（各 Agent 角色说明）。


``` powershell
orchestrator_runner.py #运行下一步

.\.venv\Scripts\Activate.ps1
python monitor\monitor.py #打开面板
```

## 一、13 个阶段（带注释版）

```text
#1.  problem_understanding        problem-decomposer        拆解小问、符号表、依赖图
#2.  literature_review            literature-researcher     文献检索、建文献池
#3.  assumption_definition        assumption-manager        定义建模假设（版本化）
#4.  mathematical_formulation     mathematical-formulator   建立数学模型（版本化）
#5.  implementation               implementation-agent      写求解代码
#6.  computation                  resource-manager          提交隔离计算任务
#7.  sanity_check                 sanity-checker            L1-L6 合理性检查
#8.  visualization                visualization-agent       生成图表
#9.  robustness（可选）            robustness-analyst        敏感性/鲁棒性分析
#10. ablation（可选）              ablation-analyst          消融实验
#11. locally_completed            —                          小问完成
#12. cross_question_review        cross-question-reviewer   全题跨小问一致性审查
#13. completed → 汇总/论文         build_final_summary / build_paper   研究汇总 + 论文
```

- 每个小问依次走阶段 1~11（阶段 9、10 可选，由对应 Agent 给出适用/跳过理由）。
- 所有小问 locally completed 后进入阶段 12（全局审查），通过后进入阶段 13。
- 阶段 13 的论文生成默认关闭（`config/paper.yaml` 的 `enabled`），开启后由 `scripts/build_paper.py` 走 draft → validate → final 链路。

## 二、阶段 ↔ Agent 提示词文件映射

| 阶段 | Agent 文件 | 备注 |
|---|---|---|
| 1. problem_understanding | `agents/problem-decomposer.md` | ✅ 一对一 |
| 2. literature_review | `agents/literature-researcher.md` | ⚠️ 另有按需复用触发（见例外 4） |
| 3. assumption_definition | `agents/assumption-manager.md` | ✅ 一对一 |
| 4. mathematical_formulation | `agents/mathematical-formulator.md` | ✅ 一对一 |
| 5. implementation | `agents/implementation-agent.md` | ⚠️ 一个管两个：兼 `computation_repair` |
| 6. computation | `agents/resource-manager.md` | ✅ 一对一 |
| 7. sanity_check | `agents/sanity-checker.md` | ⚠️ 一个管两个：兼 `computation_result` |
| 8. visualization | `agents/visualization-agent.md` | ✅ 一对一 |
| 9. robustness（可选） | `agents/robustness-analyst.md` | ✅ 一对一 |
| 10. ablation（可选） | `agents/ablation-analyst.md` | ✅ 一对一 |
| 11. locally_completed | ❌ 无 Agent | 纯机械状态（小问完成标记） |
| 12. cross_question_review | `agents/cross-question-reviewer.md` | ✅ 一对一 |
| 13. completed | ❌ 无 Agent | 脚本 `build_final_summary.py` / `build_paper.py` |

提示词注入机制（`scripts/automm/agent_runtime.py`）：

- 每个 Agent 被调用时，harness 实时读取其 `agents/*.md` 全文注入模型提示词——**改文件后下一步立即生效，无需重启**。
- 根目录 `AGENTS.md` 注入到**所有 Agent 的每一次调用**（全局规则）；`PROJECT.md`、`RESEARCH_LOOP.md` 同样全局可见。
- 作用域口诀：`agents/<名字>.md` 只改单个 Agent 行为；`AGENTS.md` 改所有 Agent 行为。

## 三、映射中的 4 个例外

### 例外 1：有的 Agent 管不止一个阶段

`config/agent_registry.yaml` 中的绑定：

```yaml
implementation-agent:  stages: [implementation, computation_repair]
sanity-checker:        stages: [sanity_check, computation_result]
```

### 例外 2：有的阶段没有 Agent，且存在大量机械动作

- `locally_completed`、`completed` 是**状态标记**，没有对应 Agent。
- 流程中还有由 Runner **直接执行、不调用 LLM Agent** 的机械动作：
  `blocked`、`poll_email`、`wait_for_compute`、`start_queued_compute`、
  `send_question_notification`、`advance_global_review` 等（policy P0~P9 动作）。

### 例外 3：有 3 个 Agent 文件不在流水线调用名单里

| 文件 | 为什么不在 |
|---|---|
| `agents/template-agent.md` | 只是**模板**（`stages: [agent_template]`），用于生成新 Agent，流水线不调用 |
| `agents/paper-writer.md` | **已禁用**（`enabled: false, stages: []`），初版本不调度；论文走 `build_paper.py` 脚本 |
| `agents/mathmodeling-orchestrator.md` | **不是被调度的 Agent**，是"全局决策策略"说明文档；实际控制由 `scripts/orchestrator_runner.py` 执行，registry 中无此条目 |

### 例外 4：literature-researcher 的按需复用

`agents/literature-researcher.md` 的触发条件不止首次 `literature_review`：

- formulation 失败回退且文献池 dry；
- 连续 3 个假设版本被拒绝（`rejected_versions_before_research_refresh: 3`）；
- Orchestrator 明确写入研究触发。

即文献研究阶段会在全流程中**被反复唤醒**，不是严格的一阶段一调用。

## 四、在阶段之间注入人工判断

每个阶段之间都可以安全停顿（状态持久化于 `runtime/workflow_state.json`），单步执行命令：

```powershell
.\.venv\Scripts\python.exe scripts\orchestrator_runner.py        # 跑一个动作即停
.\.venv\Scripts\python.exe scripts\orchestrator_daemon.py --once # 等价
```

停顿期间 4 种合法的人工介入方式：

1. **改对应阶段 Agent 的角色文件**（`agents/*.md`）——下一步立即生效。
2. **改根目录 `AGENTS.md`**——注入到所有 Agent 的每一次调用。
3. **改题面** `request/problem.md`——所有 Agent 都会读（论文阶段记得清理）。
4. **改版本内工作文件**——如 `formulation.md`、`assumptions.md`、当前版本 `code/*.py`（AGENTS.md 明确允许更新当前假设版本内的工作代码）。

受保护、不能直接改：`runtime/workflow_state.json`、question manifest 的门禁字段、假设/公式/结果/结论的历史版本（只能新建版本）。
