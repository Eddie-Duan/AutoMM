# prob03 消融实验报告（模型族对照 + 口径对照 + 结构消融）

- 阶段：`ablation`（`config/workflow.yaml: mandatory_stages`，**不得 skipped**，本报告以 `completed` 收尾）
- 小问：`prob03`（问题 3：0:00 计划 + 6:00/12:00/18:00 调整 + 紧急购电）
- 版本：`assumption_v001`（AS01–AS18 + 团队裁定 B0–B7 / T1–T9 + 团队勘误 R9–R25）与 `formulation_v001` 的 accepted `M1`
- 预注册方案：`ablations/plan.md`（**跑数前冻结**：模型集 `M1`–`M8`+`M8b`、判据 `C1`–`C10` + 补充检查 `C3L`、公平性纪律、输出隔离、失败处理）
- 交付口径对照基线：`results/prob03_v001_f001_run003`（task `6de708bbd2d3dd273013`，sanity L1–L4 `PASS_WITH_WARNING`）
- 汇总动作：`act-7173c11061af4805`（`ablation-analyst`）；执行回执见 `task_submission.md` §7 与 `runtime/tasks/ca4f2a45aa49a45236e3/`

> **效力说明**：团队裁定 `B0`–`B7`/`T1`–`T9` 效力高于 `agents/ablation-analyst.md` 的默认步骤。本报告按 B/T 组执行，
> 与默认步骤的差异已在 `plan.md` §0.1/§0.3 **逐条显式登记**，未静默沿用默认步骤；对照集合中未执行的条目（`M7·PLAN-EXP`）
> 已按 T6 写明撤销依据，未静默省略。

---

## 0. 执行回执与独立复核（本动作）

### 0.1 隔离计算 task

| 项 | 值 |
|---|---|
| task_id / attempt | `ca4f2a45aa49a45236e3` / 1 |
| status / rc | `succeeded` / `0`（`supervised`、CPU HiGHS、`gpu_required=false`） |
| 提交 / 起 / 止 | 2026-09-11T11:13:22Z / 11:15:00Z / 11:24:48Z |
| wall clock | **584.94 s**（整批预算 6600 s；`wall_seconds`=586.34 s 为含收尾写盘的口径） |
| output_directory | `problems/microgrid_2025/prob03/versions/assumption_v001/ablations/results/prob03_v001_ablation_run001/` |
| cases | 12/12 `ok`（`cases_failed=[]`）；`criteria_failed=['C3']`；`baseline_passed=true`、`anchor_passed=true`、`m6_oracle_passed=true` |
| code_hash（harness） | `33353b13255d25507d881ff034a5d2aa0478ed6aba803dc7ffc8183fa8977629` |
| config_hash / source_config_hash | `4c210b0dbc43cfdb737bf8e20869209690bce1c16502da8e7bad344cff73e729` / `0d2a455c22215df6596f2553dd2d9649527633714e6471e57ca07706b7220433` |
| input_hash | `4343d799b5830093d7a61d566219e1d2592f5bc39c8b7a9297286fa9e02d9434`（与 `run003` 同值 ⇒ `data/` 未被改动） |
| 追踪差异 | `run_manifest.json` 的 `code_sha256.ablation_prob03.py` = `eaafc64e1cd23c82…` 为**脚本自算**（仅 `*.py`），与 harness `code_hash`（含 `task_config.yaml`/`task_spec.yaml`）算法不同；**追踪与 task ID 一律以 harness 值为准** |

### 0.2 本动作的只读独立复核（54 项断言全通过，`checks_failed=[]`）

探针：`runtime/actions/act-7173c11061af4805/evidence/probe_verify_ablation.py`（输出 `.json` / `.stdout.txt`）。
**只读** `ablations/results/…/`、`results/prob03_v001_f001_run003/` 与 `prob02 …/run002/` 的既有 JSON；
**不求解 LP/MILP、不创建 task、不写 `results/`、不改 accepted 代码/数据/假设/公式**。

- **基线闸门（C1/C8）**：`M1` 与 `run003` 的 `objective_yuan`、`cost_plan/adj/em`、交付期费用、`Σq_em` 全期与交付期
  **逐位相同**（相对差 0.0）；`baseline_check.json` 89 项全通过、`label_check=true`；`delivery_consistency.json` 4/4 通过。
- **判据复算**：`C1/C2/C4/C5/C6/C7/C8/C9/C10` 与补充检查 `C3L` 的判定与阈值**逐项独立复算一致**；
  `C3` 的失败量级（`M8` 2.1672146e-05、`M8b` 1.6756115e-05）逐位复现，且 `criteria.json` 的阈值**未被事后修改**。
- **恒等式**：12 case 全部 `ok`、`failed=[]`；`settlement_balance/state_transition/cost_decomposition` 最大残差
  ≤9.8e-8、层状态 `max=0`；`M2b` 的跨日连续性残差 4800.000000002 属**预期违反**，其余 case ≤1e-7。
- **产物完整性**：14 个汇总产物与 12 个 `M*/case.json` 齐备；`raw_cases.jsonl` 12 行、无失败 case 被静默删除、
  无 NaN/Inf；5 张图均为合法 PNG。
- **不可覆盖性**：`run003` 目录内所有文件 mtime **早于**本 task 启动时刻 ⇒ 交付口径未被本次计算覆盖。
- **追踪链**：`code_sha256`（脚本自算）、4 个输入/模板 md5、`days=365/periods=52560`、
  `t7_tiebreak.rule`（T7-1 同一规则、`M1–M8` 全覆盖、`invariance_tol=1e-9`）全部核对通过。

---

## 1. 适用性判定（本阶段必做，不适用「跳过」）

`config/workflow.yaml` 已把 `ablation` 登记为 `mandatory_stages`，**不允许 `skipped`**。本报告回答三个问题：

1. 核心结论（`C_total` 及其分解、`Σq_em`、`Σc`、`Σq_dis`、表 1/2/3）**是否依赖模型族**；
2. 被消融项（跨日口径、完全信息、调整机制、决策时刻密度、LP 松弛、结算读法、降尺度口径、实现路线）各自的**经济贡献**；
3. 论文主表**推荐采用的模型及理由**（团队 B0：唯一候选 = `M1`）。

**不激活项按纪律处理，不作为跳过理由**：`M2a` 与 `M1` 算法同构（F6/T8）但仍作为**回归检查**保留并落盘；
`M7·PLAN-EXP` 在确定性口径下退化（T6），作为**撤销记录 + 退化证明**保留，其角色由 `M5` 承接。

---

## 2. 模型 × 指标总表（本阶段正式数值，来自隔离 task 产物）

统一口径：`D_full` = 365 天（含 1 月预热期）、`D_req` = 交付期 334 天；`Δt=1/6`、`η=0.9`、`c≤833.3333 kWh`、
`q_dis≤750.0000 kWh`（E1/D10 口径丙）、`E∈[1200,10800]`、`E_{1/1,0}=6000`、终端自由、`α_em=5`、
`β_def=0.5`/`β_over=1.5`、无购电上限（AS14）、无售电（AS08/D9-A）、CPU HiGHS、附件 4 与 2026 年数据**未使用**。

| case | 全期 `C_total`（元） | Δ全期 | 交付期 `C_total`（元） | Δ交付期 | `C_plan` | `C_adj` | `C_em` | `Σq_em`（kWh） | `Σc` | `Σq_dis` | `E_T` | 层数 | 耗时(s) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **M1**（主口径，B0） | **16,179,176.145228** | — | **14,540,616.335652** | — | 13,885,669.475555 | 563,098.132533 | 1,730,408.537141 | 406,353.048856 | 7,075,623.93 | 5,735,575.39 | 1,200 | 1,460 | 37.89 |
| M2a（逐日解耦+递推） | 16,179,176.145228 | +0.0000% | 14,540,616.335652 | +0.0000% | 13,885,669.475555 | 563,098.132533 | 1,730,408.537141 | 406,353.048856 | 7,075,623.93 | 5,735,575.39 | 1,200 | 1,460 | 44.60 |
| M2b（日初复位 6000） | 15,360,260.582255 | **−5.0615%** | 13,790,470.180228 | **−5.1590%** | 13,066,917.351550 | 562,934.694596 | 1,730,408.536109 | 406,353.048494 | 5,288,496.40 | 5,860,482.09 | 1,200 | 1,460 | 39.72 |
| M3（完全信息上界） | 13,758,182.573724 | **−14.9636%** | 12,233,050.830708 | **−15.8698%** | 13,758,182.573724 | 0.0 | 0.0 | 0.0 | 7,364,620.68 | 5,969,662.75 | 1,200 | 0（单段） | 4.53 |
| M4（仅 0:00，无调整） | 16,505,833.062742 | **+2.0190%** | 14,788,066.642523 | **+1.7018%** | 13,885,669.475570 | 0.0 | 2,620,163.587172 | 649,801.099900 | 7,404,616.48 | 6,002,059.35 | 1,200 | 365 | 10.53 |
| M5a（每 3 小时） | 16,179,575.041038 | +0.0025% | 14,541,015.231452 | +0.0027% | 13,885,669.475555 | 563,257.024744 | 1,730,648.540740 | 406,399.486051 | 7,075,623.93 | 5,735,575.39 | 1,200 | 2,920 | 69.90 |
| M5b（每 1 小时） | 16,179,766.212646 | +0.0036% | 14,541,187.024059 | +0.0039% | 13,885,669.475556 | 562,933.326225 | 1,731,163.410866 | 406,482.692474 | 7,075,623.94 | 5,735,575.39 | 1,200 | 8,760 | 194.79 |
| M7·D2-B（读法 B） | 16,125,753.608578 | **−0.3302%** | 14,489,176.271434 | **−0.3538%** | 13,633,407.146430 | 687,541.898247 | 1,804,804.563901 | 433,352.522324 | 7,075,880.03 | 5,735,782.83 | 1,200.000002 | 1,460 | 37.91 |
| M7·D2-C（读法 C） | 16,125,753.608578 | **−0.3302%** | 14,489,176.271434 | **−0.3538%** | 13,885,669.475297 | 435,279.569380 | 1,804,804.563901 | 433,352.522324 | 7,075,880.03 | 5,735,782.83 | 1,200.000002 | 1,460 | 37.97 |
| M7·D5-B（分段常数降尺度） | 18,741,130.202668 | **+15.8349%** | 16,830,326.022977 | **+15.7470%** | 14,099,453.495509 | 485,485.054652 | 4,156,191.652507 | 956,948.234431 | 6,896,675.10 | 5,590,626.83 | 1,200 | 1,460 | 37.29 |
| M8（独立第二实现） | 16,178,825.507754 | −0.0022% | 14,540,364.713961 | −0.0017% | 13,885,669.475555 | 563,812.131200 | 1,729,343.901000 | 405,995.365036 | 7,075,623.93 | 5,735,575.39 | 1,200 | 1,460 | 38.02 |
| M8b（+highs-ds/presolve=False） | 16,179,447.245364 | +0.0017% | 14,540,758.392355 | +0.0010% | 13,885,669.475554 | 563,812.131200 | 1,729,965.638610 | 406,165.857963 | 7,075,623.93 | 5,735,575.39 | 1,200 | 1,460 | 29.00 |

**全期口径**：Δ 相对 `M1`（`D_full`）；**交付期口径**：Δ 相对 `M1` 的 `14,540,616.335652` 元。
`M6` 只做 4 个指定日的日尺度 oracle（12 层，见 §4.6），无全期费用；`M7·PLAN-EXP` 按 T6 撤销、无数值。

---

## 3. 预注册判据判定（`C1`–`C10` + 补充 `C3L`，阈值跑数前冻结、事后未改）

| 判据 | 结果 | 关键数值 / 说明 |
|---|---|---|
| **C1** 基线闸门 | **PASS** | `M1` 与 `run003` 的 9 项总量 + 表 1（4 日期×6 槽=24 格）+ 表 2（6 块×4 日期 + 8 个端点）逐项一致：相对差 ≤1.1e-8、绝对差 ≤5e-7；`baseline_check.checks_failed=[]`、`label_check=true`（无 `exit 4`） |
| **C8** 交付一致性 | **PASS** | 表 3 四个指定日：区间集合逐字符一致，区间数 5/7/9/8，区间能量 2,062.489152778 / 840.356133333 / 1,150.194480556 / 352.514922222 kWh 与 `run003/tables.json` 逐格一致（≤1e-6） |
| **C2** `M2a≡M1` | **PASS** | `M2a` 相对差 **0.0**（阈值 1e-8）。按 T8，`C2` 是**回归检查**而非分解等价证据；`M2b` 差额 **−818,915.562973 元**按 C2 备注登记为**跨日携带价值**（结构性发现，**不判为实现错误**，机制归因见 §5.1） |
| **C3** `M8≡M1`（链级） | **FAIL（如实登记，阈值未改）** | `M8` 相对差 **2.1672146396890143e-05**、`M8b` **1.6756114994072834e-05**（阈值 1e-8）。归因见 §5.6：团队 T7-3 已证明 1024/1460 层在主目标最优面上仍有吞吐量多重最优，`M1` 为跨日向前递推（F1）⇒ 任何落在不同最优面点的独立实现都会经状态传播改变链级总费用；量级与 T7-6 的 2.2566e-04 **同阶**。**不修改阈值** |
| **C3L**（补充检查，非团队 `C1–C10`） | **PASS** | 把 `M1` 的提交输入（`e_start`、`plan_b`、支配层预报）喂入独立实现，**逐层**比较主目标最优值：16/16 层相对差 **0.0**（阈值 1e-9），两侧 `invariance_passed=true` ⇒ **实现/公式正确性独立成立**，`C3` 的链级差异是**最优面选择非唯一**而非实现缺陷 |
| **C4** 信息/机制单调性 | **PASS** | `M3` 13,758,182.573724 ≤ `M1` 16,179,176.145228 ≤ `M4` 16,505,833.062742 |
| **C5** 加密单调性 | **PASS（按 T7-4 的「反例机制可复现」口径）** | `M5b` 16,179,766.212646 **>** `M5a` 16,179,575.041038 **>** `M1` 16,179,176.145228 ⇒ **单调性不成立=False**，按 T7-4 **不得强行凑单调性**：如实登记「近视滚动 + LP 退化」反例机制（+590.07 / +398.90 元，相对 3.65e-05 / 2.47e-05），`M5a`/`M5b` 均成功执行 |
| **C6** `M6` MILP 精确化 | **PASS** | 12 层（4 日 × 3 调整层）MILP 与同层 LP 目标最大相对差 **3e-12**（阈值 1e-7）；`mip_gap=0.0`、`mip_node_count=1`、`z⁺+z⁻≤1` 互补性残差 0、紧 Big-M 残差 0 ⇒ **LP 松弛无损**；D6-A 降级（非全年 MILP）已显式登记 |
| **C7** `M7` 全量差额 | **PASS** | 3 个执行 case 的 `ΔC`/`ΔC_plan`/`ΔC_adj`/`ΔC_em`/`ΔΣq_em` **全部落盘**（`reading_deltas.json: complete=true, missing=[]`），本动作逐项独立复算一致；`PLAN-EXP` 的撤销有 T6 依据记录（未伪造数值） |
| **C9** 可解释性 | **PASS** | 采样 4 个 `q_em>0` 时段，全部可归因到「支配层预报盈余（=弃光 `s`）而实际短缺」；`layer_forecast_surplus_nonneg_ratio=1.0`；`C_adj` 两分段：欠发 0.0 + 超发 563,098.132533 = `C_adj`（残差 −0.0）⇒ 调整成本**全部来自超发方向** |
| **C10** 跨问独立锚点（F7） | **PASS** | `M3` 全期 13,758,182.573724 元、交付期 12,233,050.830708 元 == prob02 accepted `run002` 的 `objective_yuan` / `delivery_cost_yuan`（本动作**另行只读读取 prob02 run002 复核**，相对差 0.0）⇒ 本问唯一不依赖自身实现的外部锚点闭合 |

**判据纪律**：`C1`–`C10` 的阈值与比较轴在 `plan.md` §3 跑数前冻结，结果出来后**未做任何修改**；
`M6` 的降级（D6-A）、`PLAN-EXP` 的撤销（T6）、`C5` 的反例机制（T7-4）均以「登记」而非「改判」处理。

**T7 家族口径的逐 case 审计（`case.json.summary.t7`）**

| case | 层数 | 字典序提交 | **fallback** | 主目标相对变化 | 全层不变性 | 退化层 | 最优面仍多重 | 重数未定论 | 吞吐量（kWh） |
|---|---|---|---|---|---|---|---|---|---|
| M1 | 1,460 | 1,460 | **0** | 5e-10 | true | 1,460 | 1,024 | 1 | 31,781,301.97 |
| M2a | 1,460 | 1,460 | 0 | 5e-10 | true | 1,460 | 1,024 | 1 | 31,781,301.97 |
| M2b | 1,460 | 1,460 | 0 | 5e-10 | true | 1,460 | 1,024 | 1 | 30,118,156.10 |
| M4 | 365 | 365 | 0 | 5e-10 | true | 365 | 365 | 0 | 13,406,675.82 |
| **M5a** | 2,920 | 2,919 | **1** | 5e-10 | true | 2,920 | 1,881 | 10 | 53,646,280.64 |
| **M5b** | 8,760 | 8,754 | **6** | 5e-10 | true | 8,760 | 4,620 | 26 | 147,640,634.65 |
| M7·D2-B | 1,460 | 1,460 | 0 | 5e-10 | true | 1,460 | 1,460 | 0 | 31,780,183.51 |
| M7·D2-C | 1,460 | 1,460 | 0 | 5e-10 | true | 1,460 | 1,460 | 0 | 31,780,183.51 |
| M7·D5-B | 1,460 | 1,460 | 0 | 5e-10 | true | 1,460 | 1,003 | 4 | 31,112,401.02 |
| M8 | 1,460 | 1,460 | 0 | 5e-10 | true | 1,460 | 1,023 | 1 | 31,782,268.86 |
| M8b | 1,460 | 1,460 | 0 | 5.04e-10 | true | 1,460 | 1,023 | 2 | 31,782,268.86 |
| M3 | 0（单段 LP） | 0 | 0 | 0.0 | **false（空集，见 §7-③）** | 0 | 0 | 0 | 0.0 |

- 全部 case 的 `max_primary_relative_change ≤ 5.04e-10 ≤ 1e-9`、`all_layers_invariance_passed=true` ⇒ **T7-1 次目标未改变任何主目标最优值**。
- **T9-② 纪律**：`M1`/`M2a`/`M2b`/`M4`/`M7`/`M8`/`M8b` 的 `fallback_committed_layers=0`；
  **`M5a=1`、`M5b=6`** 出现 fallback（>0），按 T9-② 必须逐个说明原因，但 task 产物**只落盘计数、未落盘逐层 provenance** ⇒ 登记为追踪链欠账（§7-②）。

---

## 4. 逐项消融结果（每次只改一项；`M5`/`M8` 按 B2/T7-5 豁免）

### 4.1 跨日口径：`M2a` / `M2b`（C2）

- `M2a`（逐日解耦 + 状态递推）与 `M1` **逐位同值**（相对差 0.0，含 `C_plan/C_adj/C_em`、`Σq_em`、`Σc`、`Σq_dis`）
  ⇒ 与 T8/F6 一致：这是**结构性恒等式**，只能作回归检查（能捕捉日边界/状态传递 bug），**不能**作为分解等价的独立证据。
- `M2b`（日初复位 `E_{d,0}=6000`）：全期 **−818,915.562973 元（−5.0615%）**、交付期 −5.1590%。
  按 C2 备注记为**跨日携带价值**的结构性发现；其**机制归因**见 §5.1（不得读作「解耦更省」）。

### 4.2 信息结构：`M3`（完全信息上界，C4/C10）

- `M3`（0:00 即知全天附件 2 实际值 → 单一 52,560 时段 LP，`q≡b`、`q_em≡0`）全期 **−2,420,993.571504 元（−14.96%）**。
  该值即「三层预报驱动 + 实际结算」相对完全信息的**信息价值缺口**，也是 R11「问题 3 比问题 2 贵 18.863%」的另一面：
  两问交付期净负荷逐位相同（17,939,189.49 kWh），差额纯由**预报误差 + 无对冲机制**造成，**不得**声称问题 3 更省。
- `M3` 与 prob02 accepted **逐位相同**（C10 PASS）⇒ 跨问锚点闭合，同时**验证了本问 `M3` 的实现口径**（F7 的「先怀疑自身」条款不触发）。

### 4.3 调整机制：`M4`（仅 0:00 计划、无调整，C4）

- `M4` 全期 **+326,656.917514 元（+2.0190%）**、交付期 +1.7018% ⇒ **调整机制（6:00/12:00/18:00）的价值 = 326,656.92 元（全期）**。
- 分解：`C_em` 由 1,730,408.537141 → 2,620,163.587172（**+889,755.050031 元**，`Σq_em` 406,353.05 → 649,801.10 kWh，紧急购电时段 12,164 → 14,124），
  `C_adj` 由 563,098.132533 → 0（−563,098.132533 元），`C_plan` 几乎不变（+0.000015 元）⇒
  调整的价值几乎全部来自**减少 5× 紧急购电**，净额 = 889,755.050031 − 563,098.132533 + 0.000015 = **326,656.917513 元**，与 `ΔC`（326,656.917514 元）吻合到 1e-6 元以内。
- `M4` 的 `days_with_deviation=0`（无调整 ⇒ 无偏差项）：其可行性与题意一致，费用上升属预期。

### 4.4 决策时刻密度：`M5a` / `M5b`（A9，C5）

A9 的评价指标与对照集合由团队 D12-A/AS18 冻结（**同一发布集 D5-A：新增决策时刻只用最近一次已发布预报**）：

| 方案 | 每日决策时刻数 | 全期 `C_total`（元） | Δ全期 | `Σq_em`（kWh） | 紧急购电触发次数（时段/天） | `Σc`（kWh） | wall(s) |
|---|---|---|---|---|---|---|---|
| `M4` 仅 0:00（无调整） | 1 | 16,505,833.062742 | +2.0190% | 649,801.099900 | 14,124 / 365 | 7,404,616.48 | 10.53 |
| `M1` 题面四点 | 4 | 16,179,176.145228 | 基准 | 406,353.048856 | 12,164 / 365 | 7,075,623.93 | 37.89 |
| `M5a` 0:00+每 3 小时 | 8 | 16,179,575.041038 | +0.0025% | 406,399.486051 | 12,164 / 365 | 7,075,623.93 | 69.90 |
| `M5b` 0:00+每 1 小时 | 24 | 16,179,766.212646 | +0.0036% | 406,482.692474 | 12,167 / 365 | 7,075,623.94 | 194.79 |

**A9 结论（限定为「决策频率」，不含「新增预报信息」的价值）**：

1. **有调整 ≫ 无调整**：从 `M4` 到 `M1` 收益 326,656.92 元（+2.02%），是本题唯一量级显著的「时刻数」效应；
2. **在题面四点之上的加密没有收益**：`M5a`/`M5b` 比 `M1` 反而**贵** +398.90 / +590.07 元（+0.0025% / +0.0036%），
   且 `Σq_em` 随之微增；该量级 **≤ T7-6 登记的主结果 tie-break 敏感性 2.2566e-04 相对量级**，
   **不得**解读为「决策越密越差」的因果结论，应表述为「**加密决策时刻在本数据下无可测收益**」；
3. **成本单调上升**：wall 10.53 → 37.89 → 69.90 → 194.79 s（相对 `M1`：0.28× / 1× / 1.85× / 5.14×），层数 365→8,760；
   结合 ② 得出「**无需引入其他时刻的预报（决策频率层面）**」的可用结论；
4. **发布加密不可评估**：附件 3 只在 0/6/12/18 发布；本阶段按 D5-B 备选 A 纪律**不构造不存在的输入**，
   故 A9 的结论**不覆盖**「新增预报信息量的价值」（须团队批准新的数据构造才能评估）。

### 4.5 结算读法：`M7·D2-B` / `M7·D2-C`（T2，C7）

| 量 | `M7·D2-B`（读法 B） | `M7·D2-C`（读法 C） |
|---|---|---|
| 自身口径 `ΔC`（相对 `M1`） | **−53,422.536650 元（−0.3302%）** | **−53,422.536650 元（−0.3302%）** |
| `ΔC_plan` | −252,262.329125 | −0.000258 |
| `ΔC_adj` | +124,443.765714 | −127,818.563153 |
| `ΔC_em` | +74,396.026760 | +74,396.026760 |
| `ΔΣq_em` | +26,999.473468 kWh | +26,999.473468 kWh |
| 交付期 `ΔC` | −51,440.064218 元（−0.3538%） | −51,440.064218 元（−0.3538%） |
| **同计划在 D2-A 主口径下 recost** | **+198,839.792217 元** | **+198,839.792217 元** |

- **必须成对报告**：读法 B/C 的「−53,422.54 元」是**换口径重优化后的自报值**；若把 B/C 的最优计划放回 **D2-A 主口径**重算，
  该计划比 `M1` **贵 198,839.79 元**。两个数字口径不同（前者换目标、后者换计划），**不得**只引用其中一个。
- **读法 B ≡ 读法 C（代数恒等）**：`p·min(b,q) + 0.5p(b−q)⁺ = p·b − 0.5p(b−q)⁺` ⇒ 总费用相同（逐位 16,125,753.608578 元），
  分解项不同（`C_plan` 与 `C_adj` 互补）。该恒等式是**发现**，`C7` 仍要求两条都跑、全量落盘（已完成）。
- 读法 C 的「0.5× 作退款」在计划量可自由选择时会产生**高报计划**的非物理激励；本题由 AS07 的计划层约束抑制，但仍**不作候选主口径**（A7 备选 C）。
- `M7·D2-B/C` 的 `layers_with_remaining_multiplicity = 1460/1460`（全部层在主目标最优面上仍多重最优，`fallback=0`）⇒
  其链级数值同样受最优面选择影响，引用时须与 T7-6 口径一并说明。

### 4.6 LP 松弛精确性：`M6`（D6-A 降级，C6）

- 4 个指定日（2025-03-20 / 06-21 / 09-23 / 12-21）× 3 个调整层 = **12 层** MILP-oracle：
  `status=0`（Optimal）、`mip_gap=0.0`、`mip_node_count=1`、最大相对损失 **3e-12**、
  `z⁺+z⁻≤1` 互补性残差 0、逐时段**紧** Big-M（`u⁺ ≤ b` 实测上界 0–1,502.76；`u⁻ ≤ L·Δt + c_cap + Π_m·Δt` 实测 1,233.5–3,330.49）残差 0
  ⇒ **LP 辅助变量线性化无损**，与引理 L5 一致。
- **降级登记（D6-A）**：全年 1,460 层 MILP **未执行**；无损性由「4 日 oracle + 全年 LP 损失界（L5）」承担。规模上界：单层二元变量 ≤216、追加不等式 ≤540，全年 MILP 在 60 s/层与整批预算下不具可行性。

### 4.7 降尺度口径：`M7·D5-B`（分段常数，T3，C7）

- `M7·D5-B` 全期 **+2,561,954.057440 元（+15.8349%）**、交付期 +15.7470%；
  其中 `ΔC_em = +2,425,783.115366 元`（占 94.7%）、`ΔΣq_em = +550,595.185575 kWh`、`ΔC_plan = +213,784.019954 元`、`ΔC_adj = −77,613.077881 元`。
- **T3 能量偏差统计**（`d5b_energy_stats.json`，只统计各发布时刻的**支配域**）：

| 发布时刻 | 支配域时段数 | 插值口径能量（kWh） | 分段常数能量（kWh） | 平均绝对差（kWh/时段） | 最大绝对差（kWh） | 相对差 |
|---|---|---|---|---|---|---|
| 0:00 | 52,560 | 20,015,051.6212 | 20,015,051.6212 | 45.44 | 438.03 | 0.0 |
| 6:00 | 39,420 | 20,263,776.723983 | 20,034,544.9049 | 54.44 | 386.31 | **+1.1312%** |
| 12:00 | 26,280 | 9,858,043.663783 | 8,744,947.5167 | 42.49 | 366.65 | **+11.2912%** |
| 18:00 | 13,140 | 141.861458 | 100.1375 | 0.0032 | 1.21 | +29.4118% |
| 合计（支配域并集） | — | 50,137,013.870425 | 48,794,644.180300 | 43.01 | 438.03 | **+2.6774%** |

- **机制**：分段常数（前向保持）在支配域内**系统性低估光伏 1,342,369.69 kWh（−2.68%）**，尤以 12:00 域（−11.29%）为甚；
  低估 ⇒ 决策层自以为短缺 ⇒ `Σq_em` 上升 550,595.19 kWh ⇒ 5× 紧急购电 ⇒ `C_em` +2.43M 元。
- **披露义务（T3）**：线性插值**不保块内能量守恒**（块能量由插值曲线积分给出），该点已在假设 §6/D5-A 与 `formulation` 披露；
  本表的 per-period RMSE 探针值（303.92 / 580.62 kW 等）**只作假设裁定的诊断依据，不得作为论文或 sanity 的交付数值**。

### 4.8 实现路线与求解器设置：`M8` / `M8b`（C3 / C3L）

- `M8`（变量/行次序重排 + **消去弃光变量 `s`**，同求解器设置）链级相对差 2.167e-05；
  `M8b`（再加 `highs-ds` + `presolve=False`）1.676e-05。两者**均超** `C3` 的 1e-8 阈值（FAIL，如实登记）。
- `C3L`（同一输入、逐层比较主目标最优值）**16/16 层相对差 0.0** ⇒ 差异**不在实现或公式**，而在**主目标最优面取点**。
- 三条独立证据支持该归因：① 团队 T7-3 审计（`run003/t7_tiebreak.json`）1024/1460 层 `throughput_unique=False`；
  ② 本批 `M1`/`M8`/`M8b` 的 `degenerate_layers=1460/1460`、`layers_with_remaining_multiplicity=1024/1023/1023`；
  ③ 量级（2.2e-05 / 1.7e-05）与 T7-6 的 2.2566e-04 **同阶**，且远小于任何结构性效应（最小结构效应为 M4 的 2.0e-2）。
- **纪律**：不得为凑 `C3` 阈值而改 tie-break 规则、调 `ε` 或换求解器设置（那会破坏 B5 公平性并掩盖 T7-3 的退化事实）。
  该冲突须由团队在 `cross_question_review` 阶段确认（§7-①）。

### 4.9 `M7·PLAN-EXP`（T6 撤销，未执行、未伪造数值）

在 AS15/AS16 的确定性、无预报误差分布口径下，计划层对未来的唯一无偏代理就是 0:00 预报 ⇒ `q=b`、偏差项与 `q_em` 均为 0，
`PLAN-EXP` 与 T1 主口径 `min Σp·b` **恒等退化**。团队 T6 裁定撤销其独立对照 case；本阶段**不执行、不伪造数值**，
其「计划层信息集变化的影响」角色由 `M5` 承接（§4.4）。R11④ 建议的「给定误差分布量化对冲收益」需**新增误差分布假设**（属 ablation 的诊断性 sensitivity），本阶段未做（无该假设）。

---

## 5. 结论段（三个必答问题）

### 5.1 核心结论是否依赖模型族？

**不依赖（在 LP 结构族内）**，但对**口径与信息结构**高度敏感 —— 必须分层表述：

1. **实现路线/求解器无关**：`M2a` 与 `M1` 逐位同值；`M8`/`M8b` 与 `M1` 的链级差 ≤2.2e-05（相对），
   且 `C3L` 逐层为 0.0 ⇒ 主结果（`C_total` 10^7 元量级、`Σq_em`≈4.06×10^5 kWh、`E_T=1200`、表 1/2/3 结构）**不是实现假象**。
2. **决策时刻密度无实质影响**：`M5a`/`M5b` 与 `M1` 的差 ≤5.9e-4 元/kWh 级（≤3.65e-05 相对），与 tie-break 敏感性同阶。
3. **真正决定数值量级的是三项口径/结构选择**：完全信息 vs 预报驱动（`M3`，−14.96%）、有无调整机制（`M4`，+2.02%）、
   降尺度规则（`M7·D5-B`，+15.83%）。其中降尺度口径的影响**大于**调整机制的价值，是本题**最需要审慎披露**的口径项。
4. **结算读法（D2-B/C）影响 −0.33%，但同一计划跨读法重算差 +1.23%（198,839.79 元）** ⇒ 口径歧义必须在论文正文披露，且**两个数字成对出现**。

### 5.2 被消融项的经济贡献（全期 `D_full`，相对 `M1`）

| 排序 | 被消融项 | 经济贡献（元） | 相对 | 机制 | 是否可解读为「收益」 |
|---|---|---|---|---|---|
| 1 | 完全信息（去预报误差） | **−2,420,993.57** | −14.96% | 消除 `C_em`+`C_adj`+计划层信息缺口 | 上界，非可实现 |
| 2 | 线性插值降尺度（vs 分段常数） | **+2,561,954.06** | +15.83% | 分段常数低估光伏 1.34M kWh ⇒ `C_em` +2.43M | 口径差（非技术收益） |
| 3 | 跨日连续（vs 日初复位 6000） | **−818,915.56** | −5.06% | 364×4800 kWh 人造注入（残差 4800.000000） | **否**（非物理，§5.1 注） |
| 4 | 调整机制（6/12/18） | **+326,656.92** | +2.02% | 减少 5× 紧急购电 889,755.05，代价为偏差 563,098.13 | 是（真实可执行） |
| 5 | 结算读法 B/C（自身口径） | **−53,422.54** | −0.33% | 未履约部分改按 50% 结算 | 口径差；同计划跨读法 +198,839.79 |
| 6 | 决策加密 3 小时 | **+398.90** | +0.0025% | 近视滚动 + LP 退化 | 无可测收益 |
| 7 | 决策加密 1 小时 | **+590.07** | +0.0036% | 同上 | 无可测收益 |
| 8 | 独立实现路线 | **−350.64** | −0.0022% | 最优面取点经跨日传播 | 否（非实现优势） |
| 9 | 求解器设置（M8b） | **+271.10** | +0.0017% | 同上 | 否 |
| — | LP 松弛（`M6`） | 0（≤3e-12 相对） | 0 | 线性化无损（引理 L5） | 无损失可消融 |

**`M2b` 的机制归因（本动作补充，不改变 C2 的登记口径）**：`M2b` 的 −5.0615% 与跨日连续性残差
**4800.000000002 kWh** 同源 —— `E_{d,0}=6000` 是对链式递推 `E_{d,0}=E_{d−1,144}=1200` 的**人工复位**，
第 2–365 天每天凭空多出 4800 kWh（共 **1,747,200 kWh**）；其 `ΔC_plan = −818,752.124005 元`，
折合 **0.4686 元/kWh**，与附件 1 平价/谷段电价量级一致，且 `ΔC_em≈−0.001 元`、`ΔC_adj≈−163.44 元`（几乎不变）。
⇒ **`M2b` 的差额应表述为「跨日连续性约束的影子价格/携带价值」，不得表述为「逐日解耦可省 5%」**，
也不得作为可实现策略（它在日界处不满足能量守恒）。按 B4-C2 备注，该差额**仍不判为实现错误**。

### 5.3 论文主表推荐采用的模型及理由

**推荐 `M1`（团队 B0 的唯一候选）**，理由：
① `C1` 基线闸门 89 项与 `run003` **逐位一致**、`C8` 表 3 逐格一致 ⇒ 主表可复现；
② `C3L` 逐层等价 0.0、`fallback_committed_layers=0`、`max_primary_relative_change=5e-10≤1e-9`、全层 `invariance_passed=true` ⇒ tie-break 不改变主目标；
③ `M6` oracle 证明 LP 松弛无损（≤3e-12）⇒ 无需 MILP；
④ `M2a`/`M5a`/`M5b`/`M8`/`M8b` 的数值与 `M1` 不可区分（≤3.7e-05 相对）⇒ 换用它们不带来收益，反而增加规模或丢失与 `run003` 的逐位可比性；
⑤ `M3`（完全信息）与 `M4`（无调整）**只作上下界对照**，不得替换主结果；
⑥ `M7·D5-B` 的影响（+15.83%）说明降尺度口径必须在论文中与主结果并列披露，主表仍用 `M1` 的 D5-A 线性插值口径。

---

## 6. 与 `robustness` 阶段的边界（团队 B6/E9，无冲突）

- **不重复**：参数扰动（±5/10/20%）、输入噪声、求解器压力、结构约束压力属 `robustness`（`prob03_v001_robust_run001`，
  task `9d890669fb66a1bcc3ae`），本阶段**未重复**，其数值只作**带出处引用**。
- **购电上限阈值（E9/R19）**：`robustness` 给出的 **决策层（0:00 预报、无 `q_em` 变量）可行上界夹逼 `(4375, 5000] kW`** 只作引述；
  本阶段**未重跑** b 上限扫描，**不得**与 prob02 的 `β ∈ (4218.75, 4375.00] kW` 混用；本问 AS14 无购电上限，R5 不触发。
  同时**不得再写**「低于 10,326 kW（=10,325.33）即强制 `q_em>0`」。
- **终端/携带口径（E8/R18）**：`robustness` 的 `terminal_e_6000` 情景（+3,325.625 元 / +0.0228713%）与本阶段 `M2b`
  （日初复位）是**两个不同口径**，数值不得互替：前者改终端、后者改日初且破坏连续性。
- **稳定性分级**：`robustness` 的 `criteria_failed=[S1,S5]`、`stability_grade=条件稳定` 与 S4 FAIL（族间均值差 7.2946%、
  同 σ=5% 子集 0.0641%）**原样进入论文**，本阶段不改写、不与之混写。

---

## 7. 冲突点、偏离与技术债（显式登记，不静默）

### 7.1 与团队预注册的冲突（须团队在 `cross_question_review` 确认）

1. **`C3`（团队 B4）与 T7-3 的内在冲突（跑数前已冻结登记）**：`C3` 要求链级 `|ΔC(M8,M1)|/C(M1) ≤ 1e-8`，
   而团队 T7-3 自己证明 1024/1460 层在主目标最优面上仍有吞吐量多重最优，字典序规则**不能唯一确定提交点**；
   `M1` 为跨日向前递推（F1），故任何落在不同最优面点的独立实现都会改变链级总费用。
   本阶段**不修改阈值**，如实报 `C3` FAIL，并以 `C3L`（同一输入逐层等价 ≤1e-9，实测 0.0）单独承担**实现正确性**判定。
   **请团队裁定**：是否把 `C3` 的链级阈值改为「最优面选择敏感性」披露项、或把 `C3L` 升为正式判据。
2. **T9-② 的 fallback 说明义务未闭合（追踪链欠账）**：`M5a` 的 `fallback_committed_layers=1`、`M5b=6`
   （`prob03_model._solve_with_tiebreak` 中字典序重解未返回 `status=0` 时回落到加权式/基线解），
   task 产物**只落盘计数**，未落盘**逐层 provenance**（层 id、`committed_solution`、原因）⇒ 无法按 T9-② 逐个说明。
   影响面：所有层的 `max_primary_relative_change=5e-10≤1e-9`、`all_layers_invariance_passed=true` ⇒
   **主目标未被改变**，fallback 只影响最优面内的吞吐量取点；但**数值可追溯性弱于 `M1`**。
   建议：若要闭合，需在后续版本落盘逐层 `t7` 记录（不改模型，只加落盘字段）。
3. **`M3` 的 `t7.all_layers_invariance_passed=false` 属空集语义**：`M3` 是单一 52,560 时段 LP（非分层链，实测 262,800 变量 / 105,120 等式行），
   `t7` 列表为空 ⇒ `bool(t7) and all(...)` 求值为 `False`。该字段对 `M3` **不适用**，
   引用时不得读成「不变性验证失败」；建议后续版本对空层集输出 `null` 或 `not_applicable`。

### 7.2 图件缺陷与技术债（本动作新增，仅呈现层）

- **`figures/ablation_identities.png` 字段口径错误（task 原始图，保留不覆盖）**：该图读取
  `case.json.summary["identities"]["settlement_balance_max" | "state_transition_max" | "cross_day_continuity_max"]`，
  但这三个键实际位于 `summary["residuals"]`（`identities` 下的键名为 `settlement_max` / `transition_max` / `continuity_max`），
  `.get(...) or 0.0` 又把缺失值兜底为 `0.0`、再被下限 `1e-16` 截断 ⇒ **三个系列在图中全部落在 1e-16**，
  真实残差（`state_transition_max` 最大 9.8e-08、`M2b` 跨日连续性 4800.000000）与判据阈值 1e-6 **完全不可见**。
- **处置**：新增**只读**报告级重绘脚本 `ablations/report_figures.py`（只读 `ablations/results/…/M*/case.json`，
  不改 `ablations/code/`、不覆盖 task 原始图），输出修正版
  `ablations/figures/ablation_identities_fixed.png`（1800×840，含阈值线与 `M2b` 预期违反注释，图像抽检确认无遮挡）。
- 其余 4 张 task 图（`ablation_model_cost` / `ablation_reading_delta` / `ablation_decision_density` / `ablation_criteria`）
  本动作已逐张图像抽检，**无标签重叠、无截断**（`ablation_criteria` 的 C3 柱缺省为 0 是**预期**，表示未通过）。
- 按团队 B5/R24 纪律：**本阶段 5 张 task 图 + 1 张修正版图均不登记为交付图表**（不调用 `record_figure_review`），
  只作报告级证据；交付图表仍是 visualization 阶段已登记的 8 张 `included_in_paper` 图。若论文需要，须另写公开版绘图脚本并 `record_figure_review`。

### 7.3 结转技术债与强制披露项（不在本阶段处理）

- **披露义务（论文/`cross_question_review`）**：R11（问题 3 不得声称更省，须写明 +2,307,565.50 元 / +18.863% 的预报误差成因与无对冲机制）、
  R12（`periods_with_q_em_and_charge=5,070` 的机制解释与最小可复现示例）、R13（2025-02-01 0:00 储电量 prob02=7,950 / prob03=1,200 kWh 不可互比）、
  R1（表 2 端点语义须与 prob01 单日周期恒 6,000 区分）、R5/R19（购电上限阈值表述）、
  T7-3/T7-6/E7（退化与多重最优、tie-break 敏感性 1e-4 量级、`2025-10-12 18:00` 层重数未定论）、
  E6（`D_full`/`D_req` 与 `Σb`/`Σq` 口径）、R10（值口径 vs xlsx XML 字面口径）、E8（日边界恒为 1200 kWh 的成因与量级）。
- **文献证据边界（E4）**：prob03 文献池 25 条**全部 `status=used` 但 0 条全文**（15 `abstract_oa` + 10 `metadata`）；
  本报告未新增文献级主张，任何公式级/定量级引用须先取得全文。AS07/AS09/AS10 本质是题面与团队裁定口径，**不得声称有文献支持**。
- **D10「5000 kW 作用侧」**为 `team_decision`（口径丙），prob01/02/03 三池文献**无一条涉及**，论文不得包装为文献支持；
  本批全部 case 的 `max_side_power_kw=5000.000`（并网点侧与电池侧取更严者）为**口径约束生效**的证据，其中 `M1` 两侧均顶到上限。
- **上游登记欠账**：`prob01/assumption_v003/version.yaml` 的 AS08「该变量在部分时段必然被激活」措辞（C3/E3）权威回写待 `cross_question_review`；
  本阶段按「变量保留、最优解允许取 0」执行。
- **未裁定项**：A5/A9/A10/A11/A13/A14/A18 仍为 `assumption_definition` 推荐口径并保留决策点；
  本阶段未重裁，A9 只按已冻结的 D12-A 出结论（§4.4）。
- **`question_manifest.conclusion`**：本动作已用 `record_conclusion` 补齐（此前为空会使 `locally_completed` 门禁报错）。
- **数值纪律**：本报告全部数值来自隔离 task 产物；本动作的只读探针数值（§0.2）只作判定证据，不作为论文交付数值。

---

## 8. 产物清单与可追溯性

`ablations/results/prob03_v001_ablation_run001/`：

| 产物 | 作用 |
|---|---|
| `baseline_check.json` | C1：`M1` vs `run003` 的 9 项总量 + 表 1（24 格）+ 表 2（6 块 + 8 端点），89 项全通过、`label_check=true` |
| `delivery_consistency.json` | C8：表 3 四个指定日的区间集合/数量/能量逐格一致 |
| `anchor_check.json` | C10：`M3` vs prob02 accepted 锚点（全期/交付期相对差 0.0） |
| `milp_oracle.json` | C6：12 层 MILP oracle、Big-M 推导与紧性残差 |
| `reading_deltas.json` | C7：`M7·D2-B/D2-C/D5-B` 的全量差额 + `PLAN-EXP` 撤销依据 |
| `d5b_energy_stats.json` | T3：插值 vs 分段常数的光伏能量偏差统计 |
| `explainability.json` | C9：`q_em` 归因抽样 + `C_adj` 两分段复算 |
| `layer_equivalence.json` | C3L：16 层的逐层等价性（相对差 0.0） |
| `comparison.json` / `solver_status.json` / `identities.json` / `criteria.json` / `summary.json` | 模型×指标表、逐 case 求解状态与耗时、四条恒等式残差、判据判定、汇总与结论 |
| `raw_cases.jsonl` | 逐 case 原始汇总（12 行，含全部 case，未静默删除） |
| `run_manifest.json` | task/追踪指纹、参数、决策集、`code_sha256`、计数、耗时 |
| `M*/case.json` | 12 个 case 的逐 case 汇总（含 `daily_cost_total_yuan`、`daily_state_end_kwh`、`statistics`、`t7`） |
| `figures/*.png` | 5 张实验图（**不登记为交付图表**） |
| `../figures/ablation_identities_fixed.png` | 本动作修正版恒等式残差图（报告级） |

**复现路径**：`.venv/Scripts/python.exe ablations/code/ablation_prob03.py --days 365 --time-limit 60 --max-wall-seconds 6600 --milp-time-limit 60`
（完整命令与参数见 `code/task_spec.yaml`；本阶段未重跑，仅复核）。

---

## 9. 本阶段收尾结论

- `ablation` 阶段**已按团队 B0–B7 / T1–T9 强制执行完毕**（12 个 case，覆盖模型族对照 + 口径对照 + 结构消融，
  未缩减、未跳过；唯一未执行项 `M7·PLAN-EXP` 有 T6 撤销依据）。
- **预注册判据**：`C1`/`C2`/`C4`/`C5`/`C6`/`C7`/`C8`/`C9`/`C10` PASS，补充检查 `C3L` PASS，
  **`C3` FAIL（如实登记、阈值未改）**，`criteria_failed=['C3']`。
- **主结果不因模型族而变**（实现路线、求解器、决策密度差异 ≤3.7e-05 相对）；数值量级由**信息结构、调整机制、降尺度口径**决定；
  **论文主表推荐 `M1`**（B0）。
- 本阶段**未**产生任何新的交付数值，**未**替换 `run003` 的主结果，**未**占用 GPU，**未**修改 accepted 代码/数据/假设/公式。
- 阶段以 `record_optional_stage(stage="ablation", decision="completed", …)` 收尾，
  `question_manifest.conclusion` 三字段已补齐；推荐下一阶段 `locally_completed`（gates 允许的唯一迁移）。
