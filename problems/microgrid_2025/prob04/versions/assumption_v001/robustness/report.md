# prob04 `robustness` 阶段报告（`assumption_v001` / `formulation_v001`）

- 归属：`microgrid_2025` / `prob04`（波动电价下重算问题 2 与问题 3，交付 `result4-2.xlsx` 与 `result4-3.xlsx` **两个**文件）
- 阶段：`robustness`（`config/workflow.yaml: mandatory_stages`；本阶段以 `record_optional_stage(decision="completed")` 收尾，**不 skipped**）
- 收尾动作：`act-2f778ac1e7d04071`（负责人：robustness-analyst；策略 P5 / action=run_agent）
- 预注册方案：同目录 `plan.md`（§0 适用性、K1–K8、S1–S7、扰动矩阵、样本量、种子、失败处理、边界、CF-1–CF-10，**跑数前冻结、事后未改**）
- 权威产物（**只读**，本报告一切数值的唯一出处）：
  - 链 `4-2`：`results/prob04_v001_robust_4-2_run001/`，task `953eeb1e0b004171cad9`，`succeeded` / `rc=0` / `supervised` / CPU HiGHS / `probe_mode=false` / `days=365` / `periods=52560`
  - 链 `4-3`：`results/prob04_v001_robust_4-3_run001/`，task `5713b0a24eedeac9f000`，`succeeded` / `rc=0` / `supervised` / CPU HiGHS / `probe_mode=false` / `days=365` / `periods=52560`
- 本阶段**不写任何交付值**：未创建/覆盖 `results/prob04_v001_f001_4-2_run002`、`..._4-3_run002`，未触碰 `data/附件5/`，未修改 accepted 代码、`assumptions.md`、`version.yaml`、`runtime/workflow_state.json`
- 承接的团队口径（效力高于本报告）：`AGENTS.md`（prob04 两链结构、`A8-(b)` 六条派生口径）、本版本 `assumptions.md` 文末「团队裁定」`R-1`/`R-2` 与勘误 `E-F2`–`E-F5`、**`E-F6`–`E-F9`**、团队 `R5`/`E9`/`T7-1`/`T7-4`/`T7-6`/`AS21`/`E1`/`E6`

---

## 0. 适用性决定（复述 `plan.md` §0，本阶段按此执行）

| 类别 | 适用性 | 理由（摘要） |
|---|---|---|
| 参数扰动（设备/经济） | **适用** | 两链 `η_ch/η_dis`、`E_min/E_max`、`c/q_dis` 上限、`E_init`、`α_em`、`κ_m` 界与（仅 `4-3`）`β_def/β_over` 均为题面硬参数或团队裁定值 |
| 输入扰动（电价/负载/光伏） | **适用** | 附件 4 电价同时进**决策层（作为预测器历史）**与**结算层（实际价）**；附件 2 实际光伏在 `4-2` 进决策层、在 `4-3` **只进结算层** ⇒ 同一扰动两链传导路径不同，是本阶段信息量最大的对照 |
| 预测机制扰动 | **适用** | 主口径价格不确定性**全部**来自预测器（`A8-(b)`）；`PF-DUAL`/`PF-HIST` 是 `AS08` 强制基准 |
| 求解器压力 | **适用** | 两链均为确定性 LP；三种 HiGHS 设置给出链级量级并与 `T7-6` 的 `1e-4` 口径敏感性对照 |
| 结构约束压力 | **适用** | 终端储电量（`AS03`）与购电上限（`AS06`/`D10`）是题面/团队结构选择；**分链**扫掠 |
| 初值扰动 | **适用（并入参数族）** | `E_init` 只出现在 2025-01-01 0:00 |
| 静置损耗 / 自放电（`AS23`） | **不适用（本阶段不做）** | 实现它必须改写状态转移方程（`E ← E(1−λ)`）⇒ 属建模口径变更（prob03 同级纪律为「不增设」）。**未跑**，见 §6 `R4-C1`（`plan.md` §0/§1.8 与 §7 `CF-3` 的措辞冲突已显式登记） |
| 随机初始化 | **不适用** | 确定性 LP，求解器无随机源 |
| Sobol/Morris 全局方差分解 | **不适用** | 无输入分布证据（`AS12` 确定性口径）；强赋分布属证据越界 |
| 「价格预报误差缩放因子」类扰动 | **不适用（禁止）** | 该构造令决策价依赖当天实际价 ⇒ 违反 `A8-(b)` 派生⑤ 的信息集纪律 |
| `S-VAR`/`S-RH`/`S-VAR-RH`、`C-ANCHOR-P03`、`A2-PRE`、`M7·D2-B/C`、`PF-AR` | **不在本阶段** | 团队 `R-1`/`R-2`/`E-F4` 与 `AS08` 明文只进 `ablation`；本阶段只引用、不重跑 |

> **结论**：本阶段**未返回 `skipped`**，在适用的五类中全部产出数值结果，且**每一条判据与数值均分别指明属于 `4-2` 还是 `4-3`**。

---

## 1. 基线闸门（两链各自，`baseline_check.json`，只读对账）

| 项 | `4-2` | `4-3` |
|---|---|---|
| 参考目录 | `results/prob04_v001_f001_4-2_run002`（task `892351beefa2bc5f7804`） | `results/prob04_v001_f001_4-3_run002`（task `eca261e1d516ab2867d9`） |
| 19 项总量 `relative_diff` | **全部 0.0** | **全部 0.0** |
| 表 1（4 个指定日 × 6 个时段） | 逐格一致 | 逐格一致 |
| `worst_relative_diff` | **0.0** | **0.0** |
| `baseline_check_passed` | `true` | `true` |
| 基线（交付期 `D_req`，334 天） | `C_total = 13,006,411.041148` 元 | `C_total = 15,289,050.714738` 元 |
| 基线（全期 `D_full`，365 天） | `C_total = 14,755,884.322036` 元 | `C_total = 17,181,202.515506` 元 |

§4.9 的基线闸门（compact 与 `run002` 总量相对差 ≤ `1e-6`，`4-2` = `6.26 s`/情景、`4-3` = `22.11 s`/情景的年内 compact 口径）在两链上**逐位通过**，未触发 `EXIT_BASELINE_GATE_FAILED`。

**链内基线的已知口径（只读复核，与 accepted L4 一致）**

- `4-2`：`C_total = C_plan`（无 `C_adj`、`Σq^em ≡ 0`）；`D_req` 信念价口径 `C_total^fc = 12,485,620.202113`、`ΔC_price = 520,790.839035` 元；`Σs = 990,168.090312 kWh`；`E_{d,144} = 1200`；`max c = 833.333333 kWh`、`max q_dis = 750.0 kWh`、两侧功率 `≤ 5000 kW`。
- `4-3`：`D_req` `C_total = 13,106,641.487373 (plan) + 522,512.788776 (adj) + 1,659,896.438588 (em)`；`C_total^fc = 15,030,565.249034`、`ΔC_price = 258,485.465704` 元；`Σq^em = 394,026.040750 kWh`；`days_with_emergency = 365`、`periods_with_emergency = 12,356`、同充放 `1,456` 时段、`q_em&c` 同时正 `6,003` 时段、`deviation_plus = 61,160.196868 kWh`、`deviation_minus = 493,977.438284 kWh`；`E_{d,144} = 1200`。
  - 口径提示（承 `E6`/N2）：`sensitivity.json` 行内 `delta_c_price_yuan` 是**全期**量；交付期 `ΔC_price` 以 `baseline_check.json`/raw 记录的 `delivery.delta_c_price_yuan` 为准（`4-3` = `258,485.465704` 元，即 N2 登记的 `(G-4)` 读法）。两者不得混用。

---

## 2. 预注册判据 S1–S7 判定（**分链**，阈值与分级规则均按 `plan.md` §3，未事后修改）

### 2.1 链 `4-2`（task `953eeb1e0b004171cad9`）

`scenario_count = 162`、`solved_count = 162`、`failed_count = 0`、`wall_seconds = 966.94`、`lp_calls_total = 59,130`、`budget_exceeded = false`。
**`failed_criteria = []`，`stability_grade = 稳定`。**

| 判据 | 判定 | 关键数值 |
|---|---|---|
| `S1` 可行性 | **PASS** | 受判子集 `{baseline, param, solver, noise}` `judged = 139`、`solved = 139`、`feasible_rate = 1.0`、`failed_scenarios = []`、`identity_failed_scenarios = []`；残差最大：层等式 `2.73e-12`、层不等式 `0.0`、界违反 `0.0`、`I1/I2` `1.09e-11 / 2.91e-11`、结算平衡 `3.41e-13`、状态转移 `2.73e-12`、跨日连续 `0.0`、费用分解 `0.0`（全部 ≤ `1e-6`） |
| `S2` 紧急购电机制 | **PASS** | `max\|Σq^em\| = 0.0 kWh ≤ 1e-9`、`max_days_with_emergency = 0`（`AS16` 定理级结论） |
| `S3` OAT 方向不反转 | **PASS** | 8 个 OAT 组中 **7 个有方向组全部 `reversals = []`**（`eta_both/eta_ch/eta_dis/alpha_em/p_max/e_min/e_max`），`e_init` 为诊断组（`passed=null`）。`alpha_em` 组 6 档费用**逐位相同**（`q_em ≡ 0` ⇒ `α_em` 在本链**是空参数**，该组 `passed` 为恒真）；`e_init` 组 6 档**交付期费用逐位相同** |
| `S4` 输入噪声 | **PASS** | 4 族 × 25 = 100 样本；每族 `std_rel ≤ 0.71%`、`CI95 半宽相对 ≤ 0.28%`、`max\|z\| ≤ 2.012`、`failures = []`、`mean_rel_vs_baseline ≤ 0.54%`、同 σ=5% 档内族间均值差 `0.4463%` ≤ 3% |
| `S5` 求解器 | **PASS** | 三设置全可行；链级最大相对差 `1.25189e-4` ≤ 1%（与 `T7-6` 登记的 `1e-4` 同量级） |
| `S6` 结构情景 | **PASS** | `buy_cap` 12 档**全域可行**（`buy_cap_infeasible = []`，含最低档 3500 kW）；`terminal_e_6000` 有数值（`D_req = 13,008,590.707803`，`+0.0168%`） |
| `S7` 预测机制 | **PASS** | `predictor_pf_dual`/`predictor_pf_hist` 跑通，回测 7 项指标齐全（`MAE/MAPE/RMSE/bias/P50/P90/P99`，见 §4） |

### 2.2 链 `4-3`（task `5713b0a24eedeac9f000`）

`scenario_count = 198`、`solved_count = 195`、`failed_count = 3`、`wall_seconds = 4052.82`、`lp_calls_total = 284,700`、`budget_exceeded = false`。
**`failed_criteria = ["S4"]`，`stability_grade = 条件稳定（需给出适用边界）`。**

| 判据 | 判定 | 关键数值 |
|---|---|---|
| `S1` 可行性 | **PASS** | 受判子集 `judged = 170`、`solved = 170`、`feasible_rate = 1.0`、`failed_scenarios = []`、`identity_failed_scenarios = []`；残差最大：层等式 `8.71e-8`、层不等式 `6.24e-8`、界违反 `7.68e-8`、`I1/I2` `7.84e-8 / 7.84e-8`、结算平衡 `4.55e-13`、状态转移 `8.71e-8`、跨日连续 `0.0`、费用分解 `0.0`（全部 ≤ `1e-6`） |
| `S2` 紧急购电机制 | **PASS** | 基准 `Σq^em = 410,453.995927 kWh`（`D_full`）；受判样本 `min = 404,201.444938`、`max = 837,192.510757`，落于宽带 `[102,613.498982, 1,641,815.983708]`（`[0.25×, 4×]`）、`out_of_band_scenarios = []`；窄带 `[0.5×, 2×]` 越界样本逐条披露（`noise_white_10_001/003/004…`，按 `CF-10` 不判失败） |
| `S3` OAT 方向不反转 | **PASS** | 10 个 OAT 组中 **8 个有方向组全部 `reversals = []`**（`eta_both/eta_ch/eta_dis/alpha_em/p_max/beta/e_min/e_max`），`e_init` 与 `kappa_bounds` 为诊断组（`passed=null`）；`alpha_em` 在本链**有效**（`±20% ⇒ ±2.171353%`）；`kappa_bounds` 放宽/收紧费用差 `0.0` |
| `S4` 输入噪声 | **FAIL** | 5 族 × 25 = 125 样本；**违反项：`noise_white_10` 族均值相对基线 `+10.5919%` > 预注册 `5%`**；其余 4 族达标（`white_5 +3.75%`、`day_5 +3.67%`、`joint_day_5 +4.04%`、`pvfc_5 +3.45%`）。`std_rel ≤ 0.89%`、`CI95 半宽相对 ≤ 0.35%`、`max\|z\| ≤ 2.543`、`failures = []` 均达标；同 σ=5% 档内族间均值差 `0.5654%` ≤ 3%，跨 σ 档 `6.7954%` 仅按 `CF-10` 披露、不作判据 |
| `S5` 求解器 | **PASS** | 三设置全可行；链级最大相对差 `6.10152e-4` ≤ 1%（`highs-ipm`） |
| `S6` 结构情景 | **PASS** | `buy_cap` 12 档全部产出状态，**不可行 = [3500, 4000, 4500] kW**，可行 `≥ 5000 kW`，分界 `bracket_kw = [4500, 5000]`；`terminal_e_6000` 有数值（`D_req = 15,292,320.214739`，`+0.0214%`） |
| `S7` 预测机制 | **PASS** | `predictor_pf_dual`/`predictor_pf_hist`/`kappa_frozen_1` 跑通；回测 7 项指标齐全 |

### 2.3 分级（按 `plan.md` §3 预注册规则）

- `4-2`：S1–S7 **全 PASS** ⇒ **稳定**。
- `4-3`：S1 PASS 且**恰 1 项非 PASS（`S4`）** ⇒ **条件稳定（需给出适用边界）**（与团队勘误 `E-F9` 一致）。
- 3 个 `ok=false` 样本（`buy_cap_3500/4000/4500 kW`，`HiGHS status=2` 不可行）是 **`plan.md` §4.9 预注册的预期结果**，保留在产物中、计入可行率分母但**不计入 S1 受判子集**；`S6` 正是据此定出分界。

---

## 3. K1–K8 判定（**分链**；`T7-4`：违反即如实登记反例 + 机制 + 适用边界，禁止为凑结论改判据/统计量/口径）

| 编号 | `4-2` | `4-3` | 判定 |
|---|---|---|---|
| **K1** 量级与恒等式 | `D_req = 1.3006e7` 元、`D_full = 1.4756e7` 元，均落 `[3e6, 5e7]`；`C_total = C_plan + C_adj(0) + C_em(0)`，分解残差 `0.0` | `D_req = 1.5289e7` 元、`D_full = 1.7181e7` 元，均落带内；三分项残差 `0.0` | **两链成立** |
| **K2** 紧急购电机制 | `Σq^em ≡ 0`（`max\|·\| = 0.0`、`days = 0`），表 3/「紧急购电量」全零（须给机制说明，承 prob02 `D7-A`） | `Σq^em > 0`（`D_req 394,026.040750`、`D_full 410,453.995927` kWh）；`q_em&c` 同时正 `6,003` 时段、同充放 `1,456` 时段，**只作统计与机制归因** | **两链成立**（方向相反，`CF-5` 分链不得混用） |
| **K3** 参数方向 | 7 个有方向组不反转（`S3` PASS） | 8 个有方向组不反转（`S3` PASS） | **两链成立**（`κ_m` 界放宽/收紧费用差为 `0.0`，仅证明截断未被触发） |
| **K4** 输入噪声 | 4 族全部 `std_rel ≤ 5%`、`CI95 ≤ 5%`、同 σ 档族间差 `≤3%` | **`noise_white_10` 族均值 `+10.59%` > 5%**；其余达标 | `4-2` **成立**；`4-3` **部分不成立**（σ=10% 超设计工况） |
| **K5** 求解器与实现 | 三设置全可行、恒等式在容差内，最大相对差 `1.25e-4 ≤ 1%` | 三设置全可行，最大相对差 `6.10e-4 ≤ 1%` | **两链成立**（如实登记为最优面选择 + 跨日初值传播的口径敏感性，与 `T7-6` 同机制） |
| **K6** 结构情景 | `buy_cap` 全域可行、`terminal_e_6000` 差额 `+0.0168%`；静置损耗按 §0 不做并登记 | `buy_cap` 分界 `(4500, 5000] kW`、`terminal_e_6000` 差额 `+0.0214%`；静置损耗不做 | **两链成立**（`CF-7`：与 prob02 `β∈(4218.75, 4375.00]`、prob03 `[4375.0, 5000.0]` **只登记不互引**） |
| **K7** 价格预报误差是主口径第一不确定性源 | **反例**（`max` 口径）：价格类最大 `\|ΔC_total\|/C = 10.0%`（`price_level ±10%`）< `eta_both` 的 `13.35%`；预测机制族仅 `0.226%`、价格白噪声 σ=10% 仅 `1.97%`。`mean` 口径成立：`input 均值 2.8677% > param 均值 1.5532%` | **反例**（`max` 口径）：价格类最大 `10.0%` < `eta_both` 的 `11.70%`；预测机制族 `0.171%`、价格白噪声 σ=10% `1.76%`。`mean` 口径成立：`input 均值 7.6289% > param 均值 1.3740%` | **两链在 `max` 口径均不成立（如实登记反例）**；`mean` 口径成立。真正的一阶通道：`4-2` = 往返效率 `η_both`（`±20% ⇒ +13.35%`）；`4-3` = **光伏预报误差**（`pv_forecast_bias_p10 +23.25%`、`pv_forecast_noise_w20 +22.81%`、`pv_act_noise_w10 +10.22%`） |
| **K8** 两链同号且 `4-3` 不低敏 | 见下 | 见下 | **同号：成立**（10/10 共同情景符号一致）；**「`4-3` 不低于 `4-2`」：部分不成立** |

**K8 明细（共同情景，交付期相对变化；`4-2` → `4-3`）**

| 情景 | `4-2` | `4-3` | 同号 | `\|4-3\| ≥ \|4-2\|` |
|---|---|---|---|---|
| `price_level_m10 / p10` | `∓10.0000%` | `∓10.0000%` | 是 | 是（相等） |
| `price_noise_w5 / w10` | `+0.4077 / +1.9724%` | `+0.3706 / +1.7593%` | 是 | **否** |
| `load_noise_w5 / w10` | `+0.1416 / +0.2022%` | `+0.1176 / +0.1479%` | 是 | **否** |
| `pv_act_noise_w5 / w10` | `+0.0894 / +0.1584%` | `+3.7819 / +10.2238%` | 是 | 是（`4-3` 为 `4-2` 的 **42× / 65×**） |
| `predictor_pf_dual`（= `pf_hist`） | `−0.2261%` | `−0.1711%` | 是 | **否** |

**K7/K8 反例的机制解释（须写入论文，`T7-4`）**

1. **`4-2` 的最大敏感源是设备参数而非价格**：`4-2` 的决策层已用附件 2 **实际**负载/光伏（`D3-A`），等式平衡 + 自由处置使计划量对**价格水平**只作近线性响应（`price_level ±10% ⇒ ±10.0000%`），而价格**噪声/预测器替换**只改变最优点（`≤ 2%`）⇒「价格预报误差是第一不确定性源」在本链不成立。
2. **`4-3` 的一阶通道是光伏预报误差，不是价格**：`4-3` 的决策/调整层用附件 3 **降尺度预报**，实际光伏只进结算层，缺口由 `5×` 实际价的 `q^em` 承接（不可对冲）⇒ 预报高估 `10%` 时 `Σq^em` 由 `178,679` 跳到 `1,439,009 kWh`，费用 `+23.25%`；而价格侧（预测器替换 `0.171%`、价格白噪声 σ=10% `1.76%`）是次要通道。
3. **`4-3` 在价格/负载/预测器族上反而更不敏感**（K8 反例）：调整层用**当日已实现价格**做 `κ_m` 水平校正（`D6-A`），对价格噪声形成部分对冲；且 `0.5×/1.5×` 分段结算只在偏差方向上加权，不放大价格水平本身。该反例与 `K8` 预期的「`4-3` 更敏感」相反，**不得**为凑单调性修改判据。
4. **`κ_m` 的价格信息增益在本数据上费用口径为 0**：`kappa_frozen_1`（`κ_m ≡ 1`）的 `D_req` 与基线**逐位相同**（`0.0`）；`PF-DUAL` 与 `PF-HIST` 亦**逐位相同**（`−0.1711%`，非独立的第二条敏感性轴）⇒ 本问的价格预测精度改善在本数据/费用口径下**未兑现为一阶经济收益**（属 `A8-(b)` 派生③ 的诚实结论，须与 `E-F5` 的「指标上两预测器各有胜负」并置披露）。

---

## 4. 敏感性汇总（`sensitivity.json`；`4-2` 与 `4-3` 分列）

### 4.1 参数族龙卷（`max|ΔC_total|(D_req)| / C_base`；`α_em` 与 `e_init` 为诊断组）

| 族 | `4-2` | `4-3` |
|---|---|---|
| `eta_both`（`η` 同比例，`−20%/−5%/+5%/+10%`） | **`+13.345316%`**（`0.72` 档，`+1,953,341.06` 元）/ `−6.278770%`（`0.99` 档） | **`+11.700362%`** / `−5.341454%` |
| `e_max`（`−20%/−10%/+10%`） | `+5.140946% / −2.289435%` | `+4.142357% / −1.868444%` |
| `eta_dis`（单侧） | `+4.280111% / −3.979908%` | `+3.669763% / −3.401843%` |
| `eta_ch`（单侧） | `+2.339672% / −1.820523%` | `+2.141446% / −1.617992%` |
| `alpha_em` | `0.000000%`（**空参数**：`q_em ≡ 0`） | `+2.171353% / −2.171353%`（`4-3` 有效） |
| `p_max`（上限与 `c/q_dis` 同步） | `+0.780013% / −0.212174%` | `+0.728087% / −0.209093%` |
| `beta`（`β_def/β_over` 同比例） | —（`4-2` 无偏差结算项） | `+0.683512% / −0.683512%` |
| `e_min` | `+0.495189% / −0.481260%` | `+0.405151% / −0.382703%` |
| `e_init` | `0.000000%`（**交付期零响应**；全期仅 `3.26e-5` 相对极差） | `0.000000%`（全期 `2.80e-5`） |
| `kappa_bounds`（放宽/收紧） | — | `0.000000%`（两档同值） |

> 表中「龙卷极差」`spread_relative`（`max−min`，按全期 `C_total` 归一）与「单档 `max/min`」（按交付期 `C_base` 归一）**口径不同**，不得混引（见 `R4-10`）。

> `plan.md` §4.2 声明：`eta_both` 的 `+20%`（`1.08`）被物理上限 `η ≤ 1` 截断、`e_max` 的 `+20%`（`12,960`）截到登记上限 `12,000`，两处已在产物 `label` 中标注。

### 4.2 `4-3` 专属诊断：`κ_m` 截断

`structural_findings.kappa_clip_events_total = 196`，**全部来自刻意收紧的 `kappa_bounds_tight`（`[0.75, 1.25]`）**：`196 / 1,460` 层被截断（`13.4%`），而该情景的费用相对差为 **`0.0`**；登记界 `[0.5, 2.0]` 与放宽界 `[0.25, 4.0]` 均 **0 次截断**。⇒ **截断机制在登记口径下未被触发，且在成本上非紧**；`196` 次截断**不得**被读作「主口径发生了截断」（`N1` 的 `κ_m ∈ [0.66233, 1.54939]` 与本结论一致）。

### 4.3 输入族（交付期相对变化）

| 情景 | `4-2` | `4-3` |
|---|---|---|
| `price_level_m10 / p10` | `−10.0000% / +10.0000%` | `−10.0000% / +10.0000%` |
| `price_noise_w5 / w10` | `+0.4077% / +1.9724%` | `+0.3706% / +1.7593%` |
| `load_noise_w5 / w10` | `+0.1416% / +0.2022%` | `+0.1176% / +0.1479%` |
| `pv_act_noise_w5 / w10` | `+0.0894% / +0.1584%` | `+3.7819% / +10.2238%` |
| `pv_forecast_bias_m10 / p10` | — | `+0.6799% / +23.2485%` |
| `pv_forecast_noise_w10 / w20` | — | `+9.7030% / +22.8149%` |
| `predictor_pf_dual` / `pf_hist`（`4-3` 另加 `kappa_frozen_1`） | `−0.2261% / −0.2261%` | `−0.1711% / −0.1711% / 0.0000%` |

`CF-4` 分离逻辑成立：`price_level`（只改水平）给出**近线性**响应（`±10% ⇒ ±10.0000%`），`price_noise`（改误差、期望水平不变）给出小响应（σ=10% 仅 `+1.97%`，约为同幅度水平扰动的 `1/5`），二者成对分离「价格水平」与「预报误差」两种效应。

### 4.4 输入噪声族（每族 `n = 25`，`95% CI`）

| 链 | 族 | 交付期均值（元） | `std_rel` | `CI95 半宽相对` | `max\|z\|` | **族均值相对基线** |
|---|---|---|---|---|---|---|
| `4-2` | `noise_white_5` | `13,018,406.156` | `0.065%` | `0.026%` | `1.973` | `+0.0922%` |
| `4-2` | `noise_white_10` | `13,048,181.446` | `0.131%` | `0.051%` | `2.012` | `+0.3212%` |
| `4-2` | `noise_day_5` | `13,022,289.962` | `0.636%` | `0.249%` | `2.008` | `+0.1221%` |
| `4-2` | `noise_joint_day_5` | `13,076,598.592` | `0.709%` | `0.278%` | `1.935` | `+0.5396%` |
| `4-3` | `noise_white_5` | `15,861,642.263` | `0.110%` | `0.043%` | `2.294` | `+3.7451%` |
| `4-3` | **`noise_white_10`** | `16,908,452.788` | `0.208%` | `0.081%` | `2.211` | **`+10.5919%`（S4 失败项）** |
| `4-3` | `noise_day_5` | `15,850,391.965` | `0.839%` | `0.329%` | `2.543` | `+3.6715%` |
| `4-3` | `noise_joint_day_5` | `15,906,188.168` | `0.891%` | `0.349%` | `2.182` | `+4.0365%` |
| `4-3` | `noise_pvfc_5` | `15,816,519.292` | `0.082%` | `0.032%` | `2.433` | `+3.4500%` |

- 两链的 `std_rel ≤ 5%`、`CI95 半宽 ≤ 5%`、`max|z| ≤ 4`、`failures = []` **全部达标**；`4-3` 唯一越界的是「每族均值相对基线 ≤ 5%」在 `σ = 10%` 价格白噪声档。
- `4-3` 各噪声族均值**系统性为正**（`+3.45% ～ +10.59%`）而 `4-2` 近零（`≤ +0.54%`）：`Σq^em` 是 `max(0,·)` 泛函，噪声以凸性抬高期望紧急购电（`CF-10` 的先导结论被正式跑证实）。⇒ 该越界是**结构性**的，**不是实现缺陷**（`identity_failed = 0`、受判样本全部可行）。

### 4.5 求解器（`S5`）

| 链 | `highs-ds` | `highs-ipm` | `presolve=False` | 最大相对差 |
|---|---|---|---|---|
| `4-2` | `0.0` | `+4.969e-6` | `0.0` | `1.25189e-4`（`D_full` 口径） |
| `4-3` | `0.0` | `+6.10152e-4` | `−2.533e-5` | `6.10152e-4` |

两链 ≤ 1% ⇒ `S5` PASS。量级与 `T7-6`（prob03 实测 `≈1e-4`）一致，属 LP 最优面选择 + 跨日初值传播的口径敏感性，**不作不稳定证据**。

### 4.6 结构情景（`S6`，分链）

| 项 | `4-2` | `4-3` |
|---|---|---|
| `buy_cap` 可行性 | **全域可行**（12/12，含 3500 kW） | 可行 `≥ 5000 kW`；不可行 `[3500, 4000, 4500]`；**分界 `(4500, 5000] kW`** |
| 机制激活 | `Σq^em` 在 `≤ 4500 kW` 时**转为正**：`4500 kW → 4,682.51 kWh（+4.757%）`、`4000 → 268,031.51（+10.468%）`、`3500 → 1,136,770.06（+24.584%）` | 各可行档 `Σq^em` 稳定在 `4.10e5` 量级（`409,721 ～ 410,639 kWh`），上限**未**改变机制；不可行由 LP 无可行解给出 |
| `terminal_e_6000` | `D_req = 13,008,590.707803`，`+0.0168%` | `D_req = 15,292,320.214739`，`+0.0214%` |

`4-2` 的 3 个低上限情景在 `raw_samples.jsonl` 的 `failures` 中带 `identity_failed = ["q_em_theorem_42", "q_em_days_42"]`：这是 `Σq^em ≡ 0` 定理在「计划购电量上限过紧」下**前提失效**的正向证据，脚本按结构族登记、**不计入 S1 受判子集**（`S1.identity_failed_scenarios = []`）⇒ `4-2` 的「`q_em ≡ 0`」结论的正确表述是「**在 `AS15` 无购电上限（本问主口径）下成立**」，上限低于约 `4500 kW` 时会出现 `q^em > 0`（量级与 prob02 `C6`/`R2` 同构，但阈值**分链重扫**、不得互引，`CF-7`/`D10`/`E9`）。

---

## 5. 稳定性结论与**分链适用边界**

- **`4-2`：稳定**。主结论（费用量级、`q^em ≡ 0`、参数方向、可行率）在预注册的五类扰动下均不反转；对价格预报误差**不敏感**（预测器替换 `0.226%`、价格白噪声 σ=10% `1.97%`），对**价格水平**近线性（`±10% ⇒ ±10.0000%`）。敏感源是**题面硬参数往返效率** `η`（`±20% ⇒ +13.35%`）与容量上界 `E_max`（`−20% ⇒ +5.14%`）。
- **`4-3`：条件稳定（需给出适用边界）**。适用边界（承 `E-F9`，并补机制）：
  1. **`σ ≤ 5%` 的输入扰动**下结论稳健（族均值偏移 `+3.45% ～ +4.04%`，`std`/`CI`/`max|z|` 全部达标）；
  2. **`σ = 10%` 的价格白噪声属超设计工况**：费用均值 `+10.59%`（S4 越界），机制 = `Σq^em` 的 `max(0,·)` 凸性系统性抬高紧急购电；
  3. 结论**对光伏侧误差不稳健**（`pv_forecast_bias +10% ⇒ +23.25%`、`pv_forecast_noise σ=20% ⇒ +22.81%`、`pv_act_noise σ=10% ⇒ +10.22%`），对价格侧（预测器替换 `0.171%`、`κ_m` 冻结 `0.0%`）稳健；
  4. 计划层的低购电上限（`≤ 4500 kW`）**结构不可行**，该档仅作结构诊断，不进入费用结论。
- **两链差异（必须显式给出，不得只报一条链）**：`4-2 = 162/162` 全成功、**稳定**；`4-3 = 195/198`、**条件稳定**。两链共用同一价格预测器与结算口径，差异来自 `4-3` 的 `0:00 + 6:00/12:00/18:00` 调整层与 `0.5×/1.5×` 分段结算 + `5×` 紧急购电。

---

## 6. 技术债与披露项（本阶段只登记，不越界改写上游）

### 6.1 本阶段新登记的冲突点与偏差（`R4-*`）

- **`R4-C1`（`plan.md` 内部冲突，必须显式记录）**：§0 表与 §1.8 声明「静置损耗**不适用/本阶段不做**」，而 §7 `CF-3` 写「本阶段**采纳**（`AS23` 效力更高），但只作结构情景」。实际执行 = **不做**（两链 162/198 情景清单中**无** `self_discharge`/`AS23` 情景）。⇒ 本报告按 §0/§1.8 执行，`CF-3` 的措辞与本阶段实际口径不一致，权威回写待 `ablation` 或 `formulation` 下一版。
- **`R4-1`（`K7` 反例）**：`K7` 在 **`max` 口径两链均不成立**（价格类 `10.0%` < `eta_both 13.35%/11.70%`），`mean` 口径成立。按 `T7-4` 如实登记 + 机制（§3 第 1–2 条），**未**修改判据、统计量或口径。
- **`R4-2`（`K8` 反例）**：`4-3 |ΔC_total|/C` **不低于** `4-2` 的预期在**价格/负载/预测器**三族上不成立（`4-3` 分别低约 `10%/17%/24%`），仅在**光伏相关族**成立（`4-3` 为 `4-2` 的 `42×/65×`）；「同号」在 `10/10` 共同情景上成立。按 `T7-4` 登记，机制 = 调整层的 `κ_m` 已实现价格校正（部分对冲）。
- **`R4-3`（`κ_m` 截断来源）**：`kappa_clip_events_total = 196` **全部**来自刻意收紧的 `kappa_bounds_tight`（`196/1,460` 层），登记界与放宽界均 `0` 次，且收紧档费用差 `0.0` ⇒ 截断在成本上非紧。**禁止**把 `196` 读作主口径发生截断。
- **`R4-4`（`4-2` 的 `α_em` 与 `e_init` 是空/近空参数）**：`4-2` 的 `α_em` 六档费用**逐位相同**（`q_em ≡ 0`）、`e_init` 六档**交付期**费用逐位相同（`2025-01-01` 初值 + 1 月预热 + 终端自由）。论文**不得**把这两族当作有效敏感性来源。
- **`R4-5`（`PF-DUAL` ≡ `PF-HIST`）**：两链上换 `PF-DUAL` 与换 `PF-HIST` 的 `D_req` **逐位相同**（`4-2: −0.2261%`、`4-3: −0.1711%`）⇒ 预测机制族**只有一条独立决策轴**；**不得**当作两个独立敏感性来源重复计数，亦**不得**由 `E-F5` 的精度表推出「某预测器更保守/更激进」。第二条独立预测轴只能由 `ablation` 的 `PF-AR` 提供（`AS08`）。
- **`R4-6`（`E-F8` 复核）**：`trajectories/` 为**抽样保存** —— `4-2 = 62 份 / 162 情景`、`4-3 = 70 份 / 198 情景`；`raw_samples.jsonl` 为 `162 / 198` 行（每情景一条）、`figures/*.png` 各 `7` 张。**不得**以轨迹份数 < 情景数判为产物缺失。
- **`R4-7`（文档计数不符）**：`RUN_LESSONS_v001.md` §2b 记「`4-2` … 70 条轨迹」，实测 `4-2 = 62`、`4-3 = 70`（疑为把 `4-3` 的计数写入 `4-2` 行）。不影响任何数值与判定，仅登记。
- **`R4-8`（`4-3` 结构统计，只作归因）**：`4-3` 基线同充放 `1,456` 时段、`q_em&c` 同时正 `6,003` 时段、`deviation_minus = 493,977.438284 kWh`。按 `AS07`/`AS16`/`CF-5`，这些**只作统计与机制归因**，不判失败、不为凑 0 改 tie-break。
- **`R4-9`（结构不可行的机制未完全定位）**：`4-3` 在 `≤4500 kW` 时不可行；失败信息显示 `3500/4500 kW` 在**计划层**、`4000 kW` 在**调整层**（`day=0, hour=12`）不可行，而 `4-2` 全域可行。本阶段只登记观测（`buy_cap` 上限作用于各层购电量块，`q^em` 不受该上限约束），**具体绑定约束块未定位**，留待 `ablation` 或 `formulation` 下一版。
- **`R4-10`（口径引用纪律，写入论文前必查）**：① `S3` 用全期 `|ΔC|/C_base`、`S4` 用交付期 `C_total` 的族统计量，两口径不同须分别标注；② 行内 `delta_c_price_yuan` 是**全期**量、`baseline_check.json`/raw 的 `delivery.delta_c_price_yuan` 是**交付期**量（`4-2 = 520,790.839035`、`4-3 = 258,485.465704` 元），**不得跨口径比较**；③ `4-2` 的「`q_em` 机制激活区间 `(4500, 5000] kW`」与 `S6`「`buy_cap` 全域**可行**」是两件事（前者是 `Σq^em > 0`、后者是 LP 可行性）；④ 龙卷表的 `spread_relative`（全期归一、`max−min`）与单档 `max/min`（交付期归一）不得混引。

### 6.2 上游设施与过程事实（承接 `E-F6`–`E-F9`，不改写）

- **`E-F6`（强制披露）**：`supervised` 模式下计算期间的任何 `RESUME`/`reconcile_tasks()` 都会把运行中任务误判为 `failed(infrastructure_transient)`；`4-3` task `5713b0a24eedeac9f000` 曾于 `14:09:29Z` 被误写、真实 worker 存活至 `15:15:47.8Z` 并自愈为 `succeeded`。**纪律**：有本地任务 `running` 时禁止 `RESUME`/`reconcile`。本动作**未**执行 `reconcile`/`list`/`RESUME`，未启动或终止任何 worker。
- **`E-F7`（证据纪律）**：`act-6e395f67061f4990`/`act-458dccfaed454c5b`/`act-6141343749294f00`/`act-47b6bc4fe0b44c88`/`act-0a5a93765e2141e7` 五条空转 `append_ledger` **不得**被 `sanity_check`/`cross_question_review`/论文引用为「已做过的鲁棒性工作」；跨日日志按时间过滤必须用完整时间戳。
- **`RUN_LESSONS_v001.md` P-1～P-4**：34 天探针的 `baseline_check=false` 属**时域口径错配**（非模型错误）；`buy_cap` 不可行属**结论**；探针 `S2/S4` 越界中 `S4` 为采样量不足（`--samples 2`）、已由正式 `--samples 25` 消解或如实登记（`4-3` 的 `noise_white_10`）；P-4 的 pid 复用致 liveness 假阴性已由 `E-F6` 定为 Harness 缺陷，本阶段按纪律规避。
- **本阶段一次通过**：两条链各 `attempt = 1`、`rc = 0`、`stderr` `0` 字节，未触发 `plan.md` §4.9 的任何重跑分支，**未产生 `run002`**，`output_directory` 未被覆盖。

### 6.3 结转欠账（本阶段无权改写，指向 `cross_question_review` / `ablation` / 论文）

`C4-1`/`C4-2`/`C4-3`/`C4-6`/`C4-7`/`C4-10`、`D8`（上游文档欠账：`shared/problem_understanding.md` §7/§10 仍把 `A8` 记为「待裁定」；`prob01/assumption_v003/version.yaml` 的 `AS08`「必然被激活」措辞）；`D10`「5000 kW 作用侧」为 `team_decision`，`prob01`–`prob04` 四池文献无一条涉及，**论文不得包装为文献支持**；`N1`–`N5`/`V1`–`V10`（sanity/visualization 技术债，含 `κ_m ∈ [0.66233, 1.54939]`、`C^fc` 备选读法、`q_em` 措辞、`c` 上限字面值、交付工作簿 md5）；`A5`/`A9`/`A10`/`A11`/`A13`/`A14`/`A18` 仍为推荐口径并保留决策点；文献池 24 条未逐篇阅读正文（15 `abstract_oa` + 9 `metadata`），本报告引用只支撑框架级/机制级主张；`question_manifest.yaml` 的 `conclusion` 三字段仍为空（`locally_completed` 门禁会报错，须在 `ablation` 结束或 `locally_completed` 前补齐，本阶段不越界代写）。

### 6.4 与 `ablation` 的边界（`plan.md` §5，未越界）

本阶段**不重跑** `S-VAR`（`R-1`）、`S-RH`（`R-2`）、`S-VAR-RH`、`C-ANCHOR-P03`（`E-F4`）、`A2-PRE`、`P2` 桥接、`M7·D2-B`/`M7·D2-C`、`PF-AR`、`D9-A`/`D9-B` 对照、`4-2` 单一长时域对照；报告引用其结论时均注明出处与口径。**robustness 图件不登记为交付图表**（未调用 `record_figure_review`），只在 §4 作敏感性证据引用。**静置损耗**（`AS23`）与**跨日 MPC**（`CF-1`）均未跑，指向 `ablation`/下一版 `formulation`。

---

## 7. 产物清单（`robustness/`）

| 产物 | 状态 |
|---|---|
| `plan.md`（预注册，跑数前冻结） | 既有（未改） |
| `code/robustness_prob04.py`、`code/task_config.yaml`、`code/task_config_4-3.yaml`、`code/task_spec.yaml`、`code/task_spec_4-3.yaml` | 既有（未改） |
| `task_submission.md`、`RUN_LESSONS_v001.md` | 既有（未改） |
| `results/prob04_v001_robust_4-2_run001/{raw_samples.jsonl, summary.json, sensitivity.json, baseline_check.json, solver_status.json, run_manifest.json, forecast_backtest.json, trajectories/, figures/}` | task `953eeb1e0b004171cad9`，`succeeded` |
| `results/prob04_v001_robust_4-3_run001/{同上}` | task `5713b0a24eedeac9f000`，`succeeded` |
| `report.md` | **本文件（本动作产出）** |

**只读复核证据（本动作，位于动作目录 `runtime/actions/act-2f778ac1e7d04071/evidence/`）**：
`probe_read_robust_summary.py` / `probe_read_robust_summary_out.txt`、`probe_digest.py` / `probe_digest_out.txt`、
`probe_sensitivity.py` / `probe_sensitivity_out.txt`、`probe_report_numbers.py` / `probe_report_numbers_out.txt`、
`probe_k7k8.py` / `probe_k7k8_out.txt`、`probe_baseline_kappa.py` / `probe_baseline_kappa_out.txt`、
`probe_kappa_clip.py` / `probe_kappa_clip_out.txt`、`probe_oat_exact.py`（其输出即 `probe_report_numbers_out.txt` 的 OAT 段与本文 §4.1）、
`verify_report_numbers.py` / `verify_report_numbers_out.txt`（**本报告引用数值的自检**：`68/68 PASS`，覆盖基线闸门、S1–S7、K7/K8、噪声族统计、`κ_m` 截断来源、产物齐备与两 task 终态）。
上述探针全部**只读**权威产物（`summary.json`/`sensitivity.json`/`baseline_check.json`/`solver_status.json`/`run_manifest.json`/`raw_samples.jsonl` 与任务 `status.json`）；**未写任何 `results/` 文件、未创建或提交 task、未启动或终止 worker、未执行 `reconcile`/`list`/`RESUME`、未改动 accepted 代码与 `plan.md`**。

**收尾命令**：`record_optional_stage(stage=robustness, decision=completed)` → `transition(target_stage=sanity_check)`（触发 `sanity_check` 的 Level 6，`scripts/automm/workflow.py:264-265` 亦会在下一次唤醒自动推进）。
