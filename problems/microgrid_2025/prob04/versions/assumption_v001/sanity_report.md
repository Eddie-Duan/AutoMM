# prob04 sanity 报告（`sanity_check` 阶段正式复核，`level_1_4`）

- problem_id: `microgrid_2025`；question_id: `prob04`（问题 4：**波动电价下重算问题 2 与问题 3**；交付 `result4-2.xlsx` 与 `result4-3.xlsx`）
- 动作：`act-d7fa5a1e75ac4428`（policy **P3** / action = `run_agent`；agent = `sanity-checker`；stage = **`sanity_check`**）
- 假设 / 公式版本：`assumption_v001`（accepted）/ `formulation_v001`（accepted）
- 复核对象（**两链分别验收，不得混淆**；均为隔离 task、attempt 1、supervised、CPU HiGHS、`rc=0`、`probe_mode=false`、365 天 / 52,560 时段）：
  - 链 `4-2`：task `892351beefa2bc5f7804` → `results/prob04_v001_f001_4-2_run002`（wall 11.93 s）
  - 链 `4-3`：task `eca261e1d516ab2867d9` → `results/prob04_v001_f001_4-3_run002`（wall 38.88 s）
- **总判定：`PASS_WITH_WARNING`**（`failure_type = null`、`return_stage = null`、`blocking_reasons = []`；无硬门禁失败）
- 独立断言总计 **88 项全部通过**（L1 27 + L2/L3 49 + L4 12）；另加通用脚本 `scripts/run_sanity_check.py` 在两链 JSON 产物的**逐字节副本**上 `automated_status = PASS_WITH_WARNING`、`failures = []`（4-2：6 个数值文件 / 703,219 个数值；4-3：6 个文件 / 742,786 个数值；均 0 NaN / 0 Inf）。
- 本动作**只读**：不重解 LP、不改代码/模型/假设/公式/原始数据/任何 `results/` 产物、未创建或提交 task、未安装依赖、未占用 GPU；唯一写入为 `runtime/actions/act-d7fa5a1e75ac4428/evidence/**` 与本版本目录的 `sanity_report.md`、`machine_sanity.json`。
- **独立性**：本次三个探针脚本（`probe_a_l1.py`、`probe_b_l2l3.py`、`probe_c_l4.py`）由本动作自行实现，**未复用**上一动作的任何探针脚本或输出；L2/L3 的全部回代只依赖 `data/` 原始附件与两链落盘序列（不 import 业务模块），L1 的实现级信息集置换测试与文献门禁为对注册实现的直接调用。
- **被取代的上一版报告**：computation 阶段 P2 `inspect_compute_result`（`act-ac9fa70f8976452b`）的 `sanity_report.md` 原文逐字节保留于
  `runtime/actions/act-d7fa5a1e75ac4428/evidence/prior_sanity_report_backup.md`（24,762 B，sha256 `C95AC0F72A84360A79903362184B87F6D9BDF2BF86374F9EA167EB1728D522F0`）。

---

## 0. 与 accepted 假设 / 团队裁定 / 团队勘误的接口（强制项，先读后验）

已全文读取 `versions/assumption_v001/assumptions.md`（760 行），尤其 §0（承接与冲突）、§5（移交口径）、§6（诊断事实）、末尾效力最高的「待团队选定的决策点 D1–D12」「# 团队裁定 R-1/R-2」「## 团队勘误 E-F2–E-F5」，以及 `formulations/formulation_v001/formulation.md`（945 行，含 §1.1–§1.5、§2、§3、§4、§8）与 `implementation.md`（584 行，含 §1.4、§5、§12.4）。冲突点与差异**逐条显式记录如下，未静默沿用旧登记**：

| 编号 | 冲突/歧义 | 权威口径（本动作执行依据） | 本动作处理 |
|---|---|---|---|
| **C4-1** | `shared/problem_understanding.md` §7/§10 仍把 **A8 记为「待裁定 / formulation 前置阻塞项」** | 团队已裁定 **`A8-(b)`**（0:00 当天电价未知、决策只用历史价、结算用附件 4 实际价） | 按 `A8-(b)` 验收；上游文档欠账**结转** `cross_question_review`（本 Agent 无权改写） |
| **P4-D1** | `problem_understanding.md` 承接 prob02 `D8-A`「单一长时域 LP」与 `A8-(b)` 不相容 | `A8-(b)` 优先 ⇒ `4-2` 改为**逐日向前递推** | 已按逐日递推验收（365 个单日 LP、`lp_calls=365`） |
| **C4-7/E3** | `prob01/assumption_v003/version.yaml` 的 AS08「必然被激活」措辞与实测 `Σs_t=0` 不符 | 「变量保留、最优解允许取 0」 | 按该口径验收；权威回写待 `cross_question_review` |
| **C4-6/D10** | 「5000 kW 作用侧」文献缺口 | `team_decision`（口径丙：并网点侧与电池侧同时 ≤5000 kW，取更严者） | 本动作**不以文献名义**为该口径背书（四池无一条涉及） |
| **E-F4** | `formulation_v001` `F-8` 原表述禁止用 prob03 数值作对照锚点 | 收窄为「禁止填充/替代/混入交付值；**允许并鼓励退化情景复现类锚点**」 | `C-ANCHOR-P03` 属 **`ablation`** 预注册项，本阶段**未跑数**、**未改变**交付值 |
| **E-F5** | 主预测器与误差指标的报告纪律（禁止只报单一指标） | 主口径 = `PF-PERSIST`（MAE/MAPE/P50 为主判据），`PF-DUAL`/`PF-HIST` 成对报 RMSE/P90/P99 | 验收时同时核 MAE/MAPE/RMSE/bias/P50/P90/P99（见 §3.2）；并复核「`PF-AR` MAE 更低但按 AS08 不参与主口径选择」 |
| **R-1/R-2** | `S-VAR`/`S-RH` 只进 `ablation` | 主口径不变 | 本阶段**未跑**、未用其数值；`S-RH` 的「超 24 h 光伏预期」登记前置仍待团队勘误通道 |
| **D2/E-F1** | `formulation §3.5` 引言 `(G-4)` 与 `(FCF3-1)` 对 `C_plan^fc` 价格基数的表述不一致 | 实现按 `(G-4)` 统一口径（与落盘 `belief.*` 逐位一致） | 本动作**复算两种读法**并**更正**上一版报告中该条款的数值错误（见 §7 **N2**） |
| **D12 噪声** | 序列含求解器容差噪声 | 判据为绝对 1e-6 + 相对 1e-8 | 按该口径验收；**4-3 存在 min c = −5.23e-9、min q_dis = −3.66e-9、E_min = 1199.9999999953**（噪声级，不判失败） |

**本动作新增登记**：`κ_m` 范围登记错误（**N1**）、`C^fc` 备选读法数值错误（**N2**）、`q_em ≡ 0` 的「逐位」措辞（**N3**）、`c ≤ 833.3333` 字面值与实现值之差（**N4**）、上一版报告引用的交付工作簿 md5 不可复现（**N5**）。详见 §7。

---

## 1. Level 1：输入、task、代码、配置、日志、输出与追踪 hash

| 检查 | `4-2` | `4-3` | 结论 |
|---|---|---|---|
| 产物齐备（工作簿 + 6 JSON） | 7/7 | 7/7 | **PASS** |
| task 状态 / attempt / rc | succeeded / 1 / 0 | succeeded / 1 / 0 | **PASS** |
| `task.json` ≡ `attempt-001-task.json` | 逐字节相同 | 逐字节相同 | **PASS** |
| worker stderr | 0 字节 | 0 字节 | **PASS** |
| `code_hash`（`hash_path(code)`）独立重算 | `e50ae1323cd83ec5ca058a134ad12064d2d1ae5a5ac9420de07c0e337bd6bb64` | 同左 | **PASS** |
| `source_config_hash` 独立重算 | `49790ae0…` | `17ce597d…` | **PASS** |
| `config_hash` 独立重算（`effective_config` + `hash_json`） | `8e779d84…` | `2f40e2c8…` | **PASS** |
| `input_hash` 独立重算（`hash_path("data")`） | `4343d799…` | 同左 | **PASS** |
| `task_id` 独立复算 | `892351beefa2bc5f7804` | `eca261e1d516ab2867d9` | **PASS** |
| 附件 md5 未变 | 附件2 `bb3e493f…`、附件3 `e8dfee65…`、附件4 `cf876237…`、模板4-2 `b878ce2b…` | 模板4-3 `75be588e…` | **PASS** |
| 交付件为新写（md5 ≠ 模板） | `3df5cd3cd941ec23d7399d8073bc561f` ≠ `b878ce2b…` | `7397029ff1a5b9c7ee715bd2e24f4468` ≠ `75be588e…` | **PASS**（指纹更正见 **N5**） |
| `run001` 保留为审计负对照 | 7 件仍在（`result4-2.xlsx` md5 `e8db4df1…`） | 7 件仍在（`ca382e71…`） | **PASS** |
| `run_manifest.code_sha256` 逐文件 | 4 个 `.py` 与磁盘 sha256 全一致 | 同 | **PASS** |
| 两链 `forecast_backtest.json` | sha256 `d6c9b422…` | 同 | **PASS**（同一 `PF-PERSIST`、同一回测协议） |
| `future_price_used_in_decision` / `attachment1_used` | `false` / `false` | `false` / `false` | **PASS** |
| 逐层信息集指纹齐备 | `plan_layer` + `settlement_layer` | `plan_layer` + `hour_0/6/12/18` + `settlement_layer` | **PASS** |
| 交付隔离 | 独立 `output_directory`（`..._4-2_run002`） | `..._4-3_run002` | **PASS** |

**信息集边界（AS07/AS22，第一手实现级验证 + 独立重建）**：
1. 把**整日 `d' > d` 的未来价格置乱**后，`PF-PERSIST(d)` 预测与 `κ_m(d)`（`m=6/12/18`）**逐位不变（差 0.0）**，`d ∈ {1,2,31,100,200,364}`；
2. 把**当日 `i > 36`（未实现段）置乱**后，计划层决策价与 `κ_6` **逐位不变（差 0.0）**；
3. **正对照**：把当日 `i ≤ 36`（已实现段，合法信息）置乱后 `κ_6` **必须改变**（6/6 成立）；
4. 独立重建的信念价与落盘 `series.price_belief_yuan_per_kwh` **逐位相同（差 0.0）**：`4-2 = p^{act}_{d−1,i}`（`2025-01-01` 回退常量 1.0）；`4-3 = κ_{ν(i)}·p^{act}_{d−1,i}`、`κ_0 ≡ 1`。
⇒ 不存在「全天电价已知」类信息集越界实现（黑名单扫描见 §4）。

**表 1 标签分支（R9/R10 同源缺陷）复核**：两链 `tables.json` 的 4 个指定日期 × 6 时段 **24/24 `label_matches=true`**，`attachment_timestamp` 为左端点 `10:00/12:00/14:00/16:00/18:00/20:00`（`H:MM`、不补零），`template_label` 为完整区间，`position = 60/72/84/96/108/120`。⇒ 34 天探针的结构盲区已在 365 天正式产物上闭合。

---

## 2. Level 2：数值有限性、范围、边界与约束残差

用**原始附件 2/4**（独立 `openpyxl` 读取）与两链 `solution.json` 的 52,560 时段序列**独立回代**（不导入 `prob04` 代码）：

| 检查（阈值） | `4-2` | `4-3` |
|---|---|---|
| `series` 11 列 × 52,560 全 finite | 通过（0 NaN / 0 Inf） | 通过 |
| 结算层等式平衡 `\|q+q_em+PV^act·Δt+q_dis−L·Δt−c−s'\|`（≤1e-6） | **3.411e-13** | **4.547e-13** |
| 状态转移 `\|E_t−E_{t−1}−0.9c+q_dis/0.9\|`（≤1e-6） | **2.728e-12** | **8.715e-8** |
| `E_{1/1,0}=6000` 与跨日状态递推结构 | 精确 | 精确 |
| `(I1d)/(I2d)` 逐日恒等式（≤1e-6） | **1.091e-11 / 2.910e-11**（与落盘一致） | **7.843e-8 / 7.843e-8**（与落盘一致；`I2 = −I1 + O(Σ平衡残差)`，故两值同量级） |
| 交付期切片 `(I1)/(I2)`（≤1e-6） | −9.31e-10 / 3.73e-9 | −1.18e-7 / 1.19e-7 |
| `q_em = max(0,−r)`、`s' = max(0,r)` 互补结构 | 逐位一致 | 逐位一致 |
| `0 ≤ s' ≤ PV^act·Δt` | 通过（越界 2.3e-13） | 通过（4.5e-13） |
| `c ≤ 5000·Δt = 833.3333…`、`q_dis ≤ 750.0000`（E1） | max 833.3333333333 / 750.0000000000 | max 833.3333333333 / 750.0000000000 |
| 两侧换算功率 `max(c/Δt, q_dis/(0.9Δt)) ≤ 5000 kW`（D10 口径丙） | 5000.000000 kW | 5000.000000000001 kW（贴界，容差内通过） |
| `E ∈ [1200, 10800]` | [1200.0000000, 10800.0000000] | [**1199.9999999953**, 10800.0000000000]（噪声级） |
| 非负性 `b/q/q_em/c/q_dis` | ≥ 0（−0.0） | min c = **−5.23e-9**、min q_dis = **−3.66e-9**（求解器容差） |
| 同充放时段数 / `q_em>0 且 c>0` | 0 / 0 | 1456 / 6003（与落盘 `statistics` 逐位一致） |
| `4-2` 定理：`q_em ≡ 0`（机制级）、`q ≡ b` | `max\|q_em\| = 2.27e-13`（0 个时段 > 1e-12）、`max\|q−b\| = 0.0` | — |
| `4-3`：`i ≤ 36 ⇒ q = b`（不回溯） | — | 0.0 |
| `4-3`：`Σq^em`（交付期） | — | **394,026.0407495883 kWh**（落盘 394,026.040750） |
| 模型自检 `solution.checks_failed` | `[]`（37 项） | `[]`（37 项） |
| 层求解状态 | 365/365 `status=0` | 1460/1460 `status=0`（`layer_status_max=0`） |

---

## 3. Level 3：量纲、公式、符号与实现一致性

### 3.1 量纲与目标口径
- `Δt = 1/6 h`；序列 `load_energy_kwh == 附件2 负载功率 × Δt`、`pv_actual_energy_kwh == 附件2 光伏功率 × Δt`（残差 **0.0**）；`series.price_actual_yuan_per_kwh == 附件4` 逐点（差 **0.0**）⇒ 价格唯一来源正确、附件 1 电价未出现（H10）。
- 目标**不含** `Δt`：`C_plan^act = Σ p^act·b`、`C_adj^act = Σ[0.5 p^act (b−q)^+ + 1.5 p^act (q−b)^+]`、`C_em^act = Σ 5 p^act q_em`，独立复算与落盘逐位一致（见 §3.3）。
- 交付期窗口 `D_req` = 0-based day 31…364（334 天，`first/last_date_index = 31/364`）；全期 `D_full` = 365 天。

### 3.2 预测器与 `κ_m`（AS08/AS11，含 **E-F5** 报告纪律）
- 三个强制基准的 **MAE / MAPE / RMSE / bias / P50 / P90 / P99** 由本动作**从原始附件 4 独立重建**（`H_d = {d' < d}`），与落盘 `forecast_backtest.json` 的最大绝对差 **1.11e-16**：

| 预测器（`D_req`，334 天） | MAE | MAPE | RMSE | bias | P50 | P90 | P99 |
|---|---|---|---|---|---|---|---|
| **`PF-PERSIST`（主口径）** | **0.08432594** | **13.7801%** | 0.12383718 | −0.00059536 | 0.0500 | 0.2294 | 0.3773 |
| `PF-DUAL`（强制基准） | 0.09338750 | 15.2904% | **0.12156633** | −0.00032801 | 0.0723 | **0.2065** | **0.3359** |
| `PF-HIST`（强制基准） | 0.10023081 | 16.3301% | 0.13687750 | +0.01316084 | 0.0710 | 0.2411 | 0.3963 |
| `PF-AR`（仅 ablation） | 0.080664（落盘；**更低但按 AS08 不参与主口径选择**） | 13.4993% | 0.109836 | — | — | — | — |

  ⇒ 选择规则 `(BT-5)`（`D_req` MAE 最小、并列取更简）无歧义指向 `PF-PERSIST`（比次优低 **10.7%**，远超 1% 并列阈值）；`PF-DUAL` 的 RMSE/P90/P99 更优这一反向事实按 **E-F5** 一并披露。
- `κ_m`（**独立复算**，不借助实现模块）：`4-3` 的 `κ_{ν(i)}·p^{act}_{d−1,i}` 与落盘信念价逐位一致（**差 0.0**）；实测范围 **`[0.6623297807778713, 1.5493928472727483]`**（最小值在 0-based day 135 的 `m=18`），`κ_0 ≡ 1`，**截断事件 0 次**（`κ_m ∈ [0.5,2.0]` 未触发）。⇒ **上一版报告记的 `[0.50133, 1.54939]` 下界不可复现**，见 §7 **N1**。

### 3.3 费用分解与「价格预报误差的代价」（AS09/AS14/AS17）

| 量（元，交付期 `D_req`） | `4-2` | `4-3` |
|---|---|---|
| `C_plan^act` | 13,006,411.041148 | 13,106,641.487373 |
| `C_adj^act`（`0.5×` 段 / `1.5×` 段） | 0 / 0 | 16,678.664745 / 505,834.124031（合计 522,512.788776） |
| `C_em^act` | 0（2.94e-10 噪声） | 1,659,896.438588 |
| **`C_total^act`（主）** | **13,006,411.041148** | **15,289,050.714738** |
| `C_total^fc`（`(G-4)` 信念口径） | 12,485,620.202113 | 15,030,565.249034 |
| `ΔC_price = C^act − C^fc` | **520,790.839035** | **258,485.465704** |
| 分项 `ΔC_plan / ΔC_adj / ΔC_em`（`D_req`） | 520,790.839035 / 0 / 0 | 227,027.014807 / 11,881.553630 / 19,576.897267 |
| `C_total^fc`（`(FCF3-1)` **字面**读法） | 12,485,620.202113（同值，`belief ≡ p^{(0)}`） | **14,728,902.478574** |
| `ΔC_price`（`(FCF3-1)` 字面读法） | 520,790.839035 | **560,148.236164** |

- 分解恒等式 `C_total − (C_plan+C_adj+C_em) = 0.0`（逐项按定义复算，两链两窗口）；与 `run_manifest.delivery` 每个字段一致；`format_residuals = []`（manifest 与 workbook_info 两处）。
- 全期（`D_full`）口径亦独立复算并与 `solution.belief` 的 `cost_total_fc / delta_c_price / delta_c_plan / delta_c_adj / delta_c_em` 五个字段一致（≤1e-6）。
- ⚠️ **口径风险（更正后）**：`(G-4)` 与 `(FCF3-1)` 两种读法在 `4-3` 交付期的 `C_plan^fc` 为 **12,879,614.472595 vs 12,577,951.702107**（差 **301,662.770488** 元），对应 `ΔC_price` 由 **258,485.465704** 变为 **560,148.236164 元**；**主交付值 `C^act` 不受影响**，`4-2` 不受影响。上一版报告写「差 315,397.65 元、ΔC_price 变为 573,883.11 元」与自身给出的两个数不自洽，**已按本动作复算更正**（§7 **N2**）。
- `C_adj` 两分段分别可复算；**`4-2` 无 `C_adj` 项**。

### 3.4 逐层目标值独立回代
- `4-2`：365 个计划层目标 `== Σ p^{(0)}·b`（`p^{(0)}` = 前一日同时段；`2025-01-01` 用回退常量 1.0），最大差 **0.0**。
- `4-3`：计划层目标最大差 **0.0**；调整层 `m=18` 目标 `== Σ_{R_18}[0.5·belief·(b−q)^+ + 1.5·belief·(q−b)^+]`（最大差 **4.5e-13**）；`m=6/12` 含**前视尾巴**（未提交段由后继层重优化、取点未落盘）⇒ 只能给下界：已提交段偏差费不高于层目标（实测超出量 −5e-10 / 4.5e-15）。**这是可追溯性局限，不是缺陷**（见 §7 **D4**）。
- `AS21` 主目标不变性：`max_primary_relative_change` = **5.0000091e-10（4-2）/ 5.0001249e-10（4-3）** ≤ 1e-9；`total_shrinks = 0`、`fallback_committed_layers = 0`、`lexicographic_committed_layers = 365 / 1460`、`all_layers_invariance_passed = true`。

### 3.5 表 1 / 表 2 / 表 3 与两个工作簿逐格回溯
- **表 1**（4 日期 × 6 时段）：指定时段最终量（`4-2`：`b`；`4-3`：`q`）+ 全天量 + 全天费用（`C_total^act,d`）逐项一致（≤4.6e-7，为 6 位小数舍入）。
- **表 2**：6 块充/放电量 == 对应 24 时段 `Σc` / `Σq_dis`（≤4.9e-7）；`0:00`/`24:00` 储电量 == `E_{d,0}` / `E_{d,144}`；充放电**分别列示不冲抵**。
- **表 3**：连续 `q_em>0` 时段**合并为区间** + 左端点标签 + 逐区间能量；两链 4 个指定日期的**区间数与端点逐条一致**（4-2 全零 → 4 行「无（0 kWh）」；4-3 分别为 5/7/9/4 个区间，合计 25，能量 ≤4.5e-7）。
- **工作簿逐格回溯**：`计划购电量` 334×144 == `b`（≤5.0e-7）；`4-3` 的 `调整购电量` 334×144 == `q`（≤5.0e-7）；`充放电量` 2004 块（334 天 × 6 块）+ 668 格储电量全部一致（≤5.0e-7）；`紧急购电量`：`4-2` 334 行全零（「—」），`4-3` **2169 行**合并区间，逐日能量差 ≤ 6 位小数舍入界（单日最多 16 个区间，最大差 2.33e-6 ≤ 16×5e-7）。

### 3.6 模型规模与追踪
- `4-2`：单日 `864` 变量 / `288` 等式 / `0` 不等式；`lp_calls = 365`、名义 LP = `1460`。
- `4-3`：单日 `2232` 变量 / `720` 等式 / `432` 不等式（计划 720 + `m=6/12/18` 各 756/504/252）；`lp_calls = 1460`、名义 LP = `5840`。
- 与 `formulation §8.6` 及 `implementation §2.2` 逐项一致；`integer_variables_main = 0`（纯 LP，无 Big-M）。

---

## 4. Level 4：常识、极端行为、文献与跨问接口

- **量级**：交付期 `C_total^act` = **1.3006e7（4-2）/ 1.5289e7（4-3）** 元，日均 38,941.35 / 45,775.60 元，均落在量级带（`[3e6,5e7]` / `[5e3,2e5]`）内 ⇒ 无 `Δt` 误乘。
- **独立构造上界与下界**：无储能可行策略（`c=q_dis=0`、`b=q=max(0,LΔt−PVΔt)`、`s'=max(0,PVΔt−LΔt)`）的**价格项**独立复算 = **17,026,782.858099 元**，与 `bounds.delivery_upper_bound_parts_yuan.plan` 逐位相同；`4-3` 另加该构造下的 `adjustment 410,414.836978` + `emergency 1,446,995.525626` = **18,884,193.220703**，与落盘一致。两链 `C_total^act` 均 ≤ 三段上界 **且** ≤ 更紧的 plan 段上界。解析下界 `p_min·Σb` = 153,835.52（4-2）/155,129.41（4-3）元，**极松**（不足实际费用 1.2%），与 `E-F2` 披露一致。
- **储能价值（相对无储能价格项）**：`4-2 = 4,020,371.82 元`；`4-3 = 1,737,732.14 元`（`4-3` 的偏差结算与光伏预报误差侵蚀了部分套利收益）。
- **日边界储电量行为（直面 prob03 `E8`/`R18`）**：两链**全部 334 个交付日**均满足 `E_{d,144} = 1200`（`4-2` 逐位 0.0；`4-3` 最大偏差 3.07e-7），`Σ_d|E_{d,144}−1200| = 0.0 / 1.60e-5`、`E_{d,144}` 唯一取值数 = **1**、`E_{d,0}` 均值 = 1200.0 ⇒ **日边界不携带电量、跨日无对冲**。这正是预注册 `S-RH` 要检验的结构事实（本阶段只登记，不跑数）。`E=1200` 时段数 9346/7256；`E=10800` 时段数 7682/3954。
- **数据事实复核（`E-F2`/`E-F3`，承团队强制披露）**：`min 0.0076 / max 1.7936 / mean 0.7661976`、**无零价**、`<0.05` 9 格、`<0.10` **28 格**、`<0.20` 300 格；深谷 `5×` 紧急电价 = **0.038 元/kWh**。全局最小在 **0-based day 115 = 1-based 第 116 天 = 2025-04-26 第 72 时段**，而 `E-F2` 原文写 2025-04-27（**差 1 天**，见 §7 **D1**）。附件 4 同时段跨日均值曲线与附件 1 曲线相关系数 = **0.9999999956**（4 位小数即 1.0000）；极值为 0.37126027/1.39524575 vs 附件 1 的 0.3713/1.3952 ⇒ 「完全相同」只在 4 位小数成立。
- **文献门禁**：`automm.research.check_key_assumptions("microgrid_2025","prob04")` ⇒ `passed = true`、`errors = []`（关键假设各自绑定 prob04 本问池内 `verified+used` 文献）。
- **跨问一致性（抽查）**：prob02 交付期 `12,233,050.830708`、prob03 `14,540,616.335652` 元 ⇒ 与 prob04 两链（13,006,411.041148 / 15,289,050.714738）**均不同**，`Σq` 亦不同（prob02 22,657,938.157435 / prob03 20,842,142.600809 / prob04 20,241,516.374467 与 20,807,826.888962）⇒ 价格情景（附件 4 vs 附件 1）可区分，**未发生互填/互相替代**；`Σs'`（`D_req`）`4-2` 与 prob02 **逐位相同（990,168.090312 kWh）**——弃光由物理盈余决定、与价格情景无关，属**结构性交叉校验通过**。
- **两链差额三分项归因恒等式**：`4-3 − 4-2 = 2,282,639.673590 元 = ΔC_plan 100,230.446225 + C_adj 522,512.788776 + C_em 1,659,896.438588`（残差 1.0e-6 为 6 位小数舍入）；两链交付期净负荷逐位相同（17,939,189.494967 kWh）⇒ 差额完全来自三项，论文必须按 `P4-D5`/`D11` 分项归因，**不得只报总差**。
- **黑名单措辞**：两链交付 JSON 产物（剔除 `blacklist_note`/`note` 声明字段）**不含**「全天电价已知」「已知全天」「0:00 即知当天电价」等表述（`A8-(b)` 派生 ⑤）。

---

## 5. 两链分列结论（阶段级强制要求，不得混淆）

- **链 `4-2`（≙ 在波动电价下重算问题 2）**：逐日 0:00 一次计划、`q ≡ b`、**无 `C_adj` 项**；**`Σq_em ≡ 0`（定理级，`max|q_em| = 2.27e-13`，0 个时段 > 1e-12；`days_with_emergency = 0`）**；`C_total^act = C_plan^act = 13,006,411.041148 元`（交付期）/ `14,755,884.322036`（全期）；`ΔC_price = 520,790.839035`（交付期）/ 566,922.170018（全期）。表 3/`紧急购电量` 全零，论文**必须给机制说明**（等式平衡 + `s ≥ 0` 自由处置 ⇒ 结算层无缺口），不得只报空表。
- **链 `4-3`（≙ 在波动电价下重算问题 3）**：0:00 计划 + 6:00/12:00/18:00 调整（`κ_m` 滚动校正）+ `0.5×`/`1.5×` 双向分段结算 + 紧急购电；`C_total^act = 15,289,050.714738 元` = `C_plan 13,106,641.487373 + C_adj 522,512.788776 + C_em 1,659,896.438588`（交付期）/ `17,181,202.515506`（全期）；**`Σq^em = 394,026.040750 kWh`（交付期，非零）**，12,356 个时段 / 365 天出现紧急购电；`C_adj` 两分段可分别复算；`q_em>0 且 c>0` 的时段数 **6003**、同充放 **1456**——按 prob03 `C7/C8` 先例，属**统计与机制归因**，**不得**判为模型失败。
- **两链共用口径核查**：同一 `PF-PERSIST`（逐层目标独立复算 + `forecast_backtest.json` 完全相同）、同一结算价格入口（附件 4 实际价）、`PV^act` 唯一入口（`actual_pv_scope = settlement_layer_only`）；差异只来自调整层、偏差结算与附件 3 光伏预报误差（与 AS13 一致）。

---

## 6. 证据与可复跑

- 本动作只读探针（`runtime/actions/act-d7fa5a1e75ac4428/evidence/`）：
  `probe_schema.py`/`_out.txt`（结构摸底）、`probe_raw_layout.py`/`_out.txt`（原始附件几何）、
  `probe_a_l1.py`→`probe_a_l1_out.json`（**27/27**）、`probe_b_l2l3.py`→`probe_b_l2l3_out.json`（**49/49**）、
  `probe_c_l4.py`→`probe_c_l4_out.json`（**12/12**）、`probe_d_summary.py`→`sanity_probe_summary.json`（汇总）。
- 通用脚本在**逐字节副本**上运行（`evidence/auto_scope_4-2/`、`evidence/auto_scope_4-3/`，只复制 JSON，不复制工作簿）：
  `automated_status = PASS_WITH_WARNING`、`failures = []`，数值文件各 6 个、703,219（4-2）/ 742,786（4-3）个数值 **0 NaN / 0 Inf**。
- 上一版报告逐字节备份：`evidence/prior_sanity_report_backup.md`（sha256 `C95AC0F7…D522F0`）。
- **本报告全部数值来自 365 天正式产物**；未使用 34 天探针数值、未重解 LP、未新建 task。

---

## 7. 技术债、披露义务与新增登记（诚实登记，均不构成硬门禁失败）

### 本动作新增更正/登记（N1–N5）

- **N1（更正：`κ_m` 范围登记错误）**：上一版报告与 `question_manifest` 记 `κ_m ∈ [0.50133, 1.54939]`。本动作独立复算（raw `A_m/B_m`，`d = 1..364`、`m ∈ {6,12,18}`）为 **`[0.6623297807778713, 1.5493928472727483]`**，最小值在 0-based day 135 的 `m=18`。`0.50133` 在附件 4 与落盘序列中**均不可复现**；**截断事件 0 次**（两种读法一致），无数值影响。**论文/下游引用一律用 `[0.66233, 1.54939]`。**
- **N2（更正：`C^fc` 备选读法的数值不自洽）**：上一版 D2 写「两读法在交付期的差为 315,397.65 元（4-3）」并把 `12,879,614.47` / `12,577,951.70` 称作 `C_total^fc`。实测：二者是 **`C_plan^fc`**，其差为 **301,662.770488 元**；对应 `C_total^fc` = **15,030,565.249034（(G-4)）vs 14,728,902.478574（(FCF3-1) 字面）**，`ΔC_price` = **258,485.465704 vs 560,148.236164 元**（**不是 573,883.11**）。方向与结论不变（主交付值 `C^act` 不受影响、`4-2` 不受影响），但论文/`ablation` 引用 `ΔC_price` 必须声明所用基数并采用更正后数值，不得跨基数比较。建议 `formulation_v002` 或团队勘误择一冻结。
- **N3（登记：`q_em ≡ 0` 的措辞精度）**：上一版记 `4-2` 的 `max|q_em| = 0.0`（逐位）。落盘 `series.q_em_kwh` 的最大绝对值为 **2.2737367544323206e-13 kWh**（0 个时段 > 1e-12）。机制级恒零成立（≤1e-9，`days_with_emergency = 0`），但非逐位精确 0；属求解器容差级呈现精度。
- **N4（登记：`c` 上限字面值与实现值）**：登记口径 `c ≤ 833.3333 kWh`（4 位小数）与实现 `c_cap = 833.3333333333333 = 5000·Δt` 相差 3.3e-5；实测 `max c = 833.3333333333334`（4-3）。**按字面 833.3333 判会误报越界**；按 `5000 kW × Δt` 与两侧功率 ≤ 5000 kW 判据均满足。论文应写 `c ≤ 5000·Δt = 833.33… kWh`。
- **N5（更正：交付工作簿 md5 不可复现）**：上一版记两交付工作簿 md5 = `1fd0d039…` / `90441bc3…`。本动作独立 md5：`4-2 = 3df5cd3cd941ec23d7399d8073bc561f`、`4-3 = 7397029ff1a5b9c7ee715bd2e24f4468`（`run001` 为 `e8db4df1…` / `ca382e71…`）；`1fd0d039`/`90441bc3` 在仓库任何文件中均**不可复现**。实质结论（两工作簿均新写、md5 ≠ 模板 `b878ce2b…`/`75be588e…`）不变，但被引用的指纹必须更正。

### 结转技术债（D1–D12）

- **D1**：`E-F2` 的最小值索引/日期与实测差 1 天（实测 `2025-04-26` 第 72 时段；原文 `2025-04-27`）；`E-F3` 的「极值完全相同」只在 4 位小数成立（0.37126027/1.39524575 vs 0.3713/1.3952）。
- **D2**：见 **N2**（已更正）。
- **D3（文档过期）**：`implementation.md §2.1` 的 `prob04_io.py` sha256 = `c2bdab84…`（修复前）与当前磁盘 `b6d1b8bd…` 不一致；§2.1/§7.2/§9 的 harness `code_hash = 7fbc9610…` 为 run001 值，权威值为 `e50ae132…`（§12.4 已登记）。
- **D4（可追溯性局限）**：`4-3` 调整层 `m=6/12` 的目标含前视尾巴，未提交段取点未落盘 ⇒ 事后只能给下界复核。
- **D5（登记与描述不一致）**：`2025-01-01` 回退常量价 1.0 实际覆盖该日四层，而 `forecast_fallbacks.scope` 声明为 `plan_layer_decision_price`；仅落在 `D_full`（预热期），不影响交付期。
- **D6（披露义务，承 `E-F2`）**：`p_min` 解析下界极松（136,297.31 元 / `p_min·Σb` 153,835.52 与 155,129.41 元）；深谷 `5×` 紧急价仅 0.038 元/kWh，讨论 `4-3` 惩罚机制时不得用全期均价外推。
- **D7（披露义务，承 `E8`/`R11` 与 `C7/C8`）**：两链差额 2,282,639.673590 元**必须**按「价格预报误差的代价 / 光伏预报误差的代价 / 偏差结算制度」三分项归因；`q_em>0 且 c>0`（6003）与同充放（1456）只作统计与机制归因，**不得**判为 AS16/AS23 违反；`ΔC_price` 符号按实测（本批为正）。
- **D8（结转上游欠账，本 Agent 无权改写）**：`shared/problem_understanding.md` §7/§10 仍把 A8 记为「待裁定」（C4-1）；`prob01/assumption_v003/version.yaml` 的 AS08「必然被激活」措辞（C4-7/E3）；`D10`「5000 kW 作用侧」为 `team_decision`（prob01–prob04 四池文献无一条涉及）。权威回写待 `cross_question_review`。
- **D9（文献边界）**：prob04 池 24 条均未逐篇阅读正文（15 `abstract_oa` + 9 `metadata`），本报告引用只支撑框架级/机制级主张；`α_em=5`、`β_def/β_over`、附件 4 价格结构与预测误差量级、`D9-B`、填报口径、`5000 kW` 作用侧、`4-2` 的 `q_em ≡ 0` **均不得**包装为文献支持。
- **D10（`ablation` 边界）**：`S-VAR`/`S-RH` 只进 `ablation`，不得替换或混入主结果；`S-RH` 的「超 24 h 光伏预期」属**新增假设**，登记完成前**不得跑数**；`C-ANCHOR-P03`（`E-F4`）是实现正确性锚点、属 `ablation`，本阶段未跑、不得写入交付值或反向写入 prob03。
- **D11（LP 退化与 tie-break 敏感性）**：`degenerate_layers = 364/365（4-2）`、`1459/1460（4-3）`；`layers_with_remaining_multiplicity = 365/984`；`layers_with_unknown_multiplicity = 0`。验收以约束残差 + 恒等式 + 目标值 + 交付表为准，**不比对逐点解唯一性**，也不得用不同 tie-break/求解器设置的结果互相比对。
- **D12（数值噪声）**：序列含 ±1e-8 kWh 级求解器容差噪声（`4-3` `min c = −5.23e-9`、`min q_dis = −3.66e-9`、`E_min = 1199.9999999953`；两侧功率在 `4-3` 贴 5000 kW：5000.000000000001）。后续逐时段写值与引用须沿用同一规整纪律。

---

## 8. 判定与路由

| Level | 对象 | 结论 |
|---|---|---|
| L1 文件 / 运行 / 追踪链 / 信息集边界 | 两链 `run002` | **PASS** |
| L2 数值 / 范围 / 约束残差 | 两链 52,560 时段序列独立回代 | **PASS** |
| L3 量纲 / 公式 / 逐层目标 / 预测器 / 逐格填报 | 目标口径、费用双口径、`κ_m`、两工作簿与表 1/2/3 | **PASS** |
| L4 常识 / 极端行为 / 文献 / 跨问 | 量级、独立界、储能价值、日边界、`E-F2/F3`、门禁、跨问区分 | **PASS_WITH_WARNING** |

**总判定：`PASS_WITH_WARNING`**（`failure_type = null`、`return_stage = null`、`blocking_reasons = []`）。
两链 `result4-2.xlsx` / `result4-3.xlsx` 及各自 `solution.json`/`run_manifest.json` **可作为 prob04 的交付口径**（`run001` 仅作审计负对照，md5 见 §1，不得作为最终答案文件）。
技术债 **N1–N5 与 D1–D12** 须在论文、`visualization`、`robustness`、`ablation` 与 `cross_question_review` 中按上述归属落地；其中 **N1/N2/N5 是对上一版登记数值的更正**，下游引用一律以本报告为准。

**路由**：`PASS_WITH_WARNING` → 不退回；按 `config/gates.yaml` 的 `sanity_check → visualization` 推进至 `visualization`（`robustness`/`ablation` 为 `config/workflow.yaml` 的 `mandatory_stages`，须在可视化后完成，否则 `locally_completed` 门禁报错）。

- 报告生成：`act-d7fa5a1e75ac4428`（2026-09-11）

---

## 9. Level 6（鲁棒性与敏感性）追加复核（本动作，**追加不改写** §1–§8）

> 触发条件：`robustness` 已完成（`optional_stages.robustness.decision = completed`、`artifacts.robustness = true`）。
> 本节的完整报告、独立复算与披露项见同目录 **`sanity_report_level6.md`**；机器可读摘要见同目录 `machine_sanity_l6.json`。

- 复核动作：`act-cb24258ea2be418a`（sanity-checker，policy P3 / `level_6`）。
- 对象：链 `4-2` = `robustness/results/prob04_v001_robust_4-2_run001`（task `953eeb1e0b004171cad9`）、
  链 `4-3` = `..._4-3_run001`（task `5713b0a24eedeac9f000`），均 `succeeded` / `rc=0` / `attempt=1` / `supervised` / `probe_mode=false`。
- 独立复算：自建只读探针 `runtime/actions/act-cb24258ea2be418a/evidence/probe_l6_verify.py` ⇒ **`270/270 PASS`、`0 FAIL`**
  （产物与追踪完整性、`plan.md` 冻结先于跑数、S1–S7、噪声族逐族 `n`/`mean`/`ddof=1 std`/`1.96-CI`/`max|z|`/族均值相对基线、
  求解器双口径、`buy_cap` 分界、`κ_m` 截断来源、K7/K8 重算、报告引用值对账、交付工作簿与模板 md5、accepted 代码指纹）；
  另在逐字节副本上跑通用脚本 ⇒ `144` 数值文件 `0 NaN / 0 Inf`、`failures = []`、`PASS_WITH_WARNING`。
- 分链结论：`4-2` = `162/162` 全成功、**稳定**；`4-3` = `195/198`、`failed_criteria = ["S4"]`、**条件稳定（需给出适用边界）**
  （σ=10% 价格白噪声族均值 `+10.5919% > 5%`，机制 = `Σq^em` 的 `max(0,·)` 凸性；承 `E-F9`，**禁止**调参/放宽判据使其"通过"）。
  K7（`max` 口径两链反例）与 K8（部分反例）已按 `T7-4` 如实登记 + 机制 + 适用边界。
- 本动作新登记技术债（**不构成硬门禁失败**）：`L6-W1`（S5 求解器最大相对差在全期/交付期两种口径下混用标注：全期 `1.25189e-4`/`4.36256e-4`、
  交付期 `4.969e-6`/`6.10152e-4`，均 ≤1%）；`L6-W2`（`plan.md` 未逐字定义 S4「族间均值差」的归一化基准：代码以族均值均值为基准 `0.4463%`/`0.5654%`，
  以基线为基准则 `0.4474%`/`0.5865%`，均 ≤3%）；`L6-W3`（`plan.md` §0/§1.8 与 §7 `CF-3` 对静置损耗/`AS23` 措辞冲突，实际执行为"不做"）；
  `L6-W4`（`trajectories/` 抽样 `62`/`70`，`RUN_LESSONS` §2b 计数笔误，不得据此判产物缺失）；`L6-W5`（`4-3` task `status.json` 残留
  `E-F6` 过期 `message`，实际终态 `succeeded`；有本地任务 `running` 时禁止 `RESUME`/`reconcile`）；`L6-W6`（`E-F7` 的 5 条空转动作未被引用）。
- **Level 6 判定：`PASS_WITH_WARNING`**（`failure_type = null`、`return_stage = null`、`blocking_reasons = []`）。
- **路由**：按 `config/gates.yaml` 的 `sanity_check → ablation` 推进至 `ablation`；`ablation` 仍为 `pending`，
  **不在本 L6 结论覆盖范围内**，本结论不替代 `ablation` 的独立验收。

- 报告生成：`act-cb24258ea2be418a`（2026-09-11）
