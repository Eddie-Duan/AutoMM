# prob01 建模假设（assumption_v003）

- 归属：`microgrid_2025` / `prob01`；阶段：`assumption_definition`
- 同步动作：`act-34dcaa4ca9094965`（负责人：assumption-manager）
- 版本目录：`problems/microgrid_2025/prob01/versions/assumption_v003/`
- 父版本：`assumption_v002`（accepted，已因团队裁定 D10/D11 退回 `assumption_definition` 重做）
- 输入依据：`request/problem.md`、`request/attachments/`（只读）、`data/`（只读）、
  `../../problem_understanding.md`、`../../global_symbols.yaml`、`shared/problem_understanding.md`、
  `shared/literature.md`、`shared/literature_pool.yaml`、`../../citations.yaml`，
  **以及 `../assumption_v002/assumptions.md` §团队裁定（补充 D10）与 §D11（效力最高）**
- 结构化记录：本目录 `version.yaml` 的 `assumptions` 列表（17 条）
- 规则依据：`wiki/assumptions-formulations.md`、`knowledge/assumptions.md`、`knowledge/evidence-policy.md`
- 不覆盖历史：**未修改 `assumption_v001/`、`assumption_v002/` 下任何文件**；两版的「推荐」行与「团队裁定」行原样保留。
- 上游影响：v002 曾被接受并迁移 `mathematical_formulation`，但**尚无任何 formulation/结果产物**
  （`question_manifest.yaml` 的 `active_formulation_version=0`、`artifacts.formulation=false`），
  故本次口径收紧不触发 stale 结果标记（manifest 的 `stale.value=false`）。

---

## 0. 与上一版本的差异（团队裁定 D10/D11 落地清单 + 一处证据更正）

> 依据 `../assumption_v002/assumptions.md` §团队裁定（补充 D10，2026-09-10）与 §D11。本节逐条列出变更点、影响面与不复用的旧口径。

| # | 变更 | v002 口径 | v003 口径 | 影响面 |
|---|---|---|---|---|
| 1 | **D10 功率上限作用侧**（团队采纳丙） | `0 ≤ c_t ≤ 833.33`、`0 ≤ q_dis_t ≤ 833.33`（隐含取并网点侧，未声明） | **`0 ≤ c_t ≤ 833.33`（并网点侧收紧）、`0 ≤ q_dis_t ≤ 750.00`（电池侧收紧）**，即两侧功率都 ≤ 5000 kW | AS06 精确陈述、§5.2 模型骨架、§6 量级；**prob01 解与费用不变**（探针：丙 ≡ 甲），prob02–prob04 放电上限收紧 |
| 2 | **作用侧显式声明** | AS06 从未声明 5000 kW 加在交流侧还是直流侧 | AS06 显式写「并网点（交流）侧 + 电池（直流）侧同时约束、取更严者」，并入 §5.2 | AS06 的定义闭合；论文须按 D10 第 4 条披露口径与甲/乙对照 |
| 3 | **D11 引用登记闭合** | `ref-baloyi-2021-tou-arbitrage` 登记 `status=used` 但无任何假设绑定（24/25） | **绑定到 AS03a**（只支撑「TOU 价差套利是储能运行的经济动机」这一机制级主张） | AS03a 的 `reference_ids`/`basis`；`status=used` 的 25 条文献**全部**被至少一条假设引用（25/25） |
| 4 | **团队 365 天风险数字更正**（`agent_inference` 修正证据，结论不变） | 团队 D10 称「电池侧放电 > 5000 kW 的天数 = 38/365（10.4%）」 | **本动作独立探针更正为 249/365（68.2%）**；峰值 5555.56 kW 不变但出现在第 1 天（团队报第 184 天） | D10 的风险披露与 prob02–prob04 费用影响预估；**D10 结论（采纳丙）不变且更强**。根因：团队探针 `.tmp/probe_b1_365days.py` 从附件 1 取价的写法有 bug（`row[1:145][0]` 只取到电价列，其余是负载/光伏/0），其 day0 目标值 = 0.0000 元，见 `evidence/diagnose_365_discrepancy_out.txt` |
| 5 | **其余口径维持** | AS01、AS02、AS03a（量纲）、AS03b、AS04、AS05、AS07–AS16 | **不变** | 模型结构、可行域（除放电上限 833.33→750.00）、目标与量纲均不变 |

### 0.1 D10 影响面量化（本动作独立探针复核）

探针：`evidence/probe_d10_intersection.py`（只读 `data/附件1.xlsx`、`data/附件2.xlsx`；输出 `evidence/probe_d10_intersection_out.txt`，exit 0）。

**（a）prob01 三口径对照（附件 1，144 时段 LP，按 v003 §5.2 骨架）**

| 口径 | `c_t` 上限 | `q_dis_t` 上限 | 全天购电费 | 表 1 的 12:00-12:10 购电量 | 相对甲 |
|---|---|---|---|---|---|
| 甲 并网点侧（= v002 AS06） | 833.33 kWh | 833.33 kWh | 35,126.95 元 | 486.40 kWh | — |
| 乙 电池侧 | 925.93 kWh | 750.00 kWh | 35,101.57 元 | 579.00 kWh | −25.38 元（−0.072%） |
| **丙 交集（两侧取严，D10 裁定）** | **833.33 kWh** | **750.00 kWh** | **35,126.95 元** | **486.40 kWh** | **±0.00 元（0.000%）** |

- **丙与甲在 prob01 上给出完全相同的解**：购电量轨迹、放电轨迹、费用均逐点一致（探针断言为 `True`）。
  原因：甲口径下最优解放电峰值仅 `q_dis_t = 715.65 kWh`（并网点 4,293.92 kW、电池侧 4,771.02 kW），
  **750.00 kWh 的电池侧上限根本未被顶到**，故取交集代价精确为零。
- 充电侧：甲、丙两口径下最优解均贴合 `833.33 kWh` 上限（并网点 5,000.00 kW、电池侧 4,500.00 kW），
  故并网点侧约束在 prob01 是紧的、电池侧充电约束（≤ 925.93 kWh）是松的。
- **风险方向**：若继续用甲口径（放电上限 833.33 kWh），电池侧放电功率最大可达 `833.33/0.9/Δt = 5,555.56 kW`，
  超过题面 5000 kW 达 11.1%；若用乙口径（充电上限 925.93 kWh），并网点充电功率同样可达 5,555.56 kW 而超限。
  丙是唯一两侧都不超题面的口径。

**（b）365 天风险量化（附件 2 的负载/光伏实际 + 附件 1 电价，按 prob01 骨架逐日独立求解；对团队 D10 数字的更正）**

| 指标 | 团队 D10 报数 | 本动作复核（更正） |
|---|---|---|
| 成功求解天数 | —（未说明） | 365/365 |
| 充电贴合 833.33 kWh 上限的天数 | 365/365（100%） | **365/365（100%）**，一致 |
| 甲口径下电池侧放电 > 5000 kW 的天数 | 38/365（10.4%） | **249/365（68.2%）** |
| 全期电池侧放电峰值 | 5,555.56 kW（第 184 天） | **5,555.56 kW（第 1 天，`q_max = 833.33 kWh`，超限 11.1%）**，峰值一致、出现日不同 |
| 丙相对甲的逐日费用增量 | 「约一成工作日上收紧，费用略升」 | 受影响 249/365（68.2%）天；均值 **18.95 元/天**、中位数 15.12 元/天、最大 62.23 元/天；全期累计 **6,916.19 元** |

- 更正根因（已定位并留证）：团队探针 `.tmp/probe_b1_365days.py` 从附件 1 取价格时对整表用 `row[1:145]` 再取 `[0]`，
  得到的 144 维「价格」向量只有首元素是电价，其余是负载/光伏/0；对照诊断见
  `evidence/diagnose_365_discrepancy_out.txt`：同一天（附件 2 第 1 天）team 路径 `cost = 0.0000` 元、本动作路径 `cost = 61,778.21` 元。
- 该更正**不改变 D10 的裁定方向**：丙仍以 0.0000% 的 prob01 代价消除一个在 68.2% 的天数上真实发生的越限；
  但它把「多日小问费用仅略升」的预估上调为「约 68% 的天数上单日费用上升数十元量级」，须在 prob02 建立后按实际负载/光伏重算并记入 robustness。
- **不得复用**：v002 `AS06` 精确陈述与 §5.2 模型骨架里 `0 ≤ q_dis_t ≤ 5000·Δt`（放电上限 833.33 kWh）的写法对下游失效。

### 0.2 D11 引用登记闭合（本动作执行）

- v002 的未绑定条目仅 `ref-baloyi-2021-tou-arbitrage` 一条（探针 `evidence/check_reference_binding.py`：v002 = 24/25）。
- 按 D11 优先方案（绑定而非降级）将其加入 **AS03a** 的 `reference_ids`，并在 `basis` 中限定为
  **机制级主张**：「TOU 价差套利是储能在电价低时段充电、高时段放电的经济动机」，不参与目标口径与量纲选择。
- 闭合校验（v003）：`citations.yaml` 中 `status=used` 的 **25/25** 条文献均被至少一条假设的 `reference_ids` 引用，
  未绑定集合为空；且不存在「引用了非 used 文献」的反向问题（见 `evidence/check_reference_binding_v003_out.txt`）。
- 未修改 `citations.yaml`、`literature_pool.yaml`、`literature.md`：本条按「绑定」而非「降级」处理，文献状态保持 `used`。

---

## 1. 假设总览

| ID | 标题 | 类型 | 关键性 | 证据类型 | 绑定文献数 | 裁定歧义 |
|---|---|---|---|---|---|---|
| AS01 | 时间戳与时段的位置对齐 | 离散化约定 | high（非文献门禁项） | team_decision | 0（题面/模板事实） | A1 / D1 |
| AS02 | 附件 1 是独立代表性单日 | 边界条件 | medium | agent_inference | 0（数据事实） | A16 |
| AS03a | 目标口径为全天计划购电费最小 | 决策偏好 | **关键** | team_decision | **3**（含 D11 新绑定） | D9 / D11 / A11 |
| AS03b | 不纳入电池折旧成本 | 决策偏好 | **关键** | project_assumption | 2 | D7 |
| AS04 | 功率平衡等式与供电不低于负载 | 机理 | **关键** | literature_fact | 4 | A5 / D3 |
| AS05 | 90% 效率作用位置（两侧） | 机理简化 | **关键** | literature_fact | 4 | A3 / D2 |
| AS06 | SOC 区间与充放电功率上限（D10 口径丙：两侧取严） | 边界条件 | **关键** | literature_fact（口径为 team_decision） | 3 | H2/H3/A12 / D10 |
| AS07 | 不显式强制同时充放电（保持 LP） | 机理简化 | **关键** | literature_fact | 2 | A5/A13 / D6 |
| AS08 | 显式弃光/盈余变量 `s_t` | 机理 | **关键** | literature_fact | 3 | A13 / D4 |
| AS09 | 时间离散与 kW→kWh 换算 | 数值近似 | **关键** | **agent_inference** | 2（旁证） | A12 / D9 |
| AS10 | 计划窗首末储电量闭合（日周期） | 边界条件 | **关键** | literature_fact | 3 | H6 / D5 |
| AS11 | 0:00 储电量取 6000 kWh | 边界条件 | high（非文献门禁项） | team_decision | 0（题面硬参数） | A15 / D5 |
| AS12 | 模型族为单日确定性 LP | 数值近似 | **关键** | literature_fact | 4 | — |
| AS13 | 表 1/表 2 的汇总与填报口径 | 报告口径 | medium | team_decision | 0（模板事实） | A10/A11 / D8 |
| AS14 | 不计静置损耗与自放电 | 机理简化 | low | common_sense | 0 | A4 |
| AS15 | 外网购电无上限、余电不上网 | 边界条件 | medium | project_assumption | 0 | A14/A13 |
| AS16 | 单日确定性口径、不做随机优化 | 边界条件 | medium | project_assumption | 1 | A8 前置 |

> **关键假设 10 条**（AS03a、AS03b、AS04–AS10、AS12）满足 `config/research.yaml` 的
> `key_assumption_requires_verified_reference`：均为模型结构、可行域或主要结论的决定项，
> 各自绑定 ≥1 条 `verified=true & status=used` 文献。
> AS01/AS11/AS13 的效力来自题面硬参数或模板事实而非文献，标 `key: false`，但以 `criticality` 标明实际影响。
> **AS06 的证据类型仍为 `literature_fact`（约束集形式有文献支撑），但其「5000 kW 作用侧」这一口径本身是
> `team_decision`（D10），不得包装为文献支持**——该区分已在 AS06 的 `basis` 中显式写出。

---

## 2. 逐条假设

### AS01 时间戳与时段的位置对齐（A1，criticality=high，团队 D1 采纳 A）

- **精确陈述**：附件 1/2/4 的 144 个数据点与 `resultN.xlsx` 的 144 个时间段**按位置一一对应**；
  附件时间戳是**所属 10 分钟区间的左端点**：位置 `i` 的时间戳 `tau_i = 10i` 分钟
  （`tau_1 = 0:10`，…，`tau_144 = 0:00+1 = 24:00`），代表区间 `[tau_i, tau_i + 10min)`。
  prob01 计划窗为 `0:10 → 24:10`（144 个等长时段）；`result1.xlsx`『计划购电量』第 `i` 行填区间 `i` 的购电量；
  表 1 的「10:00-10:10」取位置 `i = 60`（附件时间戳 10:00）。
- **依据（题面/数据事实）**：题面附录 2「表中的 0:00+1 表示第二天凌晨 0:00，即当天 24:00」；
  `result1.xlsx` 模板实测（`计划购电量` 恰 144 行，标签 `0:10-0:20` … `0:00+1-0:10+1`，含末行、无 `0:00-0:10` 行）；
  `evidence/probe_a1_alignment.py`（v001 阶段）与 `evidence/probe_d9_units.py`（v002 阶段）均复核模板 144 行、含末行、0 错位。
- **为何唯一自洽**：若把时间戳当区间右端点，模板末行 `0:00+1-0:10+1` 需一个不存在的 24:10 点、
  且缺少 `0:00-0:10` 行 → 必须外推或整体错位一格。左端点口径是唯一无需外推的读法。
- **适用边界**：prob01 全部输入；并对 prob02–prob04 的同类对齐生效。
- **偏差方向**：若改用右端点口径，表 1「10:00-10:10」会改用附件 10:10 行（i=61：电价 0.9919、负载 5897.4793、光伏 6599.3808），整表错位一格。
- **验证**：sanity L1 核对 result1 行数与标签；L3 抽查表 1 六个时段对账。
- **关系**：决定 AS10（首末状态）与 AS13（表 2 聚合相位）。
- **状态**：accepted。

### AS02 附件 1 是独立代表性单日（A16）

- **精确陈述**：附件 1 的电价/负载/光伏构成独立代表性单日曲线，不等于附件 2 中任何一天；prob01 只用附件 1。
- **依据**：`problem_understanding.md` A16 探针核验（负载最接近 2025-04-07、光伏最接近 2025-03-20，均不相等）；
  数据指纹 `data/附件1.xlsx` md5 = `dbe06f92517431228efcc26e3e796ef9`（v001/v002 一致，本版未变）。
- **适用边界**：prob01；prob02–prob04 用附件 2/3/4。
- **偏差方向**：误当作真实某日，会把「单日代表性」与「逐日」问题混同。
- **验证**：对账输入指纹 md5。
- **状态**：accepted。

### AS03a 目标口径为全天计划购电费最小（团队裁定 D9 后的量纲口径，key；D11 已闭合引用）

- **精确陈述**：`min Σ_{t=1..144} p_t · b_t`，`p_t` 为附件 1 电价（元/kWh），`b_t` 为计划购电量（kWh，**不再乘 Δt**）；
  全天购电费 `C_plan = Σ_t p_t·b_t`（元）。全文不得出现 `p_t·b_t·Δt`。
- **依据**：题面问题 1「尽可能节省微网的购电费用」（目标口径）；团队裁定 D9（量纲）；
  `ref-lee-2020-purchase-cost-lp`、`ref-olivieri-2020-residential-bess` 支撑「购电/运行费用最小可写成线性目标」的形式；
  **`ref-baloyi-2021-tou-arbitrage`（团队 D11 新绑定）**只支撑「TOU 价差套利是储能在电价低时段充电、
  高时段放电的经济动机」这一机制级主张（对应论文中储能运行的套利动机段落），
  **不参与目标口径与量纲的选择**，故不以它作为口径依据。
- **适用边界**：prob01；prob02–prob04 沿用并追加紧急/调整费用项。
- **偏差方向**：误乘 `Δt` → 费用偏小 6 倍（硬错误，方向明确），最优调度不变。
- **验证**：量纲 `元/kWh × kWh = 元`；探针给出费用可行区间 [20623, 48052] 元，最优点 35,126.95 元（丙口径，见 §6）。
- **关系**：由原 AS03 拆分；与 AS13、AS12 一致；D11 要求 25/25 used 文献均被引用，本条承载此前唯一未绑定的一篇。
- **状态**：accepted，`key: true`。

### AS03b 不纳入电池折旧/退化成本项（key，显式项目假设）

- **精确陈述**：主目标不引入循环寿命/折旧/退化成本；附录 1 无折旧系数与循环寿命，无法标定。
- **依据（反向证据）**：`ref-zhang-2015-dayahead-degradation`、`ref-rawa-2023-mg-stochastic`——日前调度常把折旧纳入目标，
  说明「省略折旧」必须写成显式 `project_assumption`，不能冒充文献事实。
- **适用边界**：prob01；折旧影响按 D7 备选 C 在 robustness 中作情景量化。
- **偏差方向**：省略折旧 → 充放电频次偏多、购电费偏低（真实全寿命成本更高）。
- **验证**：robustness 加入单位循环成本情景，观察充放电次数与费用变化方向。
- **状态**：accepted，`key: true`。

### AS04 功率平衡等式与「供电不低于负载」口径（A5，团队 D3 采纳 A，key）

- **精确陈述**：每时段 `b_t + PV_t·Δt + q_dis_t = L_t·Δt + c_t + s_t`，`s_t ≥ 0`，`0 ≤ s_t ≤ PV_t·Δt`；
  实际供给 `= b_t + q_dis_t + (PV_t·Δt − s_t) = L_t·Δt + c_t ≥ L_t·Δt`，即题面「微网提供的电能不可低于小区负载」自动成立，
  且充电不与负载竞争。不采用「净供给 ≥ 负载」弱口径。
- **依据**：`ref-moradi-2018-standalone`、`ref-csee-2023-mg-ems-review`（供需平衡为 EMS 基本约束）；
  `ref-heydari-2016-lpsp`（可靠性可显式约束）；`ref-zeinalzadeh-2017-loadshed-curtail`（两方向不平衡建模）。
- **适用边界**：prob01（无紧急购电）；prob02 起平衡式左侧追加 `q_em`。
- **偏差方向**：弱口径会把充电量算作供给，放松可行性并可能制造账面套利，费用被系统性低估。
- **验证**：sanity L1 平衡残差 ≤ 1e-6；L4 检查供给 ≥ 负载。
- **状态**：accepted，`key: true`。

### AS05 90% 效率作用位置（A3，团队 D2 采纳 A，key）

- **精确陈述**：`E_t = E_{t-1} + η_ch·c_t − q_dis_t/η_dis`，`η_ch = η_dis = 0.9`（往返 81%），单位 kWh。
- **依据**：附录 1「充放电效率为 90%」；`ref-vykhodtsev-2022-bess-review`、`ref-huang-2022-dynamic-efficiency`、
  `ref-arima-2024-rte-profile`、`ref-jamroen-2023-soc-sizing`（常效率是工程简化、往返效率随 SOC/退化变化）。
- **适用边界**：prob01–prob04；效率扰动留待 robustness。
- **偏差方向**：若题面本意为往返 90%，本假设低估可用能量、抬高费用；若只作用单侧则方向相反。
- **验证**：量纲（三项均 kWh）；极限 `η→1` 退化为无损储能；robustness 扰动 `η ∈ {0.85,0.9,0.95}`。
- **关系**：与 AS06（D10 口径丙）联立：`q_dis_t/(η_dis·Δt) ≤ 5000 kW` 这一电池侧放电上限正是效率作用在放电侧的直接后果。
- **状态**：accepted，`key: true`。

### AS06 SOC 运行区间与充放电功率上限（D10 口径丙，key）

- **精确陈述**：对所有时段 `1200 ≤ E_t ≤ 10800`（kWh）；题面「最大充放电功率 5000 kW」的作用侧按团队裁定 D10
  取**口径丙**——**同时施加于并网点（交流）侧与电池（直流）侧、取更严者**，并显式声明：
  - 充电：`0 ≤ c_t ≤ 5000·Δt = 833.33 kWh`；等价于 `c_t/Δt ≤ 5000 kW`（并网点侧，紧）且 `η_ch·c_t/Δt ≤ 5000 kW`
    （电池侧，≤ 4500 kW，松）。
  - 放电：`0 ≤ q_dis_t ≤ 5000·Δt·η_dis = 750.00 kWh`；等价于 `q_dis_t/(η_dis·Δt) ≤ 5000 kW`（电池侧，紧）且
    `q_dis_t/Δt ≤ 5000 kW`（并网点侧，≤ 4500 kW，松）。
  - 统一写法：`c_t/Δt ≤ 5000 ∧ η_ch·c_t/Δt ≤ 5000`；`q_dis_t/Δt ≤ 5000 ∧ q_dis_t/(η_dis·Δt) ≤ 5000`。
  - `E_cap_max = 12000 kWh` 仅登记为设备最大容量，运行上界取 10800 kWh。
  - 该口径在 **prob01–prob04 全文统一，不得逐问更改**。
- **依据**：附录 1 的题面硬参数（12000 / 5000 / 1200-10800）；
  `ref-shen-2026-peak-valley`（功率限值 + SOC + 充放电状态 + 日能量平衡的显式约束集）、
  `ref-alguhi-2025-bess-operation`（24h MILP 充放电动态与 SOC 约束）、
  `ref-valibeygi-2020-battery-dispatch-soc`（SOC 显式状态变量）支撑约束集**形式**。
  **作用侧口径本身来自团队裁定 D10（`team_decision`）**：题面未指明 5000 kW 加在并网点侧还是电池侧；
  团队并明确现有 25 条文献池无一条涉及该约定（最贴合的 `ref-vykhodtsev-2022-bess-review` 为 closed access，
  本轮无法取得正文），故**不得包装为文献支持**；论文若需文献支撑，须在 `mathematical_formulation` 前补定向检索
  （建议检索词：`battery energy storage` + `power rating` + `AC-side/DC-side` + `modeling`、`PCS rating`、`C-rate`）
  或取得该综述全文。
- **适用边界**：prob01–prob04。多日小问在丙口径下放电上限收紧，费用上升（量化见 §0.1）。
- **偏差方向**：若按「甲：只约束并网点侧」取放电上限 833.33 kWh，则电池侧放电功率可达 `833.33/0.9/Δt = 5555.56 kW`，
  超出题面 5000 kW 达 11.1%；若按「乙：只约束电池侧」取充电上限 925.93 kWh，则并网点充电功率同样可达 5555.56 kW 而超限。
  取交集（丙）后两侧都不超题面，代价是放电上限由 833.33 收紧到 750.00 kWh。
  另：若把功率上限直接当电量上限（漏乘 Δt），可行域会被放大 6 倍，属硬错误。
- **验证**：sanity L2/L3 检查 `E_t∈[1200,10800]`、`c_t ≤ 833.33`、`q_dis_t ≤ 750.00`，并换算两侧功率均 ≤ 5000 kW；
  本动作独立探针（`evidence/probe_d10_intersection.py`）复核 prob01：丙与甲解完全相同
  （费用 35,126.95 元、`q_dis_t max = 715.65 kWh < 750.00`，放电上限未被顶到）；
  365 天复核见 §0.1(b)（甲口径在 249/365 天越限）。
- **关系**：依赖 AS09（Δt）与 AS05（η）；与 AS05 共同约束 `E` 的转移；D10 裁定同时作用于 §5.2 模型骨架与 §6 量级参考。
- **状态**：accepted，`key: true`。

### AS07 不显式强制同时充放电互补（团队 D6 采纳 A，key）

- **精确陈述**：不引入二元变量强制 `c_t·q_dis_t = 0`；依赖 `η_ch·η_dis = 0.81 < 1` 且 `p_t > 0` 时
  「同时充放被少充少放严格支配」，并**在 sanity 中显式检查**残差 `Σ_t c_t·q_dis_t` 与同时充放时段数。
- **依据**：`ref-wang-2024-complementarity`、`ref-nazir-2023-realizable-dispatch`（先解 LP、再验证的可行性）。
- **偏差方向**：若最优解出现同时充放且未发现，SOC 轨迹与效率损失被低估。
- **验证**：sanity L1 统计同充放时段数与残差；必要时加二元变量重解对比（D6 备选 B）。
  本动作探针在甲/乙/丙三口径下均得同充放时段数 = 0。
- **状态**：accepted，`key: true`。

### AS08 显式弃光/盈余变量 `s_t`（A13，团队 D4 采纳 A，key）

- **精确陈述**：`s_t ≥ 0`（kWh）表示光伏盈余未被负载/储能消纳、也未上网的部分，`0 ≤ s_t ≤ PV_t·Δt`，
  目标系数 0。附件 1 光伏峰值 7612.32 kW > 负载峰值 5958.97 kW，部分时段潜在激活
  （光伏 > 负载 30/144 时段）；但本动作探针表明最优解下 `Σ s_t = 0`（盈余全部被储能吸收），变量仍须保留以闭合余额。
- **依据**：`ref-grimaldi-2025-arbitrage-curtail`、`ref-qing-2025-pv-curtailment`、`ref-zeinalzadeh-2017-loadshed-curtail`。
- **偏差方向**：不设 `s_t` 只写不等式 → 余额不闭合或隐性售电（费用低估）；给正收益 → 引入题面未给的售电收入。
- **验证**：sanity L1 平衡残差与上下界；统计弃光总量。
- **状态**：accepted，`key: true`。

### AS09 时间离散与 kW→kWh 换算（A12，团队裁定改标证据类型，key）

- **精确陈述**：`Δt = 1/6 h`；功率量（`L_t`、`PV_t`，kW）乘 `Δt` 得电量（kWh）；
  决策变量 `b_t`、`c_t`、`q_dis_t`、`s_t` 本身以 kWh 表示（`c_t = P_ch,t·Δt` 的口径），**不带时间因子**。
- **依据（agent_inference，团队裁定）**：附件 1 为 10 分钟粒度、恰好 144 行，`Δt = 1/6 h` 是唯一自然取法。
  `ref-lin-2024-tou-industrial`、`ref-wongwut-2017-tou-prosumer` 保留为口径旁证，不再作为证据类型依据。
- **偏差方向**：单位错配会使全部电量/费用系统性偏离 6 倍（硬错误）；与 D9 共同决定费用量纲。
- **验证**：sanity L3 量纲检查；对账全天净负荷电量 55541.97 kWh（购电量下界参考）。
- **关系**：D10 的功率上限换算（`5000·Δt`、`5000·Δt·η`）直接建立在本条之上。
- **状态**：accepted，`key: true`。

### AS10 计划窗首末储电量闭合（日周期，H6，团队 D5 采纳 A，key）

- **精确陈述**：以计划窗首时段之前状态为 `E_0`、末时段之后状态为 `E_144`，加入 `E_0 = E_144`；
  在 AS01 左端点口径下计划窗恰为 24h，等价于题面「储能在 0:00 和 24:00 的储电量相同」。
- **依据**：题面问题 1；`ref-shen-2026-peak-valley`、`ref-alguhi-2025-bess-operation`、`ref-valibeygi-2020-battery-dispatch-soc`。
- **偏差方向**：缺周期约束 → 放空/充满套利，表 2 末值失真且次日不可持续。
- **验证**：sanity L1 检查 `|E_144 − E_0| ≤ 1e-6`；表 2 的 0:00/24:00 相等。
- **状态**：accepted，`key: true`。

### AS11 0:00 储电量取 6000 kWh（A15，团队 D5 采纳 A，criticality=high）

- **精确陈述**：`E_0 = E_init = 6000 kWh`（附录 1 的 2025-01-01 0:00 电量），prob01 视作该周期代表日；由 AS10 得 `E_144 = 6000 kWh`。
- **依据（题面硬参数）**：附录 1 明示「2025 年 1 月 1 日 0:00 的电量为 6000 kWh」；问题 1 未另给初值。
- **团队确认**：`criticality=high` 且 `key=false`（效力来自题面硬参数而非文献）的处理正确，维持不变。
- **偏差方向**：周期约束下最优费用对初值不敏感（仅改变套利深度），但表 2 的 0:00/24:00 数值会变。
- **验证**：表 2 的 0:00 与 24:00 均报 6000 kWh；robustness 可扫描初值。
- **状态**：accepted。

### AS12 模型族为单日确定性 LP（团队 D6 采纳 A，key）

- **精确陈述**：144 时段确定性 LP，连续变量 `b_t, c_t, q_dis_t, s_t, E_t`；目标线性、约束线性；
  默认纯 LP，仅当 AS07 验证失败才升级 MILP。D10 的口径丙（`q_dis_t ≤ 750.00`）只是更紧的线性上界，**不改变模型族**。
- **依据**：`ref-csee-2023-mg-ems-review`、`ref-carpinelli-2013-dayahead-ilp`、`ref-ignat-2019-cost-dayahead`、`ref-alamir-2025-dayahead-multimg`。
- **偏差方向**：若实际需要随机/鲁棒优化，确定性 LP 结果偏乐观。
- **验证**：求解器状态 optimal；必要时与 MILP 版本对比目标值。本动作三口径探针均 exit 0、状态 optimal。
- **状态**：accepted，`key: true`。

### AS13 表 1/表 2 的汇总与填报口径（A10/A11，团队 D8/D9，含笔误修正）

- **精确陈述**：
  - 表 1「全天购电量」`= Σ_t b_t`（kWh）；「全天购电费」`= Σ_t p_t·b_t = C_plan`（**元**；
    prob01 无紧急/调整费用项，A11 的两义在本问不可区分）。
  - 表 2 的 6 个 4 小时块**按模板行位置聚合**：块 `b` = 计划购电量第 `24(b−1)+1 … 24b` 行（`b = 1..6`）；
    充电量 `= Σ c_t`、放电量 `= Σ q_dis_t` **分别列示、不冲抵**；「0:00 储电量」`= E_0`、「24:00 储电量」`= E_144`。
- **依据**：`result1.xlsx` 模板实测——工作表 `计划购电量`（表头 `时间段`/`购电量`，144 行）
  与 `充放电量`（表头 `时间段`/`充电量`/`放电量`/`时刻`/`储电量`，6 个 4 小时块 + `0:00`/`24:00` 两格）。
- **偏差方向**：充放电冲抵后只报净量，与「充电量/放电量」两列语义不符；按严格时钟 0:00-4:00 切分则与 AS01 的 10 分钟相位冲突（见 D1/D8）。
- **验证**：sanity L1 核对 144 行与 6 块行索引映射；表 1/表 2 与 xlsx 内容一致。
- **状态**：accepted。

### AS14 不计静置损耗与自放电（A4，common_sense）

- **精确陈述**：静置时 `E` 不变；不引入自放电参数。
- **依据**：附录 1 无自放电项。
- **偏差方向**：真实自放电使实际储电量略低，费用轻微低估（量级小）。
- **验证**：robustness 可加 0.01%/h 自放电情景。
- **状态**：accepted。

### AS15 外网购电无上限、余电不上网（A14/部分 A13）

- **精确陈述**：`b_t ≥ 0` 无上界；不建模上网/售电收益，光伏盈余只能被储能吸收或以 `s_t` 弃掉。
- **依据**：题面只给电价、未给联络线容量；未提售电渠道。
- **偏差方向**：若有隐含容量，会出现不可执行的购电尖峰；若允许售电，费用被高估。
- **验证**：检查最优 `b_t` 是否出现不合理尖峰；sanity L4。
- **状态**：accepted。

### AS16 单日确定性口径、不做随机优化

- **精确陈述**：问题 1 输入视作确定的预测值，不引入误差分布或鲁棒集。
- **依据**：题面把光伏表述为「预测数据」并直接要求计划；`ref-rawa-2023-mg-stochastic`（确定性 vs 随机调度谱系）。
- **偏差方向**：预测偏差会使实际供给不足（prob02 起由紧急购电兜底），prob01 费用低估。
- **验证**：题面既定，不做反驳实验；不确定性留待 prob03/04。
- **状态**：accepted。

---

## 3. 一致性检查（假设之间 / 与题面 / 与前问）

1. **与题面硬约束**：H1 供电可靠性 → AS04；H2 容量区间 → AS06；H3 功率上限 → AS06（D10 口径丙）+AS09；
   H4 效率 90% → AS05；H5 初值 6000 → AS11；H6 首末相同 → AS10；H7 非负 → AS04/AS06/AS08/AS15；H9 交付格式 → AS01/AS13。**无冲突**。
2. **量纲闭合（D9/D10 后复核）**：目标 `p_t·b_t` 为元；平衡式各项均为 kWh（`PV_t·Δt`、`L_t·Δt` 已换算）；
   状态转移各项均 kWh；表 1 购电量为 kWh、购电费为元；AS06 的功率约束两侧写法
   `c_t/Δt`、`η·c_t/Δt`、`q_dis_t/Δt`、`q_dis_t/(η·Δt)` 均为 kW。**无元/元·h 混用、无 kW/kWh 混用**。
3. **假设之间**：AS05 与 AS06（口径丙）联立给出电池侧放电上限 750.00 kWh；AS05 与 AS07 联立才能论证 LP
   最优解自然满足 `c_t·q_dis_t = 0`（丙口径下仍成立，探针同充放时段数 = 0）。AS04 与 AS08 必须同时存在才能闭合余额；
   AS10 与 AS11 联立固定 `E_0 = E_144 = 6000`。AS03a（目标 + D11 机制级引用）与 AS03b（不计折旧）互不冲突。**无冲突**。
4. **与前问结论**：prob01 为首个小问，无前问依赖；其结论（功率平衡形式、储能动态、费用目标与量纲、时间离散、
   **5000 kW 作用侧口径**）将由 prob02–prob04 引用，已在 `version.yaml` 的 `relations` 标注。
5. **不应隐藏的简化**：常效率 90%（AS05）、不计折旧（AS03b）、不显式强制同时充放（AS07）、无自放电（AS14）、
   无购电上限/无售电（AS15）、单日确定性（AS16）、**额定功率作用侧取交集（AS06/D10）** 均显式声明，偏差方向逐条给出。
6. **可证伪性**：AS01 可用模板逐行对账证伪；AS04/AS08 可用平衡残差证伪；AS06 可用两侧功率换算证伪
   （本动作已做，365 天 249/365 越限）；AS10 可用端点差证伪；AS07 可用同充放时段数证伪；
   AS03a 可用量纲与费用量级证伪。无「不可证伪/范围无限」条目。

---

## 4. 关键假设的文献绑定与门禁自查

- 10 条关键假设（AS03a、AS03b、AS04–AS10、AS12）各自绑定 ≥1 条 `verified=true & status=used` 来源，
  全部落在 `shared/literature_pool.yaml` 的 25 条池内（25/25 verified+used）；引用 ID 与 `citations.yaml` 一致。
- 本动作自查（真实调用 `automm.research.check_key_assumptions`）：
  `passed = true`，`errors = []`，`assumption_version = assumption_v003`
  （见 `evidence/check_key_assumptions_v003_out.txt`）。
- **D11 闭合**：`citations.yaml` 中 `status=used` 的 25 条文献**全部**被至少一条假设的 `reference_ids` 引用（25/25），
  未绑定集合为空（`evidence/check_reference_binding_v003_out.txt`）。
- 文献强度纪律：全部来源未逐篇读正文（21 条 metadata、4 条 abstract_oa），仅用于**框架级主张**；
  任何公式级/定量级引用须在 `mathematical_formulation` 取得全文后使用。
- **AS06 的双重性质**：约束集形式有文献支撑（`literature_fact`），但「5000 kW 作用侧」是 `team_decision`（D10），
  AS06 的 `basis` 已明确写出该区分，不以文献包装口径选择。
- 题面/数据事实（AS01/AS02/AS11/AS13）不由文献裁定；AS09 按团队裁定保持 `agent_inference`。

---

## 5. 移交 `mathematical_formulation` 的固定口径

### 5.1 变量与符号

沿用 `../../global_symbols.yaml`，prob01 使用单日符号（省略 `d`）：
`b_t`（计划购电量，**kWh**）、`c_t`（充电量，kWh）、`q_dis_t`（放电量，kWh）、`s_t`（弃光，kWh）、
`E_t`（时段末储电量，kWh；`E_0`/`E_144` 为计划窗首/末状态）；参数 `p_t`（元/kWh）、`L_t`、`PV_t`（kW，附件 1）、
`Δt = 1/6 h`、`η_ch = η_dis = 0.9`、`E_min = 1200 kWh`、`E_max = 10800 kWh`、
`P_max = 5000 kW`（**并网点侧与电池侧共用同一额定值，两侧取严**，D10）、`E_init = 6000 kWh`。符号不得重定义。

### 5.2 模型骨架（D9 量纲 + D10 功率口径丙）

```
min   Σ_{t=1..144} p_t · b_t                                  (AS03a, AS09；单位=元，不带 Δt)
s.t.  b_t + PV_t·Δt + q_dis_t = L_t·Δt + c_t + s_t             (AS04, AS08)
      0 ≤ c_t ≤ 5000·Δt = 833.33                               (AS06/D10：并网点侧紧)
      η_ch·c_t ≤ 5000·Δt                                       (AS06/D10：电池侧，等价 c_t ≤ 925.93，松)
      0 ≤ q_dis_t ≤ 5000·Δt·η_dis = 750.00                     (AS06/D10：电池侧紧)
      q_dis_t ≤ 5000·Δt                                        (AS06/D10：并网点侧，等价 q_dis_t ≤ 833.33，松)
      0 ≤ s_t ≤ PV_t·Δt                                        (AS08)
      E_t = E_{t-1} + 0.9·c_t − q_dis_t/0.9                    (AS05)
      1200 ≤ E_t ≤ 10800                                       (AS06)
      E_0 = 6000 ,  E_144 = E_0                                (AS10, AS11)
      b_t ≥ 0                                                  (AS15)
```

> 对比 v002：目标行不变（`Σ p_t·b_t`）；**唯一改动**是放电上限由 `q_dis_t ≤ 5000·Δt`（833.33）
> 收紧为两侧取严的 `q_dis_t ≤ 5000·Δt·η_dis`（750.00），并把充电侧的电池侧约束显式写出。
> prob01 的最优解与费用**完全不变**（`q_dis_t max = 715.65 < 750.00`）。
> 实现时可直接写 `q_dis_t ≤ 750.00`、`c_t ≤ 833.33` 两条上界，另在 sanity 中换算两侧功率核对 ≤ 5000 kW。

### 5.3 对齐与填报的落地表（AS01/AS13）

| 表 1 时段 | result1 行号（= 位置 i） | 附件 1 第 i 行时间戳 | 参考电价/负载/光伏（附件 1，探针复核） |
|---|---|---|---|
| 10:00-10:10 | 60 | 10:00 | 0.9977 / 5902.2196 / 6409.5941 |
| 12:00-12:10 | 72 | 12:00 | 0.4432 / 5519.4607 / 7601.0433 |
| 14:00-14:10 | 84 | 14:00 | 0.9754 / 5692.2983 / 6412.5085 |
| 16:00-16:10 | 96 | 16:00 | 0.8055 / 5673.4767 / 3303.8878 |
| 18:00-18:10 | 108 | 18:00 | 1.2313 / 4208.5688 / 386.6731 |
| 20:00-20:10 | 120 | 20:00 | 1.3482 / 4268.6702 / 0 |

表 2 的 4 小时块与行索引：块 1 = 行 1–24，块 2 = 25–48，块 3 = 49–72，块 4 = 73–96，
块 5 = 97–120，块 6 = 121–144；0:00/24:00 储电量分别取 `E_0`/`E_144`。

### 5.4 仍未决、需按 D 节裁定或后续阶段闭合的事项

- A2（prob02 起的结果日期范围与跨日衔接）**不在本问裁定**；prob01 的 `E_0 = 6000` 周期口径为其锚点，
  建议 prob02 启动前单独裁定并登记新版本。
- A6（附件 3 降尺度与年末边界）、A7（问题 3 结算口径）、A8（问题 4 电价可观测性）、A9（附加预报时刻分析）属 prob03/04，本问不裁定。
- 表 2 的 0:00/24:00 是「计划窗首/末状态」还是「严格时钟量」，由 D8 与 A1 共同决定（本版本取前者）。
- AS06 作用侧若需文献支撑，须在 `mathematical_formulation` 前补定向检索或取得 `ref-vykhodtsev-2022-bess-review` 全文（见 §2 AS06 的 basis）。

---

## 6. 量级参考（供 sanity L1/L3 使用）

- 附件 1 日负载电量 = **111024.81 kWh**；日光伏电量 = **55482.84 kWh**；净负荷电量 = **55541.97 kWh**。
- 附件 1 电价 ∈ [0.3713, 1.3952] 元/kWh；光伏预测 ∈ [0, 7612.32] kW；负载 ∈ [3309.39, 5958.97] kW；光伏 > 负载的时段 30/144。
- **费用量级（D9 口径，探针实测）**：解析下界 = 20622.73 元（`min p × 净负荷电量`）；
  无储能、光伏全弃上界 = 88319.36 元；无储能、按净负荷购电上界 = 48052.05 元。
  → 最优全天购电费应落在约 **[20623, 48052] 元，量级 10⁴ 元**；若落在 10³ 元即说明误乘了 `Δt`（硬错误）。
- **最优点（本动作探针，丙口径）**：全天购电费 = **35,126.95 元**；全天购电量 = **59,482.70 kWh**（≥ 净负荷 55541.97 kWh）；
  弃光 `Σ s_t = 0`；`E ∈ [1200.00, 10800.00]`、`E_144 = 6000.00`；同时充放时段数 = 0。
  甲口径费用与之完全相同；乙口径为 35,101.57 元（−0.072%）。表 1 的 12:00-12:10 购电量：甲/丙 = 486.40 kWh、乙 = 579.00 kWh。
- 储能可用能量 `E_max − E_min = 9600 kWh`；单时段充电上限 **833.33 kWh**、放电上限 **750.00 kWh**（丙）；
  往返效率 0.9×0.9 = 81%；两侧功率上限均为 5000 kW。
- 多日参考（附件 2，365 天，甲口径）：充电贴合 833.33 kWh 上限 365/365 天；
  电池侧放电 > 5000 kW 249/365 天（68.2%）、峰值 5555.56 kW；丙相对甲全期累计费用增量 6,916.19 元（日均 18.95 元）。

---

## 7. 证据与只读探针

- `runtime/actions/act-34dcaa4ca9094965/evidence/probe_d10_intersection.py`：本动作独立只读探针
  （D10 三口径对照 + 365 天越限风险 + 丙/甲逐日费用增量；只读 `data/附件1.xlsx`、`data/附件2.xlsx`）。
- `runtime/actions/act-34dcaa4ca9094965/evidence/probe_d10_intersection_out.txt`：探针输出（exit 0；
  prob01 甲/乙/丙 = 35,126.95 / 35,101.57 / 35,126.95 元；丙 ≡ 甲；365 天 249/365 越限、峰值 5555.56 kW、
  丙累计增量 6,916.19 元）。
- `runtime/actions/act-34dcaa4ca9094965/evidence/diagnose_365_discrepancy.py` / `_out.txt`：团队 365 天探针数字
  （38/365）的根因定位（团队探针取价 bug，day0 `cost = 0.0000` 元）。
- `runtime/actions/act-34dcaa4ca9094965/evidence/cross_check_team_365_out.txt`：原样重跑团队 `.tmp/probe_b1_365days.py` 的输出
  （复现其 38/365 的结果，用于证明差异来自其取价路径而非数据）。
- `runtime/actions/act-34dcaa4ca9094965/evidence/check_reference_binding.py` / `check_reference_binding_v002_out.txt` / `check_reference_binding_v003_out.txt`：
  D11 引用闭合校验（v002 = 24/25 未绑定 `ref-baloyi-2021-tou-arbitrage`；v003 = 25/25 闭合）。
- `runtime/actions/act-34dcaa4ca9094965/evidence/check_key_assumptions_v003_out.txt`：关键假设文献门禁自查（`passed=true`）。
- `runtime/actions/act-34dcaa4ca9094965/evidence/build_v003_version_yaml.py`：由 v002 结构化记录派生 v003 的构建脚本（可复现）。
- 复用既有证据：`runtime/actions/act-9f35e01ebbdd4a03/evidence/probe_d9_units.py`（D9 量纲）、
  `runtime/actions/act-0d7bdca3ddf144dd/evidence/probe_a1_alignment.*`（A1 对齐）、
  `.tmp/probe_prob01_lp.py`（v002 骨架 LP）、`.tmp/probe_b1_365days.py` / `.tmp/probe_d10_variants.py`（团队 D10 原始探针，已定位其取价 bug）。
- 未修改任何原始数据；`data/` 与 `request/` 只读；未修改 `assumption_v001/`、`assumption_v002/` 任何文件。

---

## 待团队选定的决策点

> 说明：D1–D9 的完整备选定义、利弊分析与适用边界见 `../assumption_v001/assumptions.md` §待团队选定的决策点
> 与 `../assumption_v002/assumptions.md` §待团队选定的决策点（原文未改，审计轨迹保留）；
> 团队已逐项裁定（D1–D8 采纳 A、D9 量纲修正、D10 功率作用侧、D11 引用闭合），本版本按裁定执行。
> **本版本不新增未裁定的关键建模选择**；prob01 范围内已无待选决策点。以下按精简格式复述裁定结果。

### D1 附件时间戳与 result 模板时段的 10 分钟对齐口径（A1）
- 备选 A 位置一一对应 + 左端点 / 备选 B 右端点严格时钟 / 备选 C 位置对应但强解释为时钟日。
- 推荐：A（唯一无需外推且与模板标签、表 1 名称同时自洽）。
- **团队裁定：采纳 A。**

### D2 90% 充放电效率的作用位置（A3）
- 备选 A 两侧各 90%（往返 81%）/ 备选 B 仅单侧 90% / 备选 C 往返整体 90%。
- 推荐：A（对附录措辞的最小解释，且费用最保守）。
- **团队裁定：采纳 A。**

### D3 功率平衡与「供电不低于负载」的可行域口径（A5）
- 备选 A 等式平衡 + `s_t`（充电不与负载竞争）/ 备选 B 弱口径「净供给 ≥ 负载」/ 备选 C 禁止购电充电。
- 推荐：A（唯一兼顾题面可靠性与「低价充电」动机且可闭合检验）。
- **团队裁定：采纳 A。**

### D4 光伏盈余/弃光的建模方式（A13）
- 备选 A 显式 `s_t ≥ 0`、系数 0 / 备选 B 只写不等式 / 备选 C `s_t` 加极小正惩罚。
- 推荐：A（题面未给售电渠道，系数 0 是最小假设）。
- **团队裁定：采纳 A。**

### D5 prob01 的 0:00 储电量与日周期约束（A15/H6）
- 备选 A `E_0 = E_144 = 6000` / 备选 B 仅 `E_0 = E_144`、初值自由 / 备选 C A 且按严格时钟报告。
- 推荐：A（直接采用题面唯一可用初值）。
- **团队裁定：采纳 A。**

### D6 同时充放电的处理与模型族（LP 还是 MILP）
- 备选 A 纯 LP + 事后互补性验证 / 备选 B MILP 加二元变量 / 备选 C 参数化精确松弛。
- 推荐：A（规模小、文献支持先解再验，保留升级路径）。
- **团队裁定：采纳 A。**

### D7 目标函数是否纳入电池折旧/退化成本
- 备选 A 仅购电费（折旧显式省略）/ 备选 B 目标含折旧项 / 备选 C 主目标用 A、robustness 做含折旧情景。
- 推荐：A + C 作 robustness 情景。
- **团队裁定：采纳 A，并以 C 作为 robustness 情景。**

### D8 表 2 的 4 小时时段与 0:00/24:00 储电量的聚合口径（A10/A11）
- 备选 A 按模板行位置每 24 行一块 / 备选 B 严格时钟切分 / 备选 C 同时给两套。
- 推荐：A（与 result1 结构精确对应、无需插值）。
- **团队裁定：采纳 A。**

### D9 目标函数与费用的量纲口径（团队新增，硬错误修正）
- 备选 A（裁定）：`b_t` 以 kWh 计，目标 `min Σ p_t·b_t`（元），全文禁用 `p_t·b_t·Δt`；
- 备选 B（被否）：保留 v001 的 `p_t·b_t·Δt`——数值多乘 1/6，费用偏小 6 倍，与量级自述矛盾；
- 备选 C（被否）：把 `b_t` 改为 kW 并在目标中乘 Δt——与 `result1.xlsx`「购电量」列语义及 AS09 冲突。
- 推荐：A（量纲自洽、与模板列语义一致）。
- **团队裁定：采纳 A**；影响面见 `../assumption_v002/assumptions.md` §0.1。

### D10 「最大充放电功率 5000 kW」的作用侧（团队发现；对应 AS06/§5.2）
- 备选 A（甲）**并网点（交流）侧**：`c_t ≤ 833.33`、`q_dis_t ≤ 833.33` kWh；优点：与「并网点/联络线额定功率」的常见语境一致；
  缺点：电池侧放电功率可达 5555.56 kW，**超过题面 5000 kW 达 11.1%**，且充电时电池侧只到 4500 kW（未用满额定）。
- 备选 B（乙）**电池（直流）侧**：`c_t ≤ 925.93`、`q_dis_t ≤ 750.00` kWh；优点：电池侧恒为 5000 kW；
  缺点：并网点充电功率可达 5555.56 kW，同样超限，且 prob01 费用最低（−0.072%）但站不住题面。
- 备选 C（丙）**交集：两侧都 ≤ 5000 kW、取更严者**：`c_t ≤ 833.33`、`q_dis_t ≤ 750.00` kWh；
  优点：两侧都不超题面，**在 prob01 上与甲给出完全相同的解（代价 0.0000%）**；缺点：放电上限收紧，多日小问费用上升。
- 推荐：**丙**。理由：prob01 的放电峰值 715.65 kWh < 750.00 kWh，取交集精确零代价；而甲口径在 365 天中有 249/365（68.2%）天
  电池侧放电超过 5000 kW（峰值 5555.56 kW），乙口径则并网点充电同样超限；丙是唯一不违反题面的口径。
- **团队裁定：采纳丙**，并明确：prob01–prob04 统一、论文须按 D10 第 4 条披露口径与甲/乙对照数值。
- **证据更正（本动作）**：团队 D10 引用的「38/365（10.4%）」由本动作独立探针更正为 **249/365（68.2%）**
  （根因是团队探针取价 bug）；D10 的裁定方向不变，但 prob02–prob04 的费用影响预估须按更正后的量级重算（见 §0.1）。

### D11 引用登记闭合（`ref-baloyi-2021-tou-arbitrage`）
- 备选 A：绑定到最贴切的假设（AS03a），在 `basis` 中限定为机制级主张；
- 备选 B：判定其不足以支撑本问任何主张，状态由 `used` 降级为 `rejected`，并在 `shared/literature.md` / `literature_pool.yaml` 写明降级理由；
- 备选 C：保留 `used` 但不加绑定（被否：论文会列出一条正文从不引用的文献，评阅视为「凑文献」）。
- 推荐：A（改动最小，且该文元数据级信息与「TOU 套利动机」确实对应）。
- **团队裁定：采纳 A**。本版本已执行：AS03a 的 `reference_ids` 加入该条，`citations.yaml` 25/25 `used` 全部被引用；
  若 `mathematical_formulation` 阶段判定该文不足以支撑任何主张，再按 B 降级并写明理由。

> **本版本末未新增「团队裁定」小节**：D10/D11 的裁定已由团队写入 `assumption_v002/assumptions.md`（原文保留），
> 本版本将其落入口径并复述于上。若团队要替换 D10/D11 的推荐项，请在本文件末尾**新增**「## 团队裁定」一节
> （不得改动上方「推荐」行），下一版（`assumption_v004`，上限 5 版）必须按新口径重算相关结论并在差异节显式写出。
---

## 团队勘误（团队直接修正，2026-09-10）

> **通道依据**：`agents/mathematical-formulator.md` 步骤 7 建立的**免新建版本**修正通道。本节只处理**笔误、登记错误、口径澄清**，
> **不改变任何已裁定的建模口径或模型结构**。
>
> **效力**：高于本文件正文与 `global_symbols.yaml` 的旧登记。下游阶段（implementation / computation / sanity_check）
> **必须按本节执行**，并在响应中记录采用的修正项。
>
> **触发来源**：`mathematical_formulation` 阶段（`act-6a4453e99d654790`）主动上报的 C1/C2/C3 三处上游不一致。

### E1 全局符号表 `q_dis` 的域值与 D10 不一致（对应上报项 C1）

- **原值**：`global_symbols.yaml` 中 `q_dis` 的 `domain` 写作 `0 ≤ q_dis_{d,t} ≤ P_dis_max·Delta_t`（即 833.33 kWh）。
- **新值**：`0 ≤ q_dis_{d,t} ≤ min(P_dis_max·Delta_t, P_dis_max·Delta_t·η_dis) = 750.00 kWh`。
- **理由**：团队裁定 D10 采用口径丙（两侧均不超过 5000 kW，取更严者）。放电时**电池侧为紧侧**，故上界为 750.00。
  原登记是 D10 之前的写法，属**登记未同步**，不是新的口径选择。
- **要求**：`implementation` 阶段按 750.00 实现；`sanity_check` 阶段同时换算并网点侧与电池侧功率，核对两侧均 ≤ 5000 kW。
  `prob02–prob04` 沿用 750.00。

### E2 `q_spill` 的 `first_question` 登记错误（对应上报项 C2）

- **原值**：`global_symbols.yaml` 中 `q_spill` 的 `first_question = prob02`，并注明「尚未固定是否引入」。
- **新值**：`first_question = prob01`；`s_t`（同符号）自 prob01 起即为显式决策变量（AS08／团队裁定 D4 采纳 A）。
- **理由**：登记滞后于裁定，属**登记错误**。
- **要求**：更正登记字段；符号含义不变。

### E3 AS08 措辞与实测不一致（对应上报项 C3）

- **原值**：`assumption_v003/version.yaml` 中 AS08 的 `statement` 含「该变量在部分时段**必然被激活**」。
- **新值**：变量**必须保留**以闭合功率平衡，但最优解**允许取 0**；实测 prob01 最优解 `Σs_t = 0`（盈余全部被储能吸收）。
- **理由**：「必然激活」是断言，与实测矛盾；正确表述是「变量必要、取值由优化决定」。属**措辞错误**。
- **要求**：以本文件 §2/§6 的表述为准；`version.yaml` 的该句按此更正。不影响模型与结果。

### 本勘误未覆盖的事项（仍需后续处理）

- E1/E2 的**权威落点**仍是 `global_symbols.yaml`（由 problem-decomposer 维护）。本勘误只声明**下游的执行口径**；
  `cross_question_review` 阶段须回写符号表本身。
- AS06「5000 kW 作用侧」的**文献缺口**未解决：现有 25 条池无一条涉及该约定，最贴合的 `ref-vykhodtsev-2022-bess-review`
  为 closed access。论文若需文献依据，须补定向检索或取得全文，**不得包装为文献支持**。

---

## 团队裁定（ablation 多模型对比设计与预注册判据，2026-09-10）

> **效力**：本节效力**高于**本文件正文、`formulation_v001` 的默认做法与 `agents/ablation-analyst.md` 的默认步骤。
> `ablation` 阶段 Agent **必须先读本节**，按本节指定的对照集合、预注册判据与公平性纪律执行；
> 与本节冲突的默认做法（例如只做 3–4 项内部结构消融、不做模型族对照）**不得沿用**。
>
> **通道依据**：沿用 `agents/mathematical-formulator.md` 步骤 7 与上文「团队勘误」建立的**不新建版本**修正通道。
> 本节只**追加实验设计要求**，**不改变**任何已裁定口径（D1–D11、E1–E3）与 accepted `formulation_v001` 的模型结构。
>
> **触发来源**：团队要求 prob01 为论文「模型对比」章节提供定量支撑；`optional_stages.ablation.decision` 由本节钉定为**执行**。

### A0 目的与不可替代性

- 论文的模型对比章节需要**同数据、同参数、同预算**下的多模型定量对照，不得只有定性论述。
- 主结果（表 1、表 2、`result1.xlsx`）**只以 accepted `formulation_v001`（LP）的 run002 为准**；
  对照模型的数值只进 ablation 章节，**不得覆盖或替代** `results/prob01_v003_f001_run002/`。

### A1 必做对照集合（全部实跑，不得只做定性论证）

| 编号 | 模型 | 与主模型的关系 | 必须回答的问题 |
|---|---|---|---|
| A（基准） | 完整 LP（`code/prob01_model.py`，accepted `formulation_v001`） | 内部对照 | 复跑基线须与 run002 目标值 `35126.948589` 元一致（相对容差 ≤1e-6） |
| B | MILP：加互补二元变量 `c_t ≤ M·u_t`、`q_t ≤ M·(1−u_t)` | 收紧松弛 | AS07「不显式强制同充放」是否无损？`C_MILP` 与 `C_LP` 是否相等、`mip_gap` 多少 |
| C | SOC 离散化 DP（或滚动时域近似） | 换求解路线 | 不同路线能否复现同一最优费用；离散粒度对费用偏差的影响 |
| D | 结构消融 1：去掉日周期约束 `E_0 = E_144` | 放松约束 | 该约束的经济价值＝费用下降幅度（放空/充满套利的量） |
| E | 结构消融 2：去掉弃光变量 `s_t`（改用不等式平衡） | 换平衡口径 | 是否仍可行？费用口径变化与偏差方向 |
| F | 结构消融 3（时间允许时执行） | 效率作用位置 | 单侧效率（仅充电侧或仅放电侧）与「两侧」口径的差距 |

- B/C 属**模型族/求解路线对照**，D/E/F 属**结构消融**（每次只改一项，其余保持不变）；两类都必须给出结果表。
- 若某对照在预算内无法证明最优，按 `config/gates.yaml` 的 `allow_pass_with_warning` 处理，并在结果表中**显式登记技术债**，不得隐去。

### A2 预注册比较维度（**看结果前固定，不得事后更改**）

1. **约束满足度**：可行性、功率平衡残差、SOC 界越界、`c_t·q_t = 0` 残差；
2. **题目指标**：全天购电费 `C`（元）、全天购电量（kWh）、表 1 六个时段购电量、表 2 六段充放电量与 0:00/24:00 储电量；
3. **复杂度**：变量数、约束数、求解时间、迭代次数、（MILP）`mip_gap`；
4. **解释性**：解的结构是否可解释（套利方向、同充放时段数）；
5. **鲁棒性**：对 η、`E_0` 的敏感性，可直接复用 robustness run001 的敏感性结果作为统一参考。

### A3 公平性纪律（违反即结果作废）

- **同数据**：只用 `data/附件1.xlsx`（md5 `dbe06f92517431228efcc26e3e796ef9`）；模板 `data/附件5/result1.xlsx`
  （md5 `74adc298ba5e1825482464a4ea4e26ec`）只读；
- **同参数**：`Δt = 1/6 h`、`η_ch = η_dis = 0.9`、`c_t ≤ 833.33 kWh`、`q_t ≤ 750.00 kWh`（E1）、
  `E ∈ [1200, 10800]`、`E_0 = E_144 = 6000 kWh`；
- **同预算**：求解时限 60 s、同一求解器族（HiGHS）、同一 Python/包环境，并记录 `seed`；
- **同口径**：目标一律 `min Σ p_t·b_t`（元，**不乘 Δt**，D9），费用量级应为 10^4；出现 10^3 即误乘 Δt；
- **不改动**：不得修改 accepted 的 `code/`、`formulation_v001/` 与本文件；对照实现须另建在 `ablations/code/` 下；
- **输出隔离**：每个对照独立输出目录 `ablations/results/<run_id>/`，原始样本与汇总分开保存。

### A4 交付要求（供论文直接取用）

1. 一张**模型 × 指标**对比表（含 A–F 全部条目；未执行项写「未执行 + 理由」）；
2. 2–4 张图（费用对比、MILP gap 或 DP 粒度-偏差曲线、消融瀑布图等），登记进 `figures.yaml`
   （含 `stable_id`、`included_in_paper`、`question_id`），供论文正文按 `stable_id` 引用；
3. 结论段必须回答：①核心结论是否依赖模型族（LP/MILP/DP 是否同解）；②日周期约束与弃光变量的经济贡献；
   ③论文主表推荐采用的模型及理由。

### A5 与 robustness 的边界

- `robustness`（已完成：948 情景/947 可行）负责**参数扰动与置信区间**；本节负责**模型族与结构项**对照，
  两者不得互相替代、结论不得混写。
- 若某对照结果与 robustness 的稳定性结论冲突，须在 `ablations/report.md` 中显式列出冲突点与处理方式。